# Apple Silicon (M2 Pro / macOS) adaptation of requirements.txt
# Differences from the Linux/CUDA requirements.txt:
#   - onnxruntime-gpu -> onnxruntime (CPU; no CUDA on Mac)
#   - faiss-cpu bumped to a version with arm64 wheels
#   - a few pins loosened where 1.x pins have no arm64 wheel
#   - fairseq + pyworld added: the upstream requirements.txt omits them, but
#     loading any RVC model needs them (EmbedderManager imports FairseqHubert;
#     PitchExtractorManager imports the pyworld-based Dio/Harvest extractors).
#
# Install notes:
#   - librosa 0.9.1 imports pkg_resources -> needs setuptools<81
#   - fairseq 0.12.2 has legacy omegaconf metadata -> install with pip<24.1
#     (e.g. `python -m pip install "pip<24.1"` before installing this file)
uvicorn==0.21.1
pyOpenSSL==23.1.1
numpy==1.23.5
torch==2.0.1
torchaudio==2.0.2
resampy==0.4.2
python-socketio==5.8.0
fastapi==0.95.1
python-multipart==0.0.6
onnxruntime==1.16.3
scipy==1.10.1
matplotlib==3.7.1
websockets==11.0.2
faiss-cpu==1.7.4
torchcrepe==0.0.18
librosa==0.9.1
gin==0.1.6
gin_config==0.5.0
einops==0.6.0
local_attention==1.8.5
sounddevice==0.4.6
dataclasses_json==0.5.7
onnxsim==0.4.28
torchfcpe==0.0.3
fairseq==0.12.2
pyworld==0.3.5
segno
