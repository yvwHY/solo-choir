# 2026-07-09 — Track B CAD day: frame_a 手術與分檔、卡榫工單、fusion-cad skill

All-Fusion session (no audio-path code touched). Frame doc `solo_choir_frame_a`,
new assembly doc `solo_choir_frame_assembly` (same cloud folder, Default Project).

## A. 全面檢查 → 雲霄飛車穿戴方案驗證
- Full geometric audit of the post-pivot frame (headband + nape twin tubes + collar/arm
  + chest tips): nape clearance healthy 8–10 mm; shoulder graze 0.4 mm at (±75,70,−8)
  (kept — future shoulder-rest pad); headband penetrated scalp ≤2.8 mm (his profile
  placement = hoop preload, kept).
- Harry's plan verified numerically: **door-panel DML between the chest arms + a nape
  pivot for roller-coaster donning**. Collar topology already correct for it (rear
  C-arc, front open). Swing sweep sim (rotate yoke 0–130° about (0,146,−31.5) axis X):
  arms at ±77 grazed the ears (~1 mm at 50–70°); head+ear half-width = 85.5 mm →
  **arms must clear |x|≈90 in the sweep corridor**. This number drove the day.

## B. 髮箍 Loft 崩潰事故與修復（教訓已進 fusion-cad skill）
- Loft1 had carried a "Curve 1 missing — using cached geometry" warning for days (its
  rail was deleted long ago). A `computeAll()` flushed the cache → band collapsed into
  the skull; **卡榫 chain (Plane1→Sketch24→Body67/68) mutated with it** (Body68 grew
  y132→127). Marker rollback confusion on top (bodies "vanish" = rolled back, not lost).
- Repair (proven procedure, now in the skill): rebuild the rail from the scalp contour
  at z≈140 (+1.5 mm offset, endpoints pinned to the loft's profile centroids) into
  Sketch21 → `rollTo(Loft1)` → `addCenterLine(rail)` → `moveToEnd`. Regression targets
  hit: Body68 3.90 cm³ (target 3.89), Body63/66 back to 0.01/0.03, band 7.33 (orig 6.99).
- Spline-surgery physics learned (in skill): fit-point moves silently blocked by tangent
  handles; manual handle rotation oscillates (use addHorizontal/Vertical/Parallel);
  fitted splines are C2 → a "straight" span bows 0.5–5 mm no matter what; pipe tubes end
  ~9 mm short of their spline endpoint; "body would intersect itself" = path curvature
  radius < tube radius (cusp at end = twisted handle).

## C. Harry 重設計 + 最終幾何（他塑形、Claude 工程）
- Harry replaced the loft band with a **⌀3 pipe band** (his Sketch spline + Pipe6/Mirror3),
  redrew collar/arm, deleted the whole legacy chain (old Loft/Pipes/junk bodies) — his
  undo/cleanup left a clean 19-feature timeline, zero cache bombs. Final bodies:
  nape tubes Body73/74, band Body70, yoke Body75, DML panel Body69.
- Scope-locked finishing pass (his words: 只修領圈，其他別動):
  - 圈脖子段 horizontal: collar curve z = −20.000 flat; arm neck-run pinned with
    Vertical tangent handles; residual 0.55 mm = C2 leakage (documented, accepted).
  - 胸前段 = **true straight rail**: arm spline shortened to panel-top, real SketchLine
    along the panel side edge (dir (0,−0.469,−0.883)) piped ⌀3 + mirrored (Body71/72
    → now part of his merged Body75); arm end tangent ∥ rail (0.62°); rail start
    extended +12 mm to swallow the pipe shortfall.

## D. 分檔：solo_choir_frame_assembly（motion mock-up）
- Dumb-solid copies via TemporaryBRepManager (original file untouched — safe to play):
  components **head_base** (band + nape tubes + ⌀4 pivot-pin marker, grounded),
  **yoke** (Body75), **dml_panel** (Body69).
- Two as-built revolute joints, limits set: nape pivot 0–130° at (0,146,−31.5) along X;
  door hinge about the left rail axis −100–0°. Driven via API to 120°/−70° and
  screenshot-verified — the mechanism articulates. Copies don't auto-sync; refresh on ask.

## E. 材料與製造路線（決策中）
- **Correction: the 07-08 "headset metal parts ordered" did NOT happen** — Harry hasn't
  ordered the wire stock. Materials decision now open; recommendation on the table:
  yoke = **⌀3 brass half-hard** (stiffness ∝ d⁴ — 1.2 mm is 2.6% of ⌀3; solderable
  joints; ~65 g), band = off-the-shelf hoops, 0.5 mm spring wire unchanged, so CAD ⌀3
  = manufacturing spec as-is.
- CNC wire-bending quotes estimated £300–800 one-off (per-shape setup fees; freeform
  splines often declined) → **route 1 = hand-bend on a 1:1 printed jig** for v1;
  CNC only for multi-set exhibition finals. 卡榫/joints = printed PETG (channel 3.3
  for ⌀3 wire; snap arms printed flat).

## F. 卡榫工單 + fusion-cad skill（明天的開工包）
- **Artifact 工單頁** (kept open beside Fusion): claude.ai/code/artifact/2409fe9d —
  four work orders with real coordinates + beginner steps + ID concepts + prompt library:
  WO-1 pivot blocks (hinge barrel + silicone bushing = the two-islands vibration break),
  WO-2 band×nape cross-clamp, WO-3 panel door C-clips (rail = hinge pin; DML minimal-
  constraint mounting), WO-4 BCE seat (22×18×8 pocket, 1.4 mm preload, from frame_A v1).
- **`~/.claude/skills/fusion-cad/SKILL.md`** (global): bridge gotchas (cache bombs,
  stale display meshes, suppress/unsuppress recompute, screenshot workaround, spline
  handle rules) + collaboration protocol (he shapes / Claude engineers, landmark+number
  +operation, scope discipline). No future session re-learns today's breakages.

**Next (tomorrow):** WO-1..4 卡榫 modeling in frame_a (start = WO-2 clamp for practice,
then WO-1 pivot) → refresh assembly → print PETG → wire order decision (brass ⌀3?) →
hand-bend on printed jig. Fusion files saved; repo commit = docs only.
