# 2026-08-18 — bank 門控權威改麥克風（--mic-gate，重開 F26）

Harry：「將 live 機制改成只用麥克風就可以觸發」＝明示重開 08-16「能量域門控不可靠」的判決（F26 的結論是照原計畫走備案；本日由 Harry 本人翻案，理由＝擺脫鏡頭依賴）。

## 做了什麼

**bank_live v43：`--mic-gate`（預設 0，opt-in）** — `harmony/bank_live.py`

- 門的權威從鏡頭改成 mic 電平，接在 `process_hop` 原本 `mok` 的同一個位置＝下游（f0 歸零、狀態機、dump `mok`）一行不動。
- 開門判準：hop RMS > `max(--mic-open −45dBFS, 最近 8 hop 輸出電平 + --bleed + --bleed-margin)`，連續 `--mic-frames 2` 個 hop（70ms）才開；低於地板 `--mic-hold 0.8s` 才關（值抄鏡頭門寬限）。
- 漏音地板取**最近 8 hop 輸出的最大值**而非 v33 漏音門的單點 `_OLV[-5]`：F26 量到回授峰在 +279ms ≈ 8 hop，窗最大值不賭延遲估計、只多擋不漏擋。
- 開關全用 hop 計數不用牆鐘 ⇒ `--file` 台架也生效＝**門控第一次可以離線驗證**（鏡頭門做不到的）。
- 鏡頭照跑但只當畫面：`--mic-gate 1` 時看門狗/sing-gate 不再管門。

**respond_shell 凍結配置** — `app/respond_shell.py` `_spawn_bank` 加 `--mic-gate 1 --bleed -17.5`（−17.5＝08-10 CALIB 標定寬頻耦合 k；F26 證明 k(f) 平坦 3.1dB、三法一致）。docstring 已照凍結配置慣例記來歷。

## 台架驗證（素材＝F26 同一份 226s live dump `260811_bt/ear/dump_0815/noise_mic.wav`）

| 配置 | note 觸發率 | 門開比例 |
|---|---|---|
| mic 門（新） | **36.4%** | 73.7%（71 次開，中位 1.05s） |
| 裸奔（--mouth 等效關） | 60.7% | 100% |
| 原 live 場（鏡頭門） | 39.6% | 60.9%（21 次開，中位 2.94s） |

- mic 門把假觸發壓回鏡頭門等級；與鏡頭門逐 hop 一致率 72%（鏡頭門本身非 ground truth，僅 sanity 參照）。
- 誠實邊界：①素材裡嵌的回授對應**原場**的輸出，台架的 `_OLV` 是**新場**的輸出＝漏音地板在台架上只是近似，live 才算數。②F26 的定義性死穴不變——能量域把「呼吸/動作聲」誤判成發聲 ~37%，這條沒有被繞過，只是把已標定的資產（k、延遲窗）用好。
- 觀察：mic 門較碎（開門次數 3.4×、句長 1/3）——要不要拉長 `--mic-hold` 是耳測項。

## 待 Harry 耳測

live 實跑 bank 模式（app 直接 Start 即帶新旗標）：①不唱時天使會不會自己接龍 ②句中會不會被斬 ③開門延遲（規格 70ms）體感。三項有任一不過 → 旋鈕在 `--mic-open / --mic-hold / --bleed-margin`；全不過 → 這條回墓園，鏡頭門原樣還在（旗標拿掉即回退）。

未 commit（working tree 另有 08-17 switch_fail 殘工未落，一併等裁決）。

## 下午：首場 live 翻車 → 數據調參（v43 第二輪）

Harry 實測「好像會一直觸發」。首場 dump `harmony/scratchpad/app_bank_0818_134915`（140.8s）量到：門開 88.6%、note 76.1%＝門形同虛設。病因兩條：

1. **`--mic-open -45` 太低**：房間+回授底噪坐在 -34~-28dBFS（直方圖主峰），-45 門檻永遠被跨過。他唱的電平在 -22 以上（-20~-18 有獨立峰，山谷在 -24~-22）。
2. **本機當日耦合比 CALIB 髒**：實測 mic−max(近8hop out) 中位 **-13.4dB**（p10 -17.7 恰= CALIB 的 -17.5；p90 -7.9）＝漏音地板 out-11.5 只壓得住一半回授。

**調參法＝拿該場錄音掃參數格**（唱代理＝電平≥-22 的 hop，共 10s）：選 `--mic-open -26 --bleed -13.5`（margin 維持 6 ⇒ 地板 out-7.5 ≈ 覆蓋 k 的 p90）。成績：門開 15.9%、唱捕獲 96%、回授區誤開 9%（舊值 88.6/100/88）。hold 掃過 0.8/1.0/1.2＝維持 0.8（誤開最少；句中被斬再拉）。

已改 `_spawn_bank` 凍結配置＋docstring，app 已重啟。誠實邊界：①唱代理 -22 是我從直方圖切的，非 Harry 標註；②sim 的回授嵌在舊場輸出裡＝近似，live 才算數；③F26 死穴不變。

**二測裁決：Harry「我覺得可以」＝麥克風門 PASS，live 服役。** GRAVEYARD G34 已追記。鏡頭畫面去留、hold 微調＝之後再議。

## neural 跟進：solo_min 也改 mic 門（不開鏡頭）

Harry：「neural 也改成不用鏡頭觸發，如同剛改完的 bank 模式」。

**solo_min.py 新增 `MicGate`（`--mic-gate`，預設 0）** — bank_live v43 的雙胞胎（檔內已標「任一邊改邏輯另一邊要跟著看」，同 mouth_gate 紀律）：

- 判準同 bank：hop RMS（10ms hop）> max(`--mic-open`, 最近 280ms 輸出峰 + `--bleed` + `--bleed-margin`)，連續 `--mic-frames 7` hop（70ms，換算後同 bank 的 2×35ms）開；低於地板 `--mic-hold 0.8s` 關。輸出窗 14 個 callback block（960@48k）≈ 280ms 蓋 F26 回授峰。
- 掛載點＝原 MouthGate 的 `gate.ramp()` 同一個 hook（converted voices only、dry 不門、0.12s 斜坡防喀）＝下游一行不動；`--mic-gate` 開著時 `--mouth-gate` 讓位，**cv2/mediapipe 完全不 import、鏡頭不開**。
- 全部跑在 audio thread（純算術零 I/O）；開/關轉換由 main() 0.5s 輪詢印 `[gate] mic open/close lvl/floor/open%`（08-05 playbook：callback 不印字）＝live 翻車時有 open% 可看（bank 首場 88.6% 那種診斷）。

**respond_shell `_spawn_solo`**：`--mouth-gate 1 --gate-fail open` → `--mic-gate 1 --mic-open -26 --bleed -13.5`（直接沿用 bank 今天本機掃出的值，同一支 mic 同房間）。duo 模式註記更新：鏡頭只剩 bank 開（純 UI 畫面）＝08-14「兩行程共用相機」負擔消失。回退路徑寫在 docstring：換回 08-14 那行旗標即可，鏡頭門程式原樣還在。

**台架驗證（離線，合成訊號直打 MicGate）**：5/5 過——開場關、-18dBFS 唱第 7 hop（70ms）開、靜默第 80 hop（0.8s）關、輸出 -6dBFS 時地板抬到 -13.5 擋住回授量級輸入、斜坡單調平滑無跳變。argparser 冒煙＋兩檔 py_compile＋6x venv 整檔 import（`--list`）全過。

**誠實邊界**：①-26/-13.5 是 bank 的掃格值，neural 的輸出響度組成不同（四顆嘴+choir-gain vs 音庫 MIXG）＝bleed 耦合近似，live 才算數；②鏡頭版「講話誤觸 11%」變成「講話一定觸發」——mic 門分不出唱與講，對觀眾講話要先 Stop 或 neural 拉 0（已記在 _spawn_solo 註記）。

**待 Harry 耳測**：live 跑 mode 4（neural）/ mode 3（duo）——①不唱時神經聲部會不會自己響 ②句中被斬 ③講話觸發能不能接受。旋鈕同 bank：`--mic-open / --mic-hold / --bleed-margin`。未 commit。

## UI 鏡頭顯示拿掉＝鏡頭整條退役

Harry：「UI 鏡頭顯示都拿掉」。門權威上午已改 mic，畫面一拿掉，鏡頭在整個 app 就零功能＝連 mediapipe 都不用起。

**`ui/respond_live.html`**：`#mouthCam`（影像）與 `frame` case 移除；`camCap` 留著當 bank/solo 讀數列（「不上狀態列就等於沒有」不動搖），搬回右上角原鏡頭位置。bank 讀數的 `mouth open/closed` 改標 `gate`——引擎印的欄名還叫 mouth，但 08-18 起那個值是 `M["eff"]`＝**麥克風門**狀態，照實叫 gate。

**`app/respond_shell.py`**：①`_reader` 的 FRAME 行改直接丟棄（兜底：舊版引擎/手帶旗標也不會污染 log）；②`_spawn_bank` 凍結配置 `--frame-b64 5` 與 `--sing-gate 1` 移除、加 `--mouth 0`＝鏡頭執行緒不起，省 CPU、[mouth] GATE DEAD 類故障模式消失；③docstring 逐條記來歷與回退路徑（拿掉 --mouth 0、帶回兩支旗標、UI 用 08-18 前版本）。

**驗證**：離線台架實跑 `--file`＋新旗標組（5s 合成：靜默→唱→靜默）——門 close→open（lvl -20.3 / floor -26）→close，輸出 rms 0.078，無鏡頭無炸。shell py_compile 過、UI script 塊 `node --check` 過。

**影響面**：mode 1（respond）本來就無鏡頭；mode 2（bank）鏡頭退役；mode 4（neural）今天稍早已不開鏡頭；mode 3（duo）＝兩邊都不開。整個 app 從此不碰相機。未 commit。

## 晚場：neural 和諧度工程（量化天使 v3→v4）

Harry 連環裁決：單 sop 定型 → 加 tenor → alto/tenor 四度對稱 → 「和諧度不如 ddsp 離線版」→（厚度方案被否：「不是厚度問題」）→「滑音很嚴重」→「可以更優化」。

**核心診斷**：舊 neural＝他的音準複本＋整數移調＝他飄全體跟著飄、和弦無錨；離線/bank 和諧的配方＝天使唱量化音格＋各自顫音。

**`beatrice_converter.py` 新增 quantize 模式（預設關，app 原路徑逐位元不變）**：engine shift 逐 hop ＝(絕對目標＋自有顫音)−他的連續音高。演化三版：v3 初版被 Harry 判「滑音嚴重」（q_glide 0.05/hop＝換三度滑 0.6s＋句首從上一句滑過來）；修＝句首直接落點（靜默 >200ms 重置 _qtgt＋harmonizer 記憶環，不清環會拿舊 shift 撐 140ms——台架實測）、句中 0.25/hop（三度 ~270ms 圓滑）、|cont−held|>2.5 飛點防護。顫音擬真：起音 300ms 漸開＋深度 0.5Hz ±15% 呼吸。台架：輸入飄 ±30c→輸出 0.0c 釘格；顫音 18c 設定實測 12.8c std；句首 0ms 落點。

**`solo_min.py` 加 `wet_fn` 掛鉤**（3 行，預設 None）＝合唱濕層只濕轉換聲部、乾聲不進殘響（bank [wet] 同語意）。wrapper 接 `harmony/reverb.py` 的 make_ir(t60 1.2, 48k)＋ConvLive(960)、send 0.28。全鏈 callback 台架：p95 6.3ms／預算 20ms、殘響尾 −20→−57dB 正常衰減。

**現役 wrapper**（`scratchpad`（session 暫存）`/neural_sop.py`，未進 repo）：量化 alto 四度上（satb2 spk0）＋tenor 四度下（new25_2k）＋乾聲；+5dB；起音錯開 25/40ms；mic 門 -24。待 Harry 耳測。被砍方案留檔：厚度 v2（2×2 拷貝＋活失諧）確認「不是厚度問題」。

⚠ 兩筆引擎改動（quantize、wet_fn）都預設關、只有 wrapper 開；worklog 記錄即為存檔點，Harry 過耳後再議是否轉正進 app。

## 量化天使轉正進 app neural 模式（Harry：「這個先更新進去 neural 模式」）

**`solo_min.py` 加正式 CLI 旗標**（預設全關＝原路徑不動）：`--voices N`（只建前 N 聲部）、`--quantize`（含純律＋共享調音中心，voice1 leader）、`--q-glide`、`--vib "c@Hz,…"`、`--wet-send/--wet-t60`（harmony/reverb.py ConvLive，只濕轉換聲部）。

**`_spawn_solo` 凍結配置換血**：alto（satb2 spk0）四度上＋tenor（new25_2k）四度下＋乾聲＝三部；`--quantize 1 --q-glide 0.4 --vib 14@5.2,12@4.6 --wet-send 0.28 --voice-delay 0.025,0.04`；ngain 預設 0.5→1.0、you 0.6→0.4（＝+5dB 帳）；四聲部 sop/bass 舊制退役（回舊制＝git 08-18 前的本函式）。docstring 逐條記來歷。

**晚場 quantize 演化補記（v3 之後）**：「音高有點問題」＝補償平滑拖行（死區修）＋飛點凍結讓輸出跟他走出 165c 走音（≥3 hop 改釘舊目標）；「降音天使不動」＝harmonizer median+遲滯把路過值提交成 held、落點差 1 半音卡遲滯縫（單測+引擎台架雙重定罪）→ **quantize 自管音符提交：只收站穩 30ms 的音高**（beatrice_converter 整段重寫）。回歸四場景：降全音 20ms（原卡死）、上行不咬路過音（<60c）、長音抖 1.2c、句首零下滑。CLI 全鏈 callback p95 6.3ms/20ms。

app 已重啟＝模式 4（neural）即新配置；模式 3（duo）自動繼承。待 Harry app 內耳測。未 commit（整日改動一批等裁決）。

## free×quantize 接線（Harry：「四度不適用大多數歌…free 模式試試看」）

風險評估先行（Harry 問「會適應不良嗎」）：多數互相加分（站穩提交擋掉經過音誤觸換和弦；voice-lead 共同音留住＝量化天使的強項；純律在三/六度才真正發力），唯一真雷＝**雙重驅動**：quantize 下舊判音鏈（median+遲滯）仍空轉，接 free 會讓兩條鏈搶同一個和弦狀態。

**實作（beatrice_converter）**：①quantize 模式不再呼叫 compute_shift（舊鏈整段斷開，`_held_midi` 保持 None＝台架驗證）；②目標來源改雙路：`auto_chord` 開＝`_auto_chord_target(量化 held)`、關＝原 fixed 映射；有快取（held 換了才重算一次＝和弦狀態唯一驅動源、不逐 hop 打抖）。

**台架**（C 大調旋律 C-E-G-A-F-D-C，alto+3/tenor−3 各跑）：和弦音配置正確、共同音留住（alto 在 F→D 上留 A3）、聲線跳動 ≤5 半音、舊鏈斷開確認。已知小瑕：圓滑中偶有半音級瞬時提交（est 慣性讓站穩檢查邊界過），被「和弦撐到不合才換」吸收＝目標未受影響，先觀察。

**app**：`_spawn_solo` 加 `--free --voice-lead`（**試驗中未認證**，回固定四度＝刪兩旗標）；另 UI 新增 bank 欄（duo 平衡：bank_live --gain，Start 時生效）、you 預設 0.4→0.3（Harry：「user 聲音有點大」）。app 已重啟。未 commit。

## 晚場終判：neural 量化 free 四部制 PASS（Harry「可以 過」）

耳裁鏈完整走完：free×quantize 接上（「確實比平行好」）→「缺屋頂」＝上聲部錨 +3→+7（八度區）→「第三個天使補中間」＝三天使四部（屋頂 satb2♀@+7／中層 satb2♀@+3／tenor@−3／乾聲）→「轉音糊」＝滑速 0.4→0.7、--q-snap 新旗標 2.5、殘響 t60 0.9、錯開 20/28/36ms →「可以 過」。

**現役 neural 凍結配置＝_spawn_solo 現狀**（docstring 逐條有來歷）。全鏈：量化天使（站穩 30ms 提交、自管音符、防滑三件套）＋純律＋共享調音中心＋free 和弦推測＋voice-lead＋各自顫音（漸開+呼吸）＋合唱濕層＋mic 門 -24。UI 新增 bank 欄（duo 平衡）、you 預設 0.3。

## duo 平衡終判：0.5/0.5（Harry）

實作＝按模式分流：UI bank 欄改留空＝按模式用預設（mode 2 維持認證 1.0、duo 自動 0.5）；neural 0.5×既有 duo 砍半＝0.25。手動填值兩模式都照填的。commit 529e0cad 之後的補充，隨下批收。

## UI 欄位全稽核＋修復（Harry:「ui上所有欄位是影響什麼功能？檢查是否每個都正常運作」→「好都修」）

**稽核法**：20 個可操作元件逐一追 UI→shell→引擎（行號核對＋引擎 argparse 旗標存在性）＋git 歷史比對預設值來歷。接線全通，但抓到一類系統性病：**裁決值只寫進 shell 的「欄位留空」備用值，而 UI 欄位從不留空＝裁決從未生效**。

**病例（同一天 529e0cad 埋的）**：①you 裁 0.3（「user 聲音有點大」）→ UI 仍 0.6，每場實跑 0.6＝裁決值兩倍；②ngain 裁 1.0（+5dB 帳）→ UI 仍 08-14 的 0.5，solo 模式實跑一半。⚠ 這代表晚場耳裁 PASS 時 app 內實效值是 ngain 0.5/you 0.6（除非 Harry 當場手填過）——**下次 Start 聲音平衡會變（1.0/0.3），首場要耳確認**。

**修復（ui/respond_live.html＋app/respond_shell.py）**：
1. ngain/yougain 改留空＋placeholder（bank 欄 08-18 已立的同一慣例）＝shell 裁決預設真正生效。假 Popen 台架驗過：solo 空欄→choir-gain 1.0/you 0.3；duo→bank 0.5/choir 0.25/you 0.3；stream→bank 1.0；填值照填的。
2. 欄位按模式變灰＋鎖輸入（FIELD_MODES/applyRelevance）：gain/gap/stab/max-min/reverb/tail s/map 七欄只亮 respond；ngain/you 亮 solo+duo；bank 亮 stream+duo；key 除 stream 全亮。值照留，disabled 不影響 opts() 讀值（Chrome 實測）。
3. 切模式清 respond 專屬統計五格（PHRASES/WAIT/KEY/SEP/IO）＝不再凍舊數字。
4. key=auto/怪值在 solo/duo 會被降成 C——現在 Start 與 switched 都上大警告條講明（warnKeyDowngrade；Eb 等合法調名不警告，Chrome 實測）。
5. start() docstring 從 08-04 五欄更新成現役全欄。

**驗證**：py_compile PASS；UI 經 http.server+Chrome 實載：console 零錯誤、四模式灰亮矩陣正確、統計清空、警告觸發/不觸發各 case 過。**沒開真 app**（GUI+麥克風）＝整鏈 smoke test 仍是 STATE 既列待辦。

**稽核記錄但不修（各有理由）**：同模式短路（:656 by design）；stream_on/stream_st 死 pattern（spike_stream 回退路徑文件明列，留）；solo_min 有 --out-map 但 shell 不接（接了會動已認證路由，viva 前不動）；respond 乾聲固定 0.944 調不到（動了改認證聲音，要 Harry 裁）；duo 註解「填了值照他的」與實碼「填值也砍半」矛盾（行為 08-17 review #17 定的，只是註解語病，未動）。完整對照表另存 session scratchpad ui_field_map.md。

## 分聲道輸出開到全部 live 模式（Harry：「我要 neural, live, live+neural 都可以進行支援分聲道輸出」）

**制度宣告**：預設路徑＝Regime A；--out-map 新路徑＝掛旗標新行為。G11 只死「加寬」不死路由；F7＝單一多聲道裝置。

**bank_live 新增 `--out-map`**（原本完全沒有）：每聲部一道，順序＝VOICES 現值（--tenor 1 ＝ bass,tenor,alto,sop），整聲部（領唱＋團員＋pad）進同一道、不 pan；殘響 mono 匯流排回灌全部聲道（OMAP 下匯流排用 sum 不用 mean＝完整混音）。改動點：argparse、OMAP/NCH 解析（VOICES 定案後）、process_hop y 寬度、三處 pan 寫入加 OMAP 分支、worker 靜音塊、OB/LB/cb 寫出、sd.Stream channels。**Regime A 證明：--file 前後渲染 cmp 逐位元相同**（omap_before/after.wav，rms 0.1038）。路由版驗證：4 道齊唱（rms .118/.091/.074/.064）、道間相關 0.29-0.70（不是複製）、四道加總能量與舊版同量級。

**solo_min 修 out-map 路徑的濕層洞**：out-map 分支原本整段跳過 wet_fn＝一開分聲道，08-18 認證的合唱殘響就靜默消失。修＝存 _wet_rv/_wet_send 零件，out-map 路徑走 mono 匯流排（轉換聲部加總）回灌每一道（同 bank [wet] 擴散場語意）；乾聲照舊不進殘響。mono 路徑逐字未動。

**shell 接線**：_spawn_solo 把 UI 的 D·U·L·S 轉譯成 solo_min 語意（U=屋頂+7、S=中層+3、L=tenor−3 ＝ --out-map v1,v2,v3；D=--dry-ch；S 留空跟 U）；_spawn_bank 把新 UI 欄 B·T·A·S 原樣傳 --out-map。假 Popen 台架：4格/3格/留空/duo 雙引擎各拿各的，全過。

**UI**：新增 map B·T·A·S 四格（bank 四聲部）；FIELD_MODES 更新（D·U·L·S＝respond/solo/duo、B·T·A·S＝stream/duo）。過程抓到自己的 bug：applyRelevance 群組特例只寫 mapgrp，bmapgrp 的 select 沒被鎖——改成「span 包 select 就鎖裡面」。Chrome 實測四模式灰亮矩陣、bankMapValue 沒選滿回空、fillMap 填新四格，全過。

**誠實邊界**：①路由版聲音只有離線渲染健檢＋數字，**live 沒開過**——聲道對不對喇叭、四道平衡、殘響量（out-map 下每道都灌 wet＝總濕量比立體聲版多）都要 Harry 接多聲道介面耳測；②裝置聲道數不夠 max(map)+1 時 PortAudio 開流直接失敗＝app 顯示切換失敗（同 respond2 既有行為）；③duo 下兩顆引擎各開一條流寫同一裝置（既有架構），分聲道只是各寫各的道。

## 分聲道 live 首測：路由 PASS＋LiPo unit 跳保護（已診斷，rig 改插電）

Harry 多聲道實測：「可以分」＝路由本體 live 過。新症狀：unit1/unit2（各自 LiPo 供電）唱一唱斷電、**拔 LiPo 重插即復活**＝保護板鎖定（過流/低壓），非沒電。診斷＝分聲道後單一 unit 獨扛整聲部持續功率（bass/tenor trim +7/+6dB 最操）→ 電流尖峰觸發各自的保護板。**裁決＝rig 插電（07-14 既有 doctrine），LiPo 留穿戴；電容不加（擋不了持續段，且插電後無此問題）；每聲道衰減旗標備而未做。** 詳 HARDWARE_ROADMAP 新條目。

**追加（Harry live 觀察「唱一陣子回授突然變大→斷電」）**：①保護板＝意外的斷路器——回授失控推滿功率→電壓垮→切電；**光改插電會讓回授叫不停，回授本身要治**。②自查抓到路由回歸：`_OLV`（mic 門回授地板的輸出電平）用整陣列 rms，4ch 下同音量讀低 3dB＝門更容易被回授撐開→已修（OMAP 下換立體聲等效尺度，legacy 路徑原式不動，--file 重渲 cmp 仍逐位元同）。③既有 -13.5 bleed 校準是舊喇叭擺位量的，4 喇叭散開後 k 不同＝要重掃（同 08-18 上午的 dump 掃格法）或先加 --bleed-margin；物理層＝喇叭別朝 mic。④電量 vs 回授尖峰＝同機制兩端（低電量垮得早、大尖峰垮得狠），分辨法：換充飽備用 LiPo 唱同段——很快又斷＝尖峰主因。

## 實體鍵（WiFi 模式）失聰：去重按送端分流（Harry live：「respond 按鈕沒作用」）

診斷鏈：`/dev/cu.usbmodem*` 空＝非 serial 模式；respond2 抱著 8766 在聽＝接收端活。讀碼抓到設計缺陷：**UDP 8766 有兩個送端**（app space＝127.0.0.1、WiFi 按鈕＝區網 IP）各一套流水號，`TapListener._ingest` 混記單一 `_seq` ——兩邊序號落在 30 窗內時，落後方整批被當冗餘吞掉，而 HB 無序號照收＝alive 恆真、**錯得毫無徵兆**（08-12 審查 S3/A4 修的是單送端重置，沒料到雙送端interleave）。修＝去重改按送端 IP 各記各的（serial 版單流、行為不變）。台架：雙送端交錯/冗餘去重/重置回落/serial 單流四情境 PASS。**引擎每次 Start 重 import ＝ Stop→Start 即吃到修正，不用重開 app。** 待驗：修後按鈕仍死＝Pico 端問題（電池/WiFi 網路不同）。另 LiPo 充著電用「確實平穩」＝電壓下垂診斷坐實。

## 實體鍵藍牙化（Harry「那就藍牙」）——HID 死案入墓園、免配對通知版全鏈通

**路線演化**：BLE HID 鍵盤寫完 → 上板炸 `ValueError: unknown config param`＝官方 rp2 韌體沒編配對（G38）→ 改免配對自訂 GATT 通知：Pico notify "TAP n"，app（respond_shell 新 `_ble_loop`，bleak）收下轉 `self.tap()`＝與 space 同一條路**同一個流水號**（天然免疫今天剛修的序號互吞）。斷線自動重掃重連；bleak 缺/權限缺/鍵不在＝執行緒安靜退場不擋 app。

**韌體**：`firmware/pico_led_button/main_ble.py`＝火流 v5.1 逐行照抄＋BLE IRQ 驅動（主迴圈節奏不動）；USB serial TAP/HB 照印＝插線即備援（引擎 serial 優先、拔線退 UDP＝BLE 路，兩路不會同時被消費）。板載 LED 新語意：恆亮＝BLE 已連、慢閃＝廣播中、按下熄 0.2s 不變。部署前已驗裝置舊韌體與 repo main.py 逐位元同（退路＝拷回 main.py）。

**驗證鏈**：裝置 boot 心跳 OK → bleak 掃到（rssi -36；卡過一次 macOS 藍牙權限，已由 Harry 放行）→ GP15 直測按壓（第一輪 0 taps＝Harry 沒看到指令，非故障）→ **端到端 67 taps 全收**。Mac 端裝了 bleak（vcclient-dev）。**待 Harry app 內實測**：respond 模式唱歌按鈕收句。

**盤點更新**：無線按鈕不再需要 SoloChoir 熱點（WiFi 版仍在 pico_button/main.py 可回）；穿戴模式＝BLE＋LiPo、展場有線備援＝USB。

**終驗（Harry live）**：拔 USB 上電池＝無線模式，respond 模式唱歌**按鈕收句 PASS**。藍牙按鈕線結案。
