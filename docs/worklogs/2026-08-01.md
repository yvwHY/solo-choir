# Worklog 2026-08-01

## §A 方向扳位：預渲染＝「優化天使」不是「優化 live_v3」（Harry 拍板）

晚間 Harry 問「預渲染要做什麼？不在意時間限制，繼續優化天使呢？注意是優化
天使，不是優化 live_v3（有更好的版本 live_v3 可以被替換）」——這一問把設計
從「StreamMouth 加速」扳成「離線品質上 live」：

- 預渲染買到的第一件事是**同時性**（天使不再慢 0.35–0.6s；G29 已證預測路線
  死、排練是同時落地唯一活路），但本質的第二件事是 Harry 的框架：**排練過
  的歌，天使音訊可以完全離開即時約束**——離線正典管線（direct_mouth
  `--f0-mode express`＝F21 配方、uv 透傳、AGC）渲染到天花板品質，live 端只
  剩薄薄一層對位播放。從此天使品質迭代＝離線改參數→重渲染→分鐘級 A/B，
  自動出現在 live，不再穿即時管線的針眼。
- 架構佐證（偵察）：`--free-run` 的快照/回捲整套其實只活在 DDSPMouth
  （world_live.py:451-484、:507-517），預設 StreamMouth 完全不用——live_v3
  在排練段的角色本來就可以縮成薄宿主，StreamMouth 反應式留給未排練即興段。
- 誠實的 trade-off：純播 stem 有伴唱帶化風險。「活」保留在：乾聲在上、追蹤
  器跟他的速度、（後續可加）mic 包絡閘門、分歧 resync。實戴場裁。

## §B 離線半場：`prerender_stems.py`（driver 固化，07-30 血訓）

排練檔（live_v3 反應式 file 落的 `*_notes.json`，含 upper/lower/lead）→
兩聲部各自轉 direct_mouth 格式（`{"notes", "step_samps": 8268}`）→ 離線
express 渲染 stems，時間軸＝排練 take：

- 嘴同 live_v3 預設：upper=combsub-girl 30k、lower=combsub-harry-260730 30k
  （單一變因 A/B：兩路徑同模型）；per-voice expr seed 20260731/20260732 去同步。
- pair06 產出：`out/pair06_pre_{upper,lower}_angel.wav`（51.5s 逐樣本＝take1）；
  express cents sd 28.9（真人統計帶）、voicing gate 23.7% hops uv、透傳補回。
- pair06 排練檔本來不存在（07-31「端到端過」的產物沒留）——本日重產：
  `live_v3 --file take1 … --key 0` → `out/pair06_reh_notes.json`（274 ticks）。

## §C live 薄對位層：`live_v3 --pre-stems`（additive，旗標守門）

`--pre-stems upper.wav,lower.wav`（需配 `--rehearse`）＝天使改為對位播放離線
stems，兩張嘴完全不載：

- **機制**：tick k 收完（mic 前緣 (k+1)·TICK）把追蹤器譜位的 stem 寫進 live
  第 k+`--pre-ahead` 格（預設 1）＝音訊先於拍點就位 → lag 只需蓋 tick 對齊
  抖動（~0.05s 量級可用；串流嘴地板 0.35 在此路徑不存在）。譜位映射：區段
  起點對應 `p + (ahead-1)·r̂`（p＝tick k 收完的 MAP 譜位）。
- **無縫/跳針**：連續譜位＝續讀無縫；漂移 > `--pre-resync-ms`（60ms）**不立
  刻跳**，等目標譜位落在 lead 換音處（＝take 裡歌手重新起音＝剪接點天然隱
  形）才跳（10ms xfade）；漂移積到 4× 門檻硬上限、續讀游標越 stem 尾強制跳。
  v1 立即跳版在 pair06 實測 resync ×122/239 tick＝每兩 tick 跳一次（ratio
  1.21 結構性速度失配、1:1 續讀每 tick 積 ~32ms）→ 換音處版 ×103＝音符數量
  級＝每顆音重對位一次，音樂上合理。
- **收放**：他靜默 ≥2 tick（lead None）→ 寫零＝天使跟著收；再進場 10ms 淡入。
- 附帶修：`report()`/狀態列對空 mouths dict 的兩處 KeyError/ValueError。

## §D 驗證（Regime A/B）＋儀器教訓

- **Regime A（預設路徑零變更）**：byte-diff 在此路徑**無效儀器**——MPS 渲染
  本身不可重現（同碼同輸入兩跑 max|diff| 0.086、corr 0.9982）。改用
  self-noise 對照：改動前後差（max 0.099/corr 0.9982）＝同一噪音地板 → 過。
  程式面所有音訊改動皆在 `pre is not None` 守門內。
- **Regime B（新路徑）**：pair06 take2 端到端過（brain tick 7ms、resync 103、
  輸出活性 79% vs 串流版 83%、位準同帶、無 crash）。
- 血訓補一筆：延後跳針讓續讀游標可越過 stem 尾（tgt 有守門、cur 沒有）→
  尾段 broadcast 爆。守游標不是守目標。

## §E 聽測包（待 Harry 耳測）＋ NEXT

同 take2 輸入、同排練檔、同模型，變因只有天使音訊來源：

- **B（現行）**：`harmony/out/pair06_rehstream_mix.wav`——串流嘴排練模式
- **P（新）**：`harmony/out/pair06_rehpre_mix.wav`——預渲染 stems 對位
- **天花板參照**：`harmony/out/pair06_pre_ceiling_mix.wav`——stems 直接疊
  take1（零追蹤誤差＝P 的理論上限，隔離「對位誤差」與「品質」兩個軸）

聽點：①P 的離線品質（表現層/無串流殘留）有沒有真的帶上來；②跳針聽不聽
得見（103 次、都在換音處）；③「活感」比 B 掉多少、伴唱帶化了嗎。

NEXT：Harry 耳測裁 P/B → 過了接 live 冒煙（`--pre-stems --lag 0.05`，實戴場
加一格）；可調軸備著：`--pre-ahead`/`--pre-resync-ms`、mic 包絡閘門（起音
同步）、跳針對齊音符起點（snap-to-onset）。選歌仍是 demo 換裝的閘（08-08
前後成唯一 blocker）。

## §F 耳測回收：B/P 雙判死——兇手在對位層不在嘴（數字破案）

Harry：「pair06_rehstream_mix / pair06_rehpre_mix 都很糟糕、不順」。兩版唯一
共用＝排練檔＋譜追蹤器＋pair06 素材，嘴完全不同 → 共用層作案。數字：

- pair06 v3 追蹤命中 **A 37.9%**（jumps 72）＝**62% 的 tick 天使唱的音與他
  live 唱的相撞**——「不順」完全可由此解釋，spec 的 >40% 驗收下限都沒過。
- 素材自我一致性天花板（DTW oracle）pair06 只有 **49.4%**＝就算對位完美，
  一半 tick 他的 take2 跟排練線本來就不同——**pair06 是「管線跑通」的開發
  素材、不是「品質裁決」素材**（最佳 pair01：ceil 81.1、v3 A 58.5%）。
- 排練檔線本身不算碎（音長中位 upper 0.75s/lower 0.56s）＝腦線不是主兇。

裁決含義：**排練模式「同時性 vs 唱對音」的張力被量化**——同時性有了，但唱
對音被（素材一致性 × 追蹤命中）上蓋。出路要嘛選歌＋真的排練到一致（拉高兩
個因子），要嘛回反應式（永遠唱對、但慢 lag）。預渲染機制**不進墓園**（根因
＝對位命中率，非嘴/stems；stems 品質軸未被此判決觸及，ceiling mix 仍是有效
測試）。線掛起，等真選歌素材再驗。

## §G ceiling 裁決回收＋和音組成兩層修正（indep 0＋reh_polish）

Harry 裁 ceiling mix：「順、但和音組成不是很好，感覺不在調上」＝嘴/stems 無罪
（§F 判決確認），病在組成層。逐層排查：

- 調性假設無罪：lead 100% 在 C 大調音階內（F 大調 96% 次選）；天使線也
  100% 在音階內＝不是跑出調。
- **縱向撞音超標才是「不在調上感」**：天使 vs lead 不和諧 18.5%（基準
  12.1%）、upper vs lower 26.0%。
- **兇手一＝indep 0.15**（live_v3 預設被排練檔產生繼承；07-28 定價「i15
  瞬時不和諧 9.5→18%」完全吻合）→ indep 0 重產：互撞 26→10.4%，但對
  lead 18.5→18.0% 不動＝另有病灶。
- **兇手二＝反應式腦的持續性撞音** → `reh_polish.py`（新工具，driver 固化）
  ：排練檔譜面後處理，只修「對 lead 持續 ≥2 tick（≥375ms）的不和諧段」
  （整段就地移到最近協和音階音、兼顧另一天使、|移| ≤5 半音；單 tick 經過
  音/掛留語彙不動）。pair06 修 13 段：**對 lead 18.0→12.3%＝貼語料基準**、
  互撞 10.4→12.2%（可接受）。
- 範式紅利首次兌現：譜離線固定 → 組成可規則清理、分鐘級迭代（反應式 live
  做不到——音是當場決定的）。
- 產物：`out/pair06_pre2_ceiling_mix.wav`（indep0＋polish stems 疊 take1）
  **待 Harry 重裁**，聽點＝「不在調上感」還在嗎。備用假設（若還在）：lead
  35% 壓 G＋F♮＝G 中心 mixolydian，腦的 C 中心功能假設拉錯方向——移調修
  不了（+5 會把 F♮ 變 B♭ 出 C 大調音階），屬腦的模式感知課題。

## §H 調性案破案：pair06 根本不是 C 大調（儀器循環論證血訓）

polish 版 ceiling Harry 仍判「不在調上」、平行三度判別版也怪 → 連零和聲主張
都怪＝嫌疑走向調性層。Harry 自問「跟原音檔調性有關？」——正解。

- **儀器盲點**：管線表徵把音吸附進 C 大調音階 → 「lead 100% 在 C 調內」是
  循環論證。**未吸附的原始 f0 音級分布**（新儀器）：G# 19.4%、E 13.6%、
  G 12.7%、F# 12.4%＝**A/E 大調家族（4 升號），C 大調完全不沾**。
- 歧義判決：音級角色（G# 當 E 的三音 vs A 的導音）傾向 E；**終止音 A＋樂句
  尾音 A 最多 → 判 A 大調（--key +3）**。涵蓋僅 ~70%＝素材本身和聲不乾淨
  （自由唱/游移），殘餘怪感若在，剩下是素材不值得追。
- **血訓**：07-30 規矩「真歌一律 --key 手動」被踩破——bench 的 --key 0 口徑
  只對追蹤跑分有效（調不影響命中），對和聲品質渲染完全無效，兩個口徑不可
  互相沾染。今晚 C 框架下的所有組成層判決（§G indep/polish 的耳測部分）
  全部作廢重審；數字工具本身仍有效。
- A 大調鏈重跑：腦 indep 0 在 A 空間 raw 不和諧 29.0%（C 空間 18%——側證
  素材在單一大調內就是髒）→ polish 修 23 段 → 12.4%；互撞 16.2%。
  產物 `out/pair06_A_ceiling_mix.wav` 待 Harry 裁。

## §I 調性案終判＋pair06 結案

- E 大調對照版產出（raw 不和諧 32.4% vs A 空間 29.0%，粗代理偏 A）。
- **Harry 終判：A 版「變好但仍有殘餘怪感」**＝按 §H 預定判讀：**A 大調確立
  為真調（調性案結案）**、殘餘歸素材（自由唱涵蓋僅 ~70%，單一大調內就髒）
  → **pair06 不再追**。組成層工具鏈（indep 0／reh_polish／raw-f0 調性儀器）
  經過第一次公平審判、可信。
- 儀器戰績：raw-f0 涵蓋排名第一名（A 69.8%）＋終止音判準＝這次判對了
  （keydet 真歌不可用的替代品，離線限定，n=1）。
- 今晚淨資產：嘴/stems 無罪確認（ceiling「順」）、prerender_stems／
  --pre-stems／reh_polish／raw-f0 調性儀器四件落地、三條血訓
  （MPS self-noise 驗法、游標越界、口徑洩漏）。
- NEXT＝**應答式 v2**（Harry UX：唱一句→回應播「離線渲染好的和音＋他剛唱
  的那句」）——繞開對位與同時性、句間空檔跑離線正典渲染、他的聲音在回應裡
  ＝hero loop 的應答形。設計默認：句尾偵測（有聲 ≥1s 後靜默 ~0.7s）、回應
  ~1s 內開播、插話讓路淡出、key 手動（今晚血訓直接適用）、腦 indep 0＋
  polish。**選歌價值再度 F19**：調性乾淨＋唱得穩的歌讓今晚所有工具直接生效。

## §J 應答式 v2 開工——落地跑通（phrase_render＋respond2）

Harry「應答式 v2 開工」。設計＝唱一句 → 回應播「離線品質和音＋他剛唱的那
句」。墓園先查：**G「--voice-delay」07-16 判死（播他自己的延遲副本＝DAF 干擾、
耳測全滅）**——差別在 voice-delay 邊唱邊疊（DAF 前提成立），本設計只在他不唱
時播；**「他一開口回應淡出讓路」定為 live 硬規則不是選項**。

- **`phrase_render.py`（新）＝行程內常駐渲染器，本案的地基**。動機是實測：
  離線正典（direct_mouth→main.py 子行程）5s 句要 **6.7–9.5s**（每句重載模型）
  ＝做不到句尾後即時回應。搬進常駐行程：配方逐項對齊（AGC／express f0／
  voicing mask／uv 哨兵／**main.py:208-213 的 volume mask ＋9 幀膨脹**——這項
  只存在於 main.py，direct_mouth 看不到，漏了多出底噪段／uv 透傳）。
  **等價驗證（用今晚 §D 的 self-noise 規矩）**：子行程自身兩跑 corr 0.9969
  vs 行程內對子行程 0.9962＝同一噪音帶 → **離線天花板品質搬進常駐行程無打折**。
  單聲部 5s 句 1.46s（RTF 0.29）；兩嘴共用 hubert units（同 content）。
  順帶補既有邊界洞：`expressive_cents` 對零音符段空陣列索引爆 → render 早退。
- **`respond2.py`（新）＝應答式主體**（offline `--file`）：斷句 → 每句 lead→
  BrainV3（**跨句持續＝記得這首歌**）→ indep 0＋reh_polish → 兩嘴渲染 →
  排成 [他的句][回應]×N。key 手動（§H 血訓）。
- **斷句參數繼承翻車（教訓同款）**：舊 respond.py 的 gate 0.005/gap 0.6 是
  「有沒有音高」的口徑，他的換氣噪音過得了 → 全曲僅 2 個 ≥0.6s 空隙、斷出
  **34.6s 的「一句」**。實測分布（唱 rms p50 0.15、換氣 ~0.01、換氣段 0.3–0.5s）
  → 改 **gate 0.02 / gap 0.35**：15 句、中位 2.4s＝像樂句。**參數繼承要驗證。**
- 效能：兩聲部 RTF ~0.55 → 中位句 1.31s、p95 2.56s 渲染；回應在他唱完
  0.35+渲染 秒開播（2.4s 句≈1.7s）。**下一段槓桿（未做）＝管線化：腦因果、
  他唱過的音訊已在手 → 邊唱邊分塊渲染，停唱即播、等待趨近零。**
- 產物 `harmony/out/resp2_pair06.wav`（89.7s，15 對）**待 Harry 耳測**：
  ①應答形式成不成立 ②回應等待感（現 ~1.7s）③和音組成（A 大調＋polish）。

## §K 實體鍵（應答式「換你」鍵）——Pico 路線落地

Harry 追問句尾偵測在展場是否可靠 → 量測後開實體鍵線。

**先量事實（07-31 實戴 mic dump，rig 全開真房間）**：
- 喉麥底噪 0.0030、gate 0.02＝**16 dB 餘裕**；空氣噪音進不來（接觸式，
  roadmap:29 的軍用先例＝100+dB 噪音下可用）→ **展場人聲/音樂不是威脅**。
- **真威脅是自我 bleed**：天使聲經身體振動回到喉麥，停唱後 0.6s 內
  中位 0.0093 / **p90 0.016 vs gate 0.02＝只剩 2 dB**。展場音量調大就越線
  → 偵測器判「他還在唱」→ **回應永遠不觸發**。與展場噪音無關，是設計自己
  的迴路。
- 換一般空氣麥克風：噪音直入＋bleed 大一個量級＋迴授風險；「加音高條件」
  救不了（人群/音樂本身有音高）。**此格無實測數據**（手上錄音皆安靜環境），
  以物理推論記錄，勿當實測引用。

**兩個免費解**：①**播放期間不偵測**（我們知道自己何時在播）＝結構上消滅自我
誤判，兩種麥都受用（未實作，live 版必做）。②**實體鍵**＝對噪音與 bleed 全免
疫，且延遲從 0.35s 降到 ~6ms。

**盤點（零採購，兩套現成）**：
- 喉麥上的 **PTT 鈕**（並聯短路到地＝按下麥克風靜音）——07-07 決定拆除但
  **手術從未在 worklog 記錄完成**（當時卡買電表）。若還在＝零改動可用（能量
  偵測會看到乾淨斷崖）。待 Harry 目視確認。
- **`firmware/pico_w/pico_w_solo_choir.py` 骨架早已存在**（PTT 時代寫的
  button→WiFi→Mac，GP15 上拉接地）＋ OLED 線驗過的 AP/UDP 基礎建設
  （ping 6ms、60s 零丟包）＋ **OLED 那顆 Pico 自 07-23「臉線收掉」後空著**。

**Harry 選 Pico 路線 → 落地**：
- **`firmware/pico_button/main.py`（新）**：骨架的 HTTP POST 改 **UDP 廣播**
  （免 TCP 握手、不阻塞、Mac 端免知 IP、換場地免設定）；**雙模開機
  STA→退回 AP `SoloChoir`＝實作 STATE 掛著的待辦**；每按冗餘送 3 封帶序號
  抗掉包；2s heartbeat＝舞台連線指示；debounce 200ms；板載 LED 回饋。
  接線只有 GP15＋GND，無被動件。**不可用 wire-light 兩顆**（60fps WS2812
  時序敏感，加 WiFi 中斷有掉幀風險）。
- **`harmony/tap_listen.py`（新）**：Mac 端 TapListener（背景執行緒、非阻塞、
  序號去重、`.alive` heartbeat）＋ `--selftest`（免硬體，**PASS**：3 下全收、
  冗餘去重正確、舊序號不重觸發、本機延遲 1.3–1.5ms）＋ `--monitor`（接上
  硬體後驗整條鏈）。**不擋路原則**：埠被佔用/收不到＝退化成沒這顆鍵，能量
  偵測照常。
- **語意設計**：點一下＝「換你」（非按住＝我在唱：手在唱時自由、觀眾看得懂
  ＝call-response 的音樂手勢）；**按鈕只負責「結束」，開始仍由能量偵測**
  （他一開口 0.05 以上從沒錯過）；播放中再按＝barge-in 淡出（順帶實作 DAF
  紅線）。
- 儀器血訓（自己踩的）：selftest 首版把 `_send` 的 3×10ms 冗餘睡眠算進延遲
  → 報 34–38ms。改背景送才是真值 1.3–1.5ms。**量延遲要確認起點在量什麼。**

**待硬體在手**：接線＋燒錄＋`--monitor` 看見 TAP；真實 WiFi 延遲；按鈕實體
位置（建議胸前門柱區拇指可及，不建議腳踏——他是移動的、展場有觀眾）。
**未接進 respond2**（live 模式尚未存在＝下一步）。

## §L 展場前端轉向：喉麥 → 空氣麥克風（Harry 決定；深夜對話，未動工）

Harry：「我不想用喉麥，因為展場的話還是用麥克風比較好」。落帳如下——**這是
方向決定，本 session 未實作**，明日續。

**框架（重要）：不是換掉喉麥，是兩種模式不同前端。**
- 穿戴／viva demo：喉麥＋骨傳導不動（07-13「throat mic STAYS — it is the
  feedback/self-stacking architecture」＋ FINDINGS Test A 已過的迴授架構）。
- 展場：空氣麥克風＋桌上 cone units（訪客不必戴、不必調整、無衛生問題）。
- 引擎吃什麼輸入都一樣，兩者不必二選一。

**應答式與空氣麥克風是絕配（本轉向的關鍵論證）**：唱／播從不同時活著 →
**播放期間可硬靜音輸入** → ①迴授的環結構上不存在（omni 也不怕）②天使聲不會
被誤判成「他還在唱」③**回應不會滲進下一句錄音**（否則下一句把上一句和音當
他的聲音再渲染＝越疊越糊，空氣麥才有的新問題）。同步模式 live_v3 享受不到，
它的輸入輸出永遠同時開。**live 版硬規則，明日實作第一項。**

**麥克風盤點（零採購，兩支都已擁有）**：
- Daffodil MCP150B USB-C lav（2025-11 購）：**全指向**＝抗噪/迴授最差，且夾胸
  離嘴 20–30cm（vs 頭戴 2–3cm，約差 20dB）。
- **FIFINE K669B USB 電容（2025-10 購）＝勝出**：**心形指向**，背面抑制＝環境
  與喇叭進得少；有實體音量旋鈕。擺位槓桿：cone units 放麥克風正後方（null）。
- **高度問題**（Harry：要讓人站著唱）：附的是桌上三腳架 → **器材室借 boom
  落地架**（零成本，與借 Slate AX 同一趟）；FIFINE 麥克風夾是標準螺牙可直上。
  建議 boom 非直桿（訪客身高 150–190 需快調）。自製黃銅麥架＝可做但**高瘦頭重
  會倒、重量必須在底座**＝先借跑通、體驗定案後再議。
- **Harry 問「買 USB 轉 3.5mm 頭？」→ 否，物理上不可行**：USB 麥克風的 ADC
  在機身內、無類比輸出；「USB 轉 3.5mm」本身是另一張音效卡（USB *裝置*），
  兩個 USB 裝置不能對接（都需要 host）。要一個時鐘只能換**類比**麥克風插
  AI-Micro（其 3.5mm 有 plug-in power）。

**F7「一顆卡一個時鐘」在應答式下鬆綁（本日新認識）**：該規矩是為 live_v3
同步模式訂的（雙工 underrun＋延遲預算對漂移極敏感）。應答式回應本來就在
~1.5s 後，聚集裝置 drift correction 多幾 ms 聽不出來 → **USB 麥克風＋聚集
可用**（`mulit-set` 07-16 實測 0 underrun，模式現成）。F7 三條仍照做：單一
聚集／開 drift correction／**in 與 out 同指該聚集**。→ **零採購結論**。

**空氣麥克風的真風險（唯一未驗格）＝輸入 SNR 影響天使音質**，不只影響斷句：
天使是拿他原聲當 content 餵 DDSP，而 07-30 已記錄「低位準/低 SNR 輸入的轉換
退化＝資訊論的牆」。**待 Harry 錄 K669B 站姿實唱（安靜一段＋有背景音樂一段）
→ 跑 phrase_render 比對喉麥/錄音室版**，15 分鐘可得答案。

**按鈕（§K）在此轉向下的狀態**：PTT 路線隨喉麥退出而作廢；AI-Micro input 2
有線路線、Pico WiFi 路線皆不受影響（兩者都零採購）。展場吵雜使按鈕從「可選」
變「更必要」。

## §M 明日續（08-02）優先序

1. **respond2 live 模式**＋**播放期間輸入硬靜音**（§L 的地基，兩種麥皆需）
2. Harry 錄 K669B 實唱 → 輸入 SNR 對天使音質的影響實測（唯一未驗格）
3. 斷句門檻**開場自動校準**（展場底噪免手調）＋按鍵介面接上（`take()`）
4. 管線化渲染（腦因果、音訊已在手 → 邊唱邊分塊渲染，停唱即播，等待趨近零）
5. 硬體待辦：器材室借 boom 麥架（順道 Slate AX）；按鈕實體接線＋燒錄＋
   `--monitor` 驗；unit Pico 開 WiFi 是否干擾 60fps WS2812（5 分鐘可驗）
6. **選歌仍是關鍵路徑閘門**（08-08 前後成唯一 blocker）
