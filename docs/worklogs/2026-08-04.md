# Worklog 2026-08-04（凌晨場，接 08-03 深夜）

K669B 接上、Harry 在場實戴的第一個完整調校夜。表面上是調旋鈕，實際上抓到
**兩個潛伏 bug、翻掉兩個我自己的量測結論**，最後以一次全面體檢＋兩個修正收工。
今晚的主軸不是任何單一修正，是**三條方法論血訓**（見 §G）。

## §A 實戴調校：平衡、女聲、自主性（多輪，commits 無——都是旋鈕）

- **混音平衡**：他「我好像比其他聲部音量弱」→ 實測他低 4.3 dB（見 08-03 §G）
  → 多輪 live 手調收斂到 **dry 0.944／天使 0.75／女聲 +1 dB**＝兩天使合計領先
  他約 +0.7 dB。「調整先這樣，先以目前數據為主」→ 已寫成預設（`d0e1461c`）。
- **⚠ 離線 A/B 的 dry 1.1 作廢＋方法檢討**（他：「不然你 review 一下」）：
  量測確認兩份素材的天使/乾聲比例幾乎相同（K669B +2.2 dB vs pair06 +2.5 dB）
  ＝訊號鏈無罪，**是我的判決方法錯**——那輪 ①沒盲（檔名與訊息都寫著「你領先
  3 dB」，而且緊接在他抱怨音量之後＝預期效應）②素材用 pair06（明文「不能拿來
  裁決品質」）③在桌上判（踩 07-25 in-context 規矩）④響度對齊使「天使大聲」
  等價於「他小聲」，與現場的處境不同。
- **自主性 indep 0→0.15→0.3→1.0 探索**：他的關鍵觀察——**「只有在長音才會
  顯得自主」**。機制上正確：indep 的兩個觸發點（他長音／他換音）都掛在**他的**
  音符事件上，應答式短句裡觸發機會本來就少＝**這條軸的上限是樂句形狀，不是
  旋鈕數值**。未終判，暫用 0.15。
- **天使尾巴（tail_carrier）一輪即撤**：他唱完後天使續唱 1.5s，載體＝循環他
  最後 0.5s 有聲音訊。Harry：「像跳針」——**G32 的血訓原樣重演**（拼貼載體＝
  捏造內容；循環藏得掉接縫藏不掉重複）。我動手前沒查墓園＝流程錯誤。變體
  （只借真實長音、不循環）議而未做；G32 說這片地是死的，最多再試一輪。

## §B 兩個潛伏 bug（`a92336f5`＋`c2d8dcf4`）

他：「和音不在調上、不和諧」＋「天使應該要能知道調…同時在和諧上有自由度」。
追出來的東西比抱怨大：

1. **polish 的音高空間 bug**：`lead_hist`（腦空間）與天使（麥克風空間）差
   key+octave 的偏移，**偏移是 12 的倍數時剛好隱形**——而此前所有調參都跑在
   `--key 0`＝正好站在隱形點上。填真實的調反而讓 polish 默默做錯（拿差五度的
   lead 判撞音＋把天使吸附到絕對 C 大調）。修後真實調上不和諧 29–33%→10–15%。
   ⚠ **08-01 的 polish 數字（18.0→12.3%）量在破空間裡（pair06 用 --key +3）**，
   該數字與據此的組成層結論要重審；prerender_stems/reh_polish CLI 的離線路徑
   同形狀（root 預設 0＝舊行為）未動，待查。
2. **我的第一個修正量錯了參照**：修完 bug 的「不和諧率」對錯調（10.1%）與對調
   （10.6%）**看不出差別**——因為參照物是被 C 吸附過的內部 lead，系統跟自己
   永遠自洽。換成**他真實唱的音高**（harvest）才對上耳朵：錯調 37.7% vs 對調
   17.3%。再把 polish 的參照也換成真實音高 → **錯調 17.9%＝幾乎追平對調**。
   ＝天使不需要被告知調，需要的是「不跟他實際唱的音撞」。

## §C `--key auto`（`7083202c`）

上面那步把角度錯掉的代價從「一錯全毀」壓到「幾乎不痛」→ 07-30/31 判死自動
定調的理由失效 → `KeyTracker`（raw-f0 音級分布逐句累積、`--key-margin` 防抖、
校準唱段種初值；刻意不經 VoiceToTokens＝不重演 08-01 的循環論證）。驗證：
pair06 第 4 句收斂到 +3＝**獨立重現 08-01 人工追一晚的答案**。誠實面：他自由
哼唱的素材沒有調（領先僅 1–2pp），任何定調器都只是挑最不壞的旋轉。
腦本來就能適應所有調（平移不變＋正規化＝12 調等價），缺的一直只是角度；
**C 大調吸附仍在**＝藍調音/轉調/調式吃虧，真解是未正規化語料重訓（viva 後）。

## §D auto-octave 用校準唱段鎖（`b3713289`）

STATE 掛兩天的待決。判「技術決定不是音樂決定」（shift 只搬 token 空間、不決定
天使八度）→ 直接做。式子沒動、只換餵料：最初 6 個有聲 tick（1.1s 換氣與起音
垃圾）→ 校準唱段（5s 刻意演唱）。邊界餘裕 2.0→3.1 半音（真值 6.1）。
今晚 11 輪全鎖 +12＝調參判決未被此變數污染。

## §E 殘響早期反射改擴散簇（`8e8c7be7`）

他：「開口會重複」。一旋鈕鑑別（`--reverb 0` 消失）＝定罪 ER 離散 taps：對
持續音是空間感、對子音瞬態是一串可逐一聽出的 slap echo。改同區間密集隨機微
tap（能量不變）。儀器第一版失敗留案（全段 onset 自相關被音節節奏淹掉、乾聲
自己 0.414＝三格分不開——**儀器要對準機制**）；第二版：IR 早期 crest 27.0→5.0
（≈高斯地板）＋onset 鎖相回聲能量 0.105→0.089（能量保留、離散性消滅）。

## §F 全面體檢＋兩修（`af35b0ec`，儀器固化 `angel_audit.py`）

他：「全面檢查：和諧、和聲、音量、殘響，所有天使表現層面」。八軸成績單，
素材＝他的 K669B 實唱 8 句（⚠ 素材 bug 留案：dump 裡回應段被誤收——回應裡他
的乾聲最大聲、girl 佔比判不到；**句長成對＝污染指紋**，靠「他的句永遠在回應
前」取偶數位去重）：

| 軸 | 修前 | 修後 | 判 |
|---|---|---|---|
| 錯開（他換音→天使跟進） | 中位 562ms p90 1200ms | **0ms／375ms** | `--stab` 2→1 |
| 子音疊加（uv 段 vs 純乾聲） | +7.7 dB | **+0.2 dB** | `--uv-dry` 0 |
| 和諧（對他真實音高） | 29.4% | 27.5% | 掛起＝素材無調 |
| 兩天使互撞 | 8.6% | **17.3%**（語料 12.1） | ⚠ stab 1 的代價 |
| 音準執行 | 8.1c | 9.2c | ✓ 嘴無罪 |
| 音量平衡 | +0.7 dB | +0.8 dB | ✓＝他手調組合 |
| 殘響 濕/直 | −12.2 dB | −12.0 dB | ✓（送出量待房間裁） |
| 削峰 @gain 0.5 | 0.66 | 0.83 | ✓ |

- **錯開的真兇是腦的節奏慣性不是 tick 網格**：stab=2 的 375ms 地板＋HOLD 偏好
  ＋indep 掛留。stab 1 換來跟得上，代價互撞 +8.7pp＝「跟得上 vs 穩」的真實
  取捨，**待他 live 判**（`--stab 2` 一鍵退回 08-02 耳測行為）。
- **子音疊三份**：回應混音＝乾聲＋兩 stems，而 stems 的 uv 段各透傳一份他的
  原聲。`uv_passthrough` 加 `dry_gain=`（1.0＝逐 bit 舊行為），respond2 預設 0；
  stems 單獨播的路（live_v3/perform/prerender）透傳照舊。
- 和諧 27.5% **要等有調的歌才有判決力**＝選歌再 +1 票。
- 3b 音程 SD 61c 與 F21 的 11c **口徑未對齊**（F21 疑量在注入 f0 曲線、我量
  渲染後 stems 含過渡段），不喊退步，待同儀器重比。

## §G 三條方法論血訓（今晚真正的資產）

1. **「剛好差一個八度」會讓跨空間的 bug 隱形。** 所有測試都站在隱形點上，
   bug 就能活過無數次耳測（§B1 活了三天）。
2. **驗證「給人聽」的東西，參照物必須是人聽得到的那個量。** 拿內部表徵當參照
   ＝量系統跟自己的一致性，數字可以在使用者說「不對」的同時很漂亮（§B2）。
   今晚兩次都是耳朵贏儀器。
3. **Harry 拍板的流程**（他：「盲 A/B 無法代表 live」「對需要人力檢查修改
   不滿意」）：**他的判決一律 live／in-context；離線量測與盲測只當我的工程
   驗證；能用 dump 回放鑑別的不叫他唱**（今晚 reverb on/off 那次本可離線做）。

## 實戴數字（順帶收官）

等待 p50 1.06–1.26s、p95 ≤1.6s（8–16 句多輪）、io **全程 0/0**、gain 0.5 峰值
≤0.86＝K669B 的增益餘裕確認。校準分離度 13.8 dB（擦邊過；那 3 秒他沒真的安靜
——底噪 p95 0.0214 vs 實際房間 0.004）。**08-02 §M 清單至此全收**（管線化的
真麥克風確認：等待與 io 兩格都過；6.5s 長句那格今晚句子偏短、未完全覆蓋）。

## 明天（08-04 白天）

1. **live 終判**（他檢查、我不再改）：新預設一條指令即可——
   `dpy respond2.py --live --key auto --in-name "USB PnP" --gain 0.5`
   聽點＝①錯開好了沒（stab 1）②抖不抖（stab 1 的代價）③換氣乾不乾淨（uv-dry 0）
   ④和聲在不在調上（key auto）。覺得抖→`--stab 2`；要舊透傳→`--uv-dry 1`。
2. 和諧那格＝**唱一首確定調性的歌**（選歌的實物測試）。
3. 掛起：indep 終判、殘響送出量房間裁決、~~08-01 polish 數字重審、
   離線排練檔路徑的 root~~（→§H 已收）、tail 變體（G32 陰影下最多一輪）。

## §H 白天場：離線路徑 root 補修＋08-01 polish 數字重審（兩掛起項一起收）

§B 的 ⚠ 兩項，全程 dump/檔案重放、沒叫他唱。

**1. `reh_polish` CLI 補同空間換算。** live_v3 落檔的 notes json：lead＝腦空間
（`live_v3.py:431` 直接存 `lead_hist`）、天使＝mic 空間、`k_shift` 有存。
`v2t.shift` 恆為 12 的倍數（`live.py:76`）→ mod-12 口徑下兩空間只差 `k_shift`，
而 polish/stats 全部是 mod-12 比較 → CLI 從 JSON 讀 `k_shift`、比較視圖裡
lead 減掉它、`root=(-k_shift)%12`。**落檔的 lead 保持腦空間不動**——
ScoreTracker 等下游吃的就是腦空間，只有比較要換算。
迴歸：k=0 檔（reh0）新舊碼產物逐項相同 ✓（k=0＝隱形點，行為本來就對）。

**2. 重審結果＝08-01 那晚其實是兩個數字、兩種命運：**
- **18.0→12.3%（§G）量在 reh0、`--key 0`＝隱形點上，mod-12 口徑其實有效**
  ——commit `a92336f5` 說它「量在破空間裡（--key +3）」記錯了檔；
  新碼原樣重現 18.0→12.3 ＝ 該數字平反。
- **真正破的是 §H 的 A 大調鏈（29.0→12.4%、修 23 段）**：破空間口徑
  逐位重現 29.0→12.4 ✓，但放到**正確空間**量——polish 前 21.0% →
  polish 後 **33.1%＝變糟**；天使在 A 大調內 **98.7%→66.2%**（被吸進
  C 大調 92.6%）。破 polish 拿差 3 半音的 lead 判撞音＋往絕對 C 大調推＝
  對 A 大調素材**每一刀都是幫倒忙**，而破空間裡的統計跟它自洽所以印出
  「12.4%＝變好」。§G 血訓第二條的又一實例：量給人聽的東西，參照物錯了，
  數字可以在變糟的同時很漂亮。
- 新碼重跑 rehA：21.0→18.3%（對 lead）、互撞 27.5→18.9%、
  天使 99.6% 留在 A 大調＝方向對、幅度誠實。

**3. 對 08-01 結論的影響（修正歸因，不重開案）：**
- **「A 大調確立為真調」不動搖、反而加強**——Harry 聽的 A 版 ceiling 是用
  被破 polish 汙染過的 stems 做的（1/3 天使音被推出 A 調），帶著這個
  handicap 仍勝過 C 版。
- **「殘餘怪感歸素材」的歸因要軟化**：當晚 A 版的怪感裡有機械成分
  （polish 亂改音），素材涵蓋 ~70% 的髒只是共犯之一。
- **pair06 結案維持**（Harry 裁的，素材上限也還在）；若要重驗，
  `python reh_polish.py out/pair06_rehA_notes.json <dst>` 一條指令即有
  乾淨 polished 譜可重渲染——重開與否是他的決定，這裡只留鑰匙。
- 「組成層工具鏈經過第一次公平審判」要改口：**那次審判對 reh_polish
  並不公平**（它在 A 空間是壞的）；公平審判＝今天這次，結果＝有效但
  幅度溫和（-2.7pp 對 lead、-8.6pp 互撞）。

## §I C 案分離度量測管線落地（白天場；08-03 §J 第 10 項，`clap_probe.py` 新）

08-03 §J 拍板「先離線量 C（拍手／彈指暗號）」。管線今天建好、工程驗證通過；
真判決只差 Harry 拍 20 下手（15 分鐘，指令見下）。

**誠實標準**（延續 08-01 量 bleed 的 p90-vs-gate）：不是比「拍手 vs 歌聲平
均」，是拿同一偵測器掃過全部歌聲＋bleed 素材，找出**最像拍手的瞬間**（塞音
/t/ /k/ 是預期最強假陽性），量它與**最弱一下拍手**的餘裕；≥6 dB 才算過。

**偵測器**（故意簡單到能在 callback 逐 hop 算）：rise（~12ms 內 dB 爬升，
-80 dBFS 地板）× hf（4kHz+ 頻譜佔比）二維規則；grid search 找「全收拍手、
背景零誤觸」工作點。素材寫死：歌聲＝`out/resp2_live_260802_234841.wav`
（252s K669B 站姿）、bleed＝07-31 三段有 live_out 配對的實戴 dump
（225804/230150/233041）。

**背景那半不用等拍手、已經量了**：252s 歌聲裡 rise 最高的瞬間（51–53 dB）
**全部 hf≈0.0–0.06**＝低頻／諧波起音、不寬頻；全段唯一危險瞬間 **0:37.4
（rise 46.3、hf 0.44）**。⇒ 分離是 hf 維度在做工；拍手預期 hf 0.5–0.8，
大概率分得開——但這句要等真拍手數字才能寫成判決。

**合成工程驗證**（5ms 噪聲 burst ×10 假拍手）：管線通、餘裕 26.6 dB、252s
掃描 <2s。只驗程式；不進判決（08-04 凌晨血訓 (c)：判決一律 live/real）。

**儀器血訓（又一條「起點在量什麼」）**：數位靜音＝-200 dBFS，不設 -80 地板
時「從靜音跳出來」的 rise 被灌成 170 dB、淹沒真正要量的起音形狀——與 08-01
selftest 把冗餘睡眠算進延遲同族。**量之前先問這個數字的起點是什麼。**

**Harry 回來的 15 分鐘**（K669B 站姿、展場距離；每下隔 1 秒以上）：
```
cd harmony
dpy clap_probe.py --record out/claps_260804.wav    # 拍手 10 下
dpy clap_probe.py --record out/snaps_260804.wav    # 彈指 10 下
dpy clap_probe.py --analyze out/claps_260804.wav out/snaps_260804.wav
```
輸出直接給判決：通過＝展場**零按鈕硬體**；不通過＝A 案軟體早就好
（`tap_listen.py` selftest PASS），接兩截黃銅線 10 分鐘上工，什麼都沒損失。

**同場掛著不動**：喉麥 PTT 目視確認（穿戴模式零改動選項）仍等 Harry；
三案定案等量測數字進來再談。

## §J live 終判實跑（08-04 下午，Harry in-context）＋dump 掛死救援

**四聽點裁決（35 分鐘、25 句、K669B）：**
①**錯開＝「目前沒有錯開了」＝stab 1 live 過關**（08-04 凌晨 §F 的待判收掉）。
②抖：解釋定義（音符層級的躁、非顫音）後他未回報、未要求退 stab 2＝暫站住；
  顫音他另問「有設定音高顫抖嗎」→ 確認固定顫音＋去同步是 08-02 他自己裁過
  的配方。③換氣判不了——能量斷句下句中換氣即收句直接進渲染；**等實體鍵**
  （軟體 08-02 已好：鍵連上 gap→2.0s 句由按鈕決定）。④**「目前都在調上」＝
  key auto live 過關**＝昨晚判不了的和諧格，今天 live 給了正向判決（量化那格
  仍等有調素材跑 angel_audit）。
**數字**：25 句｜停唱到回應開播 p50 1.44s / p95 1.78s｜io 全程 0/0｜校準
分離度 24.1 dB（比凌晨 13.8 乾淨——這次他真的安靜）｜gate 0.0364｜
auto-octave +12｜key 種 +0（涵蓋 64%＝校準段沒調性，後續逐句重估）。

**新症狀（進 DEBUG_PLAYBOOK）：Ctrl-C 後行程掛死、dump 差點陪葬。**
SIGINT 有收到、主迴圈退了，卡在 `with sd.Stream` 的 `__exit__`——
`sample` 定位＝PortAudio `FinishStoppingStream` 掛在 CoreAudio 裡，60s+
不自解；dump（`sess`）還在記憶體、`sf.write` 還沒跑到。**lldb 救援成功**：
attach → `PyGILState_Ensure` → `PyRun_SimpleString` 注入
`sys._current_frames()` 找到 live() 的 frame → `f_locals` 拿 `sess`/`st`
→ 寫 `out/resp2_live_260804_rescue.wav`（600.0s）＋撈回終場統計 → kill -9。
兩個坑：(a) 多補的 Ctrl-C 積成 pending KeyboardInterrupt，第一次注入被它
炸掉（-1）＝先讓注入把它吃掉再重跑；(b) 純 numpy 陣列不進 `gc.get_objects()`
（非容器不被 GC 追蹤），要從 frame locals 拿。
**兩修（`respond2.py`，不動音訊路徑）**：dump＋統計搬到關流**之前**落檔
（n 先快照、callback 只寫 n 之後＝stream 開著寫檔安全）；dump buffer 滿
印一次警告——今天 35 分鐘 session 被 `--max-min 10` 無聲截成 600s，
「no silent caps」又中一次。長 session 記得 `--max-min 40`。

**環境註記**：`dpy`＝`260724_ddsp_svc/venv/bin/python`（vcclient-dev 缺
parselmouth，respond2 會在 import DDSP vocoder 時炸）。

## §K 應答式 app 介面（`adb22998`，Harry:「可以先給我一個app介面嗎」）

08-02 §I 血訓（站著唱看不到 console＝校準敗過）的正解。
`app/respond_shell.py`＋`ui/respond_live.html`：pywebview 殼、respond2 跑
**子行程**（ddsp venv；殼在 vcclient-dev）、stdout 解析→大字狀態
（可以唱／停·渲染中／回應中／校準指示，2–3 公尺可讀）＋統計列
（句/等待/key/分離度/io）＋警告舉牌（削峰、dump 滿）＋raw log 面板。
音訊不過橋；respond2 唯一改動＝收句後補「收句→渲染…」一行標記
（不然收句→句N 列印之間 UI 誤示可唱）。收＝SIGINT（dump 關流前已落檔），
8s 不退＝CoreAudio 掛死 → 自動 SIGKILL。頂列可調 gain/stab/key/mic/max-min
（預設＝08-04 終判組、max-min 40）。
驗證：解析器對下午真實 session log 全型命中（1063 聽/25 句/校準/警告）；
app 實啟存活。啟動：`conda 環境 vcclient-dev python app/respond_shell.py`。

**二輪（`a19f29be`，Harry:「改英文版，另外天使一樣要分四個聲道（先分，
另外兩聲部再補上）」）**：UI 全英文（SING/HOLD/RESPONSE/QUIET）＋
respond2 `--out-map 'D,U,L'` 分路——照 solo_min F7 慣例（同一台多聲道
裝置、0-based、之後聲部往後加），守衛式＝預設路徑構造上不變；殘響改
逐 stem 送（卷積線性＝總和同聲、各自定位）；分路 dump＝聲道總和；
嗶聲分路時全聲道廣播。端到端驗證＝BlackHole→NDI 6ch `--out-map 0,1,2`：
合成句收句→渲染→分路播放→收乾淨、dump 含輸入＋回應；io 3/0＝測試台
雙時鐘假影（F7）。UI 頂列補 out 裝置與 map 欄位（空＝舊行為）。
UI bug 一修（`7cb5f658`，Harry「沒有看到 Stop」）：`style.display=''`
掉回 CSS 的 `display:none`＝Stop 永不出現；改 `'inline-block'`。

## §L 和諧掛起格收案（16:00 場 dump 離線審計，沒叫他唱）

Harry 下午自己用 app 跑了一場**有調的歌**（AI-Micro `map 0,1,1` 兩路分路、
19 句、wait p50 0.94s、io 0/0；校準失敗回退 gate 0.02 但斷句照常）。
dump 73MB/13.8 分鐘 → `angel_audit` 離線體檢（14 句實唱、gate 0.02）：

1. **和諧 11.2%＝優於語料基準 12.1%** —— 凌晨掛起的 27.5% 是素材無調
   造成的，有調素材＋key auto 下這格直接過。**§F 那格收案。**
2. **互撞 9.6%＝stab 1 的代價在真歌上消失**（凌晨量 +8.7pp→17.3% 是
   無調自由哼唱；有調旋律天使譜位分開走，撞不起來）＝「跟得上 vs 穩」
   的取捨在真素材上**不存在**，stab 1 白拿。
3. 定調軌跡 [0,-3,-3,-4,-4,…]＝第 4 句起鎖 -4（E 家族）穩定不再跳。
4. 其餘軸：錯開中位 0ms/p90 262ms ✓、子音疊加 +0.0 dB ✓、音量平衡
   +0.1 dB（目標 +0.7，略少可接受）、削峰 max 0.44 ✓、殘響結構 ✓
   （送出量仍待房間裁）。音程 SD 47.4c＝§F 已知口徑未對齊軸，不動。

掛起清單至此：**只剩 indep 終判、殘響房間裁、tail 變體**（全需 Harry
在場）＋選歌拍板＋拍手彈指。

## §M 實體鍵：C 案退場、USB serial 到底、整條鏈驗通（晚場，Harry 在焊台）

**C 案（拍手暗號）退場——Harry 的語意判決**：「拍手跟合唱哪有什麼強聯繫」。
§J（08-03）把拍手包裝成 call-response 手勢，但拍手的語彙屬於打擊樂與鼓掌，
不在合唱的聲音世界——這是美學層、一票制。`clap_probe.py` 留 repo（零成本、
背景量測那半仍是有效數據）但不再擋路。**三案收斂為 A 案。**

**架構連兩折，各折掉一層複雜度**：
1. Harry：按鈕接 unit 1 內的 Pico、長線從出線口出——麥克風上只剩按鈕本身
   （比 boom 桿掛殼乾淨；08-01 WS2812 紅線查過不適用，unit 1 是 OLED 臉）。
2. Harry 再問「這條線不是不會用到 wifi 嗎」→ 點破 unit 1 路線其實仍有
   Pico→Mac 的 WiFi 跳。**既然按鈕都有線了，線就走到底**：Pico 留 Mac 旁吃
   USB（供電＋通訊一條線），按鈕貼 K669B、兩芯長線拉過來。WiFi、AP、掉包
   冗餘、heartbeat 逾時全數退場；WiFi 版 firmware 保留給穿戴模式（身上無線
   可拉，它仍對）。

**盤點補遺（ELEGOO Mega 套件昨盤點漏掉）**：tactile button ×5＋杜邦母對公
×20 在手 → §J「微動開關 £5–15 買手感確定性」那行**作廢**，零採購成立。

**實作**：`firmware/pico_button/main_usb.py`（新；協定同 TAP n／HB n，serial
可靠傳輸＝免冗餘 3 封）＋ `tap_listen.py` 增 `SerialTapListener`（介面與 UDP
版完全同：take()/alive/err 不擋路；auto-glob /dev/cu.usbmodem*）與
`--monitor --serial`。UDP 版 selftest 迴歸 PASS（1.4–1.7ms）。

**驗收（實體鍵硬體那半收工）**：對角腳＋GP15/GND 接線 → 首輪不通 → 全腳位
掃描（PULL_UP 全開逐 hop 印變化）確認線曾在 GP15 活動過 → Harry 重接一次
→ **38 下全收、一下不漏**，最短間隔 200ms＝debounce 如設計。

**協作血訓**：連四輪「我開時間窗、Harry 沒按在窗內」＝異步對話跟不上焊台
節奏。解法是把回饋長在硬體上不是對話裡——板載 LED 按下熄 0.2s，Harry 自己
就能驗，不必與我對時。**硬體在使用者手上時，回饋迴路要留在使用者手上。**

**剩下**：①按鈕貼 K669B（裸貼 VHB 先跑通；迷你座要做就量筒徑）②長線焊上
按鈕＋熱縮 ③ respond2 live 接 `tap.take()`（軟體最後一塊：點一下＝換你、
播放中按＝barge-in）④穿戴模式的真實 WiFi 延遲（不擋展場，掛著）。

## §N VocalSet male3 試訓（深夜；查證單後 Harry:「先訓一顆 VocalSet 試試」）

新聲部語料的實物驗證，全程沒叫他唱。VocalSet 2.1GB（Zenodo 1193957，
CC BY 4.0）→ 11 位男歌手依「乾淨技法時長＋f0 中位」排序 → **male3 出線**
（23.0 min、f0 中位 186Hz＝最低）。乾淨技法篩選（straight/vibrato/
slow·fast_forte·piano/belt/messa；排除 breathy/lip_trill/vocal_fry/
inhaled/trill/spoken）→ 115 clips 21.8 min ＋ 6 val。config 照抄 girl、
MPS 訓練 ~85 分鐘至 15k，val 谷底 **14k＝1.007**（曲線吵＝val 僅 6 clips；
6k 1.023／11k 1.017 皆屬同一噪音帶）→ 取 `model_14000.pt`。
**f0 對照**：male3 p10/p50/p90＝127/186/292Hz（MIDI 47.5/54.1/61.9）vs
girl 197/299/539——**下方聲部 shift≈0 即可用**（girl 要 +12）；
`data_vs_male3/f0_stats.json` 已落檔。
耳測樣本（`vs_male3_trial.py`，配方照抄 angel_audit 同口徑；素材＝16:00
場 dump 兩句最長實唱，key -4）：`harmony/out/vs_male3_trial/`
＝dry／identity（male3 唱他的線＝音色檢查）／lower（下天使線）／
lower_girl（girl 同線對照）／mix。渲染乾淨（無 NaN）。**判決＝Harry 聽。**
過了的話下一步：respond2 lower 換 male3（或 VOICES 加第三聲部）＋
CREDITS 記 VocalSet 署名（CC BY）。

**§N 續（Harry 耳測回收＋跨歌手鑑別＋轉進 GTSinger）**：Harry 裁 male3 轉他的
真歌「完全不像人」→ 鑑別鏈三步：域內（自己的練習音）像人 → 訓過的有詞片段
像人（在訓練集＝證據力打折，Harry 自己點破）→ **跨歌手（male1 同曲、未進訓練）
也像人＝units 不帶說話人、學過的詞會泛化** → 死因收斂為**詞素材庫存**（全包
只有三首短歌的音素）→ **G33 立碑**（方法無罪；重開條件＝無詞聲部/補充料）。
diag 的「auto tune 感」＝測試台配方（vib 0＋逐 tick 量化），非模型病。
Harry 拍板演唱語言＝**中英都會用** → 跨語言泛化成為語料選擇的實測格。
GTSinger 全庫無 Bass（ZH: Alto+Tenor；EN: Alto×2+Tenor）；真 Bass 只有
M4Singer（要填申請表＝Harry 的動作）。**ZH-Tenor-1（真中文流行歌）下載中**
→ 下一輪：真歌語料訓男聲 → 用他的中文/英文段各測一次＝跨語言劣化的
實測數字。

## §O 實體鍵殼：K669B 夾環建模＋出圖（晚場續，接 §M）

**焊接排到明天學校** → 順做兩件：

**焊槍盤點→放行採購**：「越來越不好用」先走盤點——換頭是零元解，但 Harry
已換過多支、參照點是學校焊台＝限制在**工具層架構**（£16 套件發熱芯/頭分離、
溫控量芯不量頭；學校好用的本質＝cartridge 式頭內發熱）→ 購買正當性成立。
選項給了四支：TS101 £69（明天到＋退貨權）／Pinecil V2 ~£35（同級最強火力、
等貨）／HS-02／SI012。註：TS101/Pinecil 吃 PD＝MacBook 充電器直驅，
展場搶修加一顆 45W+ 行動電源即可。

**K669B 殼（`260804_prints/`，Fusion `solo_choir_frame_a` 尾端 9 特徵）**：
C 形夾環（內徑 48.4、壁 2.0、高 10、開口 135°＝扣上撐開 ~3.7mm）＋正面鈕盒。
Harry 兩輪修改：底圓孔→方孔（按鈕可整顆從底進、免穿線）→「兩扇窗」
（底窗內縮 2mm 留橫樑＝按鈕四面有靠）。每刀體積對帳＋探點全過。
**筒徑 48 是 FIFINE pop-filter 配件規格非卡尺實測** → 同出 3mm 試環
（3 分鐘）先驗配合再印全件。

**gcode 撞規自查（Harry 丟 HatchLabs induction PDF 問「有照這個嗎」）**：
第一版切 PETG＝撞「實驗室只有 PLA、Never change the filament」——
fusion-cad 慣例「clip 用 PETG」是家用情境，拿到學校機器就錯。已用 260728
實跑過的 miniis profile 重切 PLA 版（試環 3m25s／殼 13m21s），PETG 版降級
「談好自帶料才用」。維持不加 support（兩座橋 5.0/6.4mm 剛過他們 >5mm 線，
但 support 長在按鈕窗裡挖不出來）。**PLA 夾臂＝一次扣到位別反覆拆裝**，
試環同時驗這件事（白化＝回報，放寬開口重出）。

**明天**：學校焊線＋熱縮 → 印試環 → 印殼 → 組裝上麥。respond2 接
`tap.take()` 仍是軟體最後一塊。

**§N 續 2（跨 08-05 凌晨）＝M4Singer Bass-1 訓成、耳測樣本備妥**：M4Singer
其實**不用申請表**（README 的 Google Drive 連結直下，使用＝視同接受 NC）＝
8.79GB 自動抓完。三位 Bass 比較 → **Bass-1**（148.6 min、f0 中位 MIDI 43.6
＝真男低音；p10/p50/p90＝39.0/43.0/48.1，f0_stats.json 已落）。60 分鐘子集
（684 clips 跨 57 首真中文流行歌＝音素涵蓋是 VocalSet 的量級差）→ 同管線訓
至 14k（訓練與 GTSinger 下載在 ~14k 時被外部停掉；機器沒重開、checkpoints
俱在＝不重訓），**取全域 val 谷底 11k＝0.974**。耳測樣本
`harmony/out/m4_bass1_trial/`（同 male3 trial 的兩句＝可直接 A/B）：
dry／identity（⚠ 他的線 ~52 在 Bass-1 p90=48 之上＝出域風險格）／lower
（下天使線）／oct_down（低八度＝bass 本域展示）／mix。渲染乾淨。
**待 Harry 裁**：①像不像人（G33 的死因在真歌語料上應消失）②英文段測試
還缺素材（他 16:00 唱的語言未確認；「中英都會用」→ 英文劣化那格要一段
他的英文演唱才能量）。GTSinger ZH-Tenor-1 下載中斷、可續（次要線）。

## §O autotune 立案＋prosody 移植 A/B/B2/C（跨 08-05 凌晨，Harry 裁決後開工）

**Harry 裁決：「auto tune 有就是有 放 mix 也是有 有 auto tune 就是不行 就是要解決」**
＝表現層品質的硬標準，成為 standing rule。

**診斷鏈（他點破我兩次之後才收斂）**：①斷續＝uv 段設計性靜音（96%/100% 的
輸出靜音正好落在無聲段；seg1 的響亮素材 62% 是 uv）＝stem 獨聽的必然、
mix 裡由乾聲補＝模型無罪。②autotune＝**架構把他的真實 f0 曲線整條丟掉**，
量化譜位重新合成（幀間活動 13.2c→4.4c、剩下的是定速顫音模板）；girl 同配方
同病（Harry 對照確認）＝**不是 Bass-1 的病、不是訓練方法的病**——他問
「不是跟我的聲音以及女聲一樣做法嗎」＝對，模型層三顆同管線，病在共用的筆。

**修法＝prosody 移植**（`phrase_render` 加守衛參數 `prosody_cents`，None＝
逐 byte 舊路徑）：dev(t)＝他的 f0 − 他自己的量化線，**兩聲部共用同一條＝
音程恆定**（08-02 express 的死因構造上不可能重現）。四版 A/B：
A＝現行；B＝0.29s 窗慢成分（量測：勾滑被砍半＝注入僅 11.6c）；
B2＝0.12s 窗保勾滑帶；**C＝全移植含他的顫音（合成顫音模板關閉）**。
幀間活動：dry 13.2c｜A 4.4｜B 4.9｜B2 4.8｜**C 8.7**＝C 最接近人。
seg1 誠實註記：62% uv＋唱得直＝B/B2 無料可移（注入 1c），C 仍有 6.1c。
產物 `out/prosody_ab/`（mix＋裸 stem ×4 版 ×2 句）**待 Harry 裁**。
儀器血訓：首輪「兩天使音程 SD」把不同和聲音符的正常變化與 harvest 八度
錯誤全算進去＝廢指標，已棄；驗注入改用「B−A 實測差 vs 注入曲線」
（seg0 幅度吻合 14.3c/14.2c、r=0.45）。

**§O 判決（08-05 早）：A/B/B2/C 四版 Harry 全數判不過（「還是都不對」）。**
移植量最大的 C 也沒過＝「補回音內微偏差」不足以殺死 autotune 感。機制檢討：
C 的偏差曲線是「對量化線的殘差」＋clamp ±60c——**換音瞬間的滑行正是被
量化線的跳變吃掉／被 clamp 砍頭的那段**＝autotune 的簽名（音之間的瞬移）
原封不動。教訓：autotune 的主體可能不在「音內平直」而在**「音間瞬移」**。
下一個假設＝D：天使 f0 ＝ 他的連續 f0 曲線 × 和聲音程比（量化只決定音程、
不再決定曲線）——與原始 Beatrice live 的 diatonic shift 同構，那條線
從來沒人抱怨過 autotune。

**§O 續（08-05 早）＝D 版落地**：天使 f0＝他的連續 f0 × 和聲音程比
（量化只決定音程、曲線 100% 是他的、含換音滑行；與原始 Beatrice diatonic
shift 同構）。第一版踩了 §B 的原坑（lead 腦空間 vs f0 mic 空間、差 8 半音
被 clamp 壓平＝D 活動 3.4c 比 A 還死——**血訓第三次應驗：跨空間的量一定
先問座標系**）；換算後 D 幀間活動 10.5c＝全場最接近 dry 13.2c。
`out/prosody_ab/` 現有 A/B/B2/C/D 五版，D 為主候選待 Harry 裁。

**§O 續 2（08-05 早）＝Harry 直指「更源頭」→ E 版（歌手化）**：他點出斷續
仍在＝A–D 都在修 f0、沒動到真正的上游。**源頭診斷：天使不是歌手，是他的
逐幀變換**——voicing（他停就停）、音量包絡（照抄他每音節的谷）、units
（連子音幀都照唱）逐幀複製 → 每個子音天使斷一次、重起音一次＝斷續＋
pitch snap 兩個症狀同源。E＝`phrase_render sustain_ms`（守衛，0＝舊路徑）：
≤250ms 的 uv 洞橋接（vm 補 1、units 凍最後母音幀、音量兩端插值），疊在
D 的連續 f0 上。量測：seg0 uv 洞 97%→6%；seg1 100%→86%（剩的是 >250ms
的長氣音段＝真休止類，by design 不橋）。`out/prosody_ab/` 六版待裁，
E 為主候選。

**§O 判決 2（08-05 早）：D／E 也不過（Harry「還是不對」）→ session 交接。**
六版（A/B/B2/C/D/E）全數未過耳。已排除：模型層（G33 閉環）、f0 曲線層
（B–D）、voicing/音量/units 逐幀複製（E 橋接後 seg0 uv 洞 97%→6% 仍不過）。
**剩餘嫌疑＝合成器層**：DDSP CombSub 的起音形狀／幀級平滑／`_vol_mask` 硬
門檻／33ms porta 的骨架本身——下一 session 建議先做「單一變因裸測」：
同一段素材，(a) 直接用他的 f0＋他的 units 過合成器（零量化零修改＝合成器
上限）對 (b) 現行配方，鑑別 autotune 到底生在合成器前還是合成器裡。
**交接狀態**：phrase_render 的 prosody_cents／sustain_ms 均為守衛參數、
未接進任何正式配方＝live/respond2 行為與 08-04 相同；樣本在
out/prosody_ab/（六版×兩句）；Bass-1 模型（音色已過耳）在
exp/combsub-m4-bass1/model_11000.pt＋f0_stats；一支 code-review agent
仍在跑（產出若落地在 scratchpad/review_code.md）。
