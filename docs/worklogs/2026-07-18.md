# 2026-07-18 — WO-5 滑蓋首印失敗 → v8 C 型槽修正（CAD＋重切）

## A. 診斷（Harry 實印回饋 → 對設計數字）
首印滑蓋兩症狀：①難進入 ②進入後無法維持平行持續推進。對 07-15 §B 設計數字後定性為**設計缺陷，非列印精度**（層高 0.1 救不了）：
1. **蓋下方無支撐（Harry 指出的根因）**：簷（1.2×1.3）只從上方壓住蓋；蓋下是 12mm 空腔——滑入全程前緣懸空，重力下栽、後緣頂簷底，楔死。
2. 簷底 1.2mm 懸垂是無支撐印的（盒腔口朝上、buildplate-only 進不了內腔）→ 垂料 0.1–0.3 吃光 0.2 縱隙。也解釋難進入。
3. 側隙 0.15/邊貼 FDM 極限；蓋寬 60.7 薄 1.6、入口段咬合短 → 抽屜效應（racking）放大偏轉。

## B. v8 修正（fusion-cad bridge，`solo_choir_frame_a`，六刀）
- 下軌 ×2（`wo5_rail_yminus/yplus`）：兩長牆內側，頂面 z12.05＝蓋底名義面（蓋坐軌上滑，**卡點凸點/凹槽/右牆一刀未動**，頂隙 0.25 < 凸點 0.4 卡上不被頂開）；內伸 1.2、內緣厚 0.5、底面 45° 斜回牆（自支撐可印，落牆 z10.35，y− 牆出線窗頂 z9.5 淨空 0.85）。
- y+ 軌斷口 x[−332,−311]：讓開 TP4056（x[−330,−313]、貼 y21 內牆）與充電口（x[−326.5,−316.5]）。
- 導坡 ×3（1.5×0.5 楔形切）：入口端兩軌＋斷口再進入端。
- 簷底內緣 45°×0.8 倒角：1.2 懸垂大半變自支撐（治垂料主因）兼進入漏斗。
- 蓋縮寬 60.7→60.4（offsetFaces ±0.15）：側隙 0.3/邊；簷/軌咬合仍 0.9。
- 體積帳全中：shell 29398.2→29518.6（軌 +110.9×2、斷口 −28.1、坡 −0.45×3、簷倒角 −71.9）、lid 9460.0→9418.9（−41.1）。
- 驗證：滑動干涉模擬（蓋分身 0/+5/+20/+50mm）全 0；探點驗斷口空/軌實/充電口通道空；timeline 351 全健康（僅既有 Sketch25）。
- 背上預覽分身重換：舊 `wo5_PREVIEW_shell/lid` remove，新幾何複製後套原 transform（timeline Move81 挖出：x 軸 −75°＋(318, 153.6, −88.8)），落位與舊 bbox 一致。

## C. 重出＋重切（260717_prints，README §H）
- STL 覆寫 `wo5_shell.stl`（8970→9074 tris，外框 88×90.92×22.68 不變）／`wo5_lid.stl`（寬 60.4）；read-back 解析 binary STL bbox 驗證。
- PLA 重切：`P1_PLA_wo5_shell.gcode` 3h20m／46.35g（**+0.25g ≈ 軌道 194mm³ 的料——支撐無變化的旁證**）、`P2_PLA_wo5_lid.gcode` 37m／11.91g；<5h lab 規則 OK。v6 舊盤（含 Harry 自切 0.1mm bgcode）→ `gcode/superseded/`；`petg_hold/` 三盤仍 v6 幾何，用前要重切。
- **同日追加：左壁音訊進線孔 ⌀6→⌀10.5**（Harry 看模型指出線頭 ⌀10 過不去）。圓心 z5→z6（同心放大孔底會到 z−0.25 破底板；上移後孔緣離地板 0.75／簷底 2.65），外圈口緣 0.4 倒角（內圈被舊孔投影拆成 0.5mm 碎邊，倒角 ASM 炸、照 §9 放過）。dV −116.6＋−2.71 全對帳；探點四方驗證；preview 分身同步刷新。
- **同日追加 2：y− 牆 PAM 出線槽 ×2 高 6→8**（Harry 指示 0.8cm）。往下長 2（槽頂上方 z10.35 是下軌，往上會斷軌）：槽 A z[1.0,9.0]、槽 B z[1.5,9.5]，離地板 1.0/1.5。dV −105.9（毛 128 − 既有空區/舊倒角 22）；新槽底外緣 0.4 倒角（−2.48）——外緣底邊在 **y−41.5 內縮階**上不在 −42，第一輪按 −42 抓邊全空、第二輪誤咬 0.5mm 碎邊（垃圾特徵已刪），診斷式全掃 z∈[0.8,1.8] 水平邊才定位。P1 盤＝3h20m30s／46.18g（中間盤 → `superseded/P1_..._pre-hole.gcode`、`P1_..._pre-slot8.gcode`）。
- **同日追加 3：盒體 y 向拉長 3mm——PAM–出線牆間距 2.0→5.0**（Harry 要 0.5cm；純平移在原盒內極限只有 3.3（y 預算 61 = LiPo 33 + PAM 21.5 + 肋/隙），Harry 拍板「盒子拉長沒關係」）。發現順帶修正：**原南肋是 1.75 厚貼牆實心（3 高），線材只能從 z3 翻出、槽下半被擋**。實作五步：①拆貼牆肋帶（−306.6 分毫不差）②y=−38.5 剖開（splitBody）、南段（含 y− 牆/滑軌/簷/槽/鉤板）southward 3 ③從切面擠 3mm 橋接 join 合體（308mm²×3=+924 ✓）④新 1.2 薄南肋 ×3（+198.7，離板 0.25；板–牆 5.0 中留 3.55 全高淨通道）⑤蓋 offsetFaces 南面 +3（60.4→63.4，+411.3）。盒外 65→**68**。驗證：探點 9 點（2 個 FAIL 是探進槽 A/充電口的自誤，重探 PASS）、滑動干涉 0/+20/+50 全 0、timeline 健康。STL y 跨距 +3 確認；P1 盤 3h24m46s／47.26g、P2 終盤 **38m16s／12.42g**（pre-stretch 盤 → superseded）。教訓：**探點座標必先對照開孔表**——本日三次把探點戳進自己開的孔裡誤報 FAIL。
- **同日追加 4：薄南肋全數取消**。Harry 在 UI 收掉中格肋（Claude 偵測：vol −66.2＝恰一段、探點中 OUT/西東 IN、另偵測到 lid+ai_micro 被 +x 90mm 停放）→ Harry 拍板另兩條一起收、蓋歸位。用新 cut 收尾（−133.3；不刪 timeline 舊 feature，避免他那步變 cache 炸彈）、lid/ai_micro −90 歸位、閉合干涉 0。PAM 南向定位改靠泡棉/線材（三面圍欄仍在）。P1 終盤＝**3h24m53s／47.19g**（pre-noribs → superseded）。

## 過程教訓（fusion-cad skill 素材）
- two-distance chamfer 的距離分配方向不可控：一刀成功但 1.5 吃向下（陡壁非導坡）、下一刀直接 ASM_BL_UNFIN_SHEET 炸掉——**導坡用楔形切（sketch 三角形＋extrude cut）確定性完勝**。
- XZ 構造平面草圖 sketch y 映到 **model −z**：畫在 z=−12 的空氣裡切了個寂寞。座標一律 `modelToSketchSpace` 換算，不手算映射。
- 切割驗證：dV 帳 ＋ pointContainment 探點雙門；feature.faces 的 bbox 有雜訊（回報全寬集合），別拿它當位置證據。
- defineAsFreeMove 對手打 6 位小數旋轉矩陣挑剔（InternalValidationError）——用 `setToRotation`＋`translation` 正規建構。

## 下一步
1. Fusion **⌘S**（v8 檢查點）。
2. 帶新 P1/P2 去 lab 重印 → 印後驗收照 README：滑蓋應可平行滑入全程、卡點手感、鉤板 ⌀3 試扣（±0.1 迭代如需要則併下一輪）。

---

## D. Live app：UI voice→channel mapper 落地＋ `--split-lr` 退役（晚間；code review 先行）

Harry 拉前 W5 Step-2 的「`--out-map` UI 化」（07-16 已 ratify），並要求動工前整體 code review。

**Review（兩個 fresh-context agent 並行，opus 引擎端／sonnet app 端）關鍵發現：**
- `out_map`/`n_out` 只在引擎 `__init__` 讀一次、燒進 `sd.Stream` → `set_control` 改它是狀態假象；正解 = shell 層 `set_out_map()` → `_rebuild()`（引擎零改動）。
- `_rebuild()` 對 engine-start 失敗零防護 → 選了裝置沒有的 channel 會音訊靜默死亡、UI 無感（F7 的 -9998 同雷）。
- `list_devices()` 沒回傳 channel 數；`dry_ch` 無上界檢查（超界 = dry 無聲無報錯）；`out_gain` 不被 rebuild 保留（每次切裝置悄悄歸 1.0）。
- 完整報告：session scratchpad `review-engine.md` / `review-app.md`（ephemeral）。

**`ee487cad` feat：mapper + rebuild 加固（`app/solo_shell.py` + `ui/solo_min.html`，server/ 零 diff）**
- `Bridge.set_out_map(map, dry_ch)`：驗證（長度=4、int、≥0、≤所選裝置 channel 數、dry ∈ [-1, n_out-1]）**先於** rebuild；被拒時 running engine 原封不動、理由進新的 UI 錯誤行。
- `_rebuild()`：catch `(Exception, SystemExit)`、丟棄半殘 engine（`_start` 先賦值後 start 的坑，headless 測試 t5 揪出）、錯誤推 UI；`out_gain` 跨 rebuild 保留。
- `set_devices()`：新裝置裝不下現行 map → 誠實降級（map 關閉＋通知），不留死引擎。
- UI：Harmony 卡新增 Routing 列（Route 開關＋You/V1–V4 channel 下拉；選項 0..N-1 來自所選輸出裝置；You 有 "all"；預設開啟值 0,0,1,1）；被拒後從 bridge 真相重繪。
- 驗證：headless 驗收腳本（真載模型、真開 stream、無視窗）**12/12 PASS**——狀態 carry、5 條拒絕路徑不動引擎、有效 map 套用、route-off 回 mono、CLI 式壞 map 的 guarded 失敗＋復原。

**`923c06f0` refactor：`--split-lr` 退役**（STATE 07-16「下次碰到時退役」的兌現）
- 移除：旗標、`split_lr`/`out48_b` 狀態、互斥 guard、callback 兩分支；help/註解同步。
- **Regime A**：`eval/sm_render.py`（10 s studio_mel clip）default 路徑與 `--out-map 0,0,1,1 --dry-ch 0` 路徑 before/after `cmp` **皆 byte-identical**；`--split-lr` 現被 argparse 拒絕（預期）。

**附帶**
- `475ab775` docs(hardware)：voice-unit 三平面接線圖＋5V 軌 one-pair-per-consumer 規則（下午聊天 Q&A 落檔）。
- 校正：`eval/sm_render.py` 早在 `971697b4` 已 commit——STATE 的「uncommitted」為舊資訊，已修（voice-delay plan Task ① 的 commit 部分其實已完成）。

**下一步**：Harry 開 app 實測 Routing 列（真多 channel 裝置上選 map＋by-ear）；`--voice-delay` plan Tasks 2–3 仍 pending。
