# 2026-08-06 worklog — 日間修理場：08-05 §Q 菜單四刀

接 08-05 深夜 v16–v19＋dump 破案。今日按 Daily 菜單序執行：①裁 v19 →
②粗糙切鍋 → ③enhancer 合批 → ④bass −1 挖根。素材：diag1（179s 真唱，
v18）、diag2（v19 短測，**voiced 僅 1%＝不能當判據**）。
diag 檔已從 volatile 的 /tmp session scratchpad 搶救進
`harmony/scratchpad/`（checksum 驗證過；含 analyze_diag.py、各版 run log）。

## §A ①v19 工程驗證：一條線方向成立＋昨夜量測翻案（12ms 是假象）

diag2 幾乎沒唱 → 改離線重播：從 diag1 off1/off2 還原 tick 級 iv 流
（v18＝逐 tick 全收），同一條流上模擬 v19 兩 tick 一致規則
（`scratchpad/sim_v19.py`）：
- girl 換音 1.9→1.3 次/s、駐留 p50 372→**743ms**；bass 1.9→1.1 次/s、
  駐留 372→**929ms**（唱歌時段計）。
- **翻案**：§Q「換音 7 次/s、駐留 p50 12ms」是 analyze_diag 逐幀 diff 把
  porta 斜坡（3 幀）算成多次換音＋1 幀駐留的量測假象。真實 v18 tick 級＝
  1.9 次/s、駐留 372ms——抖動是真的（音程散），但量級沒那麼末日。
- live 終判仍歸 Harry（`--sustain 250` 唱）。

## §B ②粗糙切鍋：離線正典對照組已產出（待耳測）

`scratchpad/canon_diag1.py`：從 dump 還原當晚三聲部實際唱的線
（sop=lead+12、girl=lead+off1、bass=lead+off2，tick=186ms），diag1_mic
餵 PhraseRenderer 正典配方（enhancer on、uv_dry=0、sustain 250、
增益 0.8/0.85/1.0×0.8 全對齊串流）。9 段渲染 25s 完成，位準對齊
（mix RMS 0.0156 vs 串流 angel 0.0161）。
**耳測法：`diag1_angel.wav`（串流）vs `diag1_canon_mix.wav`（正典）——
粗糙兩邊都在＝模型固有；只在串流＝串流管線固有。** stems 也在
（`diag1_canon_{sop,girl,bass}.wav`）。不用重唱。

## §C ③enhancer 合批（commit `826ae265`）：−12ms 實測，−30~40 估太樂觀

- `Svc.infer` enhancer 移出聲部迴圈一發 batch。44.1k/512＋adaptive_key=0
  時 `enhance()` 全路徑 batch 透明（無 resample、f0 走 squeeze(-1) 分支、
  get_mel/generator 原生吃 batch）。
- 等價性：batch vs 逐聲部 max|diff| 1.3e-2 **< MPS 自噪地板 1.9e-2**
  （同呼叫跑兩次的差）＝等價。
- 時間（0.71s 窗實測）：enhance 67→55ms＝**省 12ms**，§M 的 −30~40ms
  估計太樂觀。block 0.17 帳面：p50 165−12≈153ms vs 預算 170ms＝可行但
  p95 貼線，讓 field test 裁。
- **墓碑：尾段-only enhance**（只 enhance SOLA 用到的尾端 ~0.3s，可再省
  ~28ms）：NSF 源相位從輸入頭積分，切輸入＝整段相位平移（使用區
  diff 0.21、不隨 margin 收斂，非受野問題）＝不上。重開需先解相位
  連續性（如把 accs 相位帳餵給 enhancer 側）。

## §D ④bass −1 破案（commit `6c2bbd4c`，v20）：吸附差補償；girl 音程散同顆 bug

- 先用 §A 重播確認：v19 遲滯殺不掉 −1（11.3%→12.8%，−1 是連續 ≥2 tick
  的系統性選擇）→ 挖 feed 層。
- 根因：`live.py token()` 的 C 大調吸附把黑鍵下折 1 半音＝腦全程在吸附
  空間寫和聲；F6 把 iv 參照改成**未吸附**的真唱後，吸附 tick 的
  iv＝腦內音程 −1。bass 想同度→−1 貼臉；girl 五度→三全音（4/6 其實是
  5/7 的錯位）。
- diag1 量證：吸附觸發 36% tick；P(bass=−1|吸附)=**24%** vs 未吸附 4%；
  修正模擬 −1 佔比 11.3%→**4.2%**、girl 常駐 4/6→5/7。
- 修法（v20）：iv 參照＝`real − d`，d＝(real+offsp)%12 落黑鍵時 1
  （token 同款判準）。仍在 `--sustain` 守衛內。
- ⑦排練譜模式**暫不啟動**：根因已修，反應式還沒到天花板；今晚 live
  若和聲感仍不到位再上（Daily 條件式照舊）。

## 今晚 live 檢核單（Harry）

1. `python spike_stream.py --sustain 250 --block 0.17`（v19+v20＋合批一起驗）
2. 聽：一條線回來沒（①）；bass 還貼不貼臉（④）；慢/缺塊 log（③）
3. 另放 A/B：`diag1_angel.wav` vs `diag1_canon_mix.wav` 裁「粗糙」的鍋（②）
4. 過耳後收尾照 Daily：F6/F7 解除守衛轉常開；app 接 `--sustain`

## §E 晚場 live 三輪＋Harry 委任自主迭代迴圈（21:00–）

diag3（v19+v20+合批、揚聲器）：和諧感有進步；echo 重複句＋怪詞（回授）。
diag4（耳機）：echo 消失＝回授案收（Playbook#15）；怪詞仍在→破案＝gate
開啟幀 61% 是喉麥氣音垃圾（parselmouth 給 562Hz 假 f0 餵腦）→ **v21
`--floor-db`**（`b5fc5a8c`；−36dB 殺 100% 垃圾誤傷 0%）。
diag5（v21、MacBook 揚聲器）：Harry 判「更糟」——但工程面更乾淨（垃圾
61%→20%、bleed 0、地板零誤殺）＝指標沒量到耳朵在聽的東西。

**Harry 委任：自主迭代到滿意為止。** 迴圈設計＝工程指標守退化、質感排名
只能他耳批次驗收：
- **v22 `--replay`**（`ec63d0d2`）：dump mic 離線走同一條 step/brain 路徑
  ＝迭代不用重唱。等價性：包絡 corr 0.972、gate 100%；音程線 ~20%＝
  brain_v2:74 multinomial 逐 tick 取樣（設計如此）→ 和聲指標用分佈。
- battery（melL1 vs canon＋junk/flap/seam）＋battery2（逐聲部×頻段）。
- **H1 AGC 判負**（`--agc` 留旗標不預設）：diag5 melL1 1.638→1.619、
  diag4 1.716→1.742＝沒贏；但抓到「沒力」一角＝串流 stems 比 canon
  小聲 30%（模型吃超訓練位準輸出壓縮），AGC 補一半（mix 0.0325→0.0362，
  canon 0.0390）。
- **量測天花板承認**：canon＝量化骨架＋合成表情、串流＝他的曲線移調，
  melL1 混入「演繹差異」不可歸零——質感判決回到 Harry 耳。
- 診斷旋鈕（`f1174fd1`）：--dump-stems／--ema-alpha／--ema-streak。

**交棒：`scratchpad/聽單_0806/`**（A 原唱｜B 串流現場｜C 正典同譜＝
關鍵判決｜D 串流+AGC｜C 分軌）。C 可接受→磨串流；C 不行→換戰場
（register 映射／排練譜模式）。

## §F 深夜自主迭代續（23:41–08-07 00:18，v23–v26；本節由 sync-state 從 commit 補記）

- **v23 `--enh-tail` 翻案上線**（`ca7df7f4`）：§C 的「尾段-only enhance 立墓碑」
  紙上死刑推翻——SOLA+phase_vocoder 本來就在縫相位。只 enhance SOLA 取用尾段
  ＋0.1s 邊距，replay 實測 RTF 0.83→0.66（−35ms/block），接縫指標 0.36 不變、
  melL1 持平。
- EMA-CPU：units EMA 迴圈搬 numpy（離線無感；live 執行緒搶 MPS 場景待驗）。
- **v24 `--rehearse` 譜模式接線**（路線圖⑦，同 `ca7df7f4`）：ScoreTracker v3
  對位、天使唱譜線（mic 空間絕對音直進直出＝token/吸附/k_shift 整層旁路）；
  譜由 make_score.py 從 dump 自產。**未煙測＝下一步。**
- 感知代理定案：DNSMOS canon 2.47 > mic 2.15 > 串流 ~2.1＝迭代目標函數成立；
  Whisper CER 對唱段判死（轉寫不穩定），咬字量測改 hubert units 保真度。
- **v25 `--lookahead` 判負**（`96a6bf25`）：右上下文 0→186ms 無效
  （units-cos 0.551→0.564），旋鈕留不預設。
- **v26 `--enc-win` 左上下文＝咬字主兇的藥**（同 `96a6bf25`，guarded）：
  歸因＝滑窗 hubert 邊緣幀保真度僅 0.52＝模型原料在編碼步就毀一半；
  窗長劑量反應 0.7s→0.524、1.5s→0.694、3s→0.788（canon 0.813）＝
  左上下文買質感、**零延遲代價**。實作＝encode 專用 3s 滾動緩衝、只取渲染窗
  尾端幀＝下游無感知。EMA α 掃描：0.3 微勝（0.665 vs 0.637）＝EMA 非主兇。

## §F 深夜自主迴圈第二波：咬字主兇破案＝hubert 窗長（v25/v26）

量尺升級：Whisper CER 對唱段判死（轉寫不穩）→ **units-cos 保真度**
（hubert units(輸出) vs units(原唱)＝「發音內容存活率」）＋ DNSMOS。
基線：canon mix 0.813、串流 mix 0.632＝串流丟兩倍發音內容。

歸因鏈（一步步排除）：
- EMA α 掃描 0.12/0.3/1.0 → 0.637/0.665/0.647＝**EMA 非主兇**（α0.3 微勝採用）
- lookahead 8 幀（右上下文）：對位修正後 0.635＝無效 → **--lookahead 判負**
  （旋鈕留；「右上下文」假說死）
- 純編碼模擬（不經渲染）：滑窗最新幀 vs 20s 段參照＝**0.524**＝原料在
  編碼步就毀一半；窗長劑量反應 0.7s→0.524、1.5s→0.694、**3s→0.788**
  （canon 0.813），hubert 成本 15→40ms
- **結論：主兇＝hubert 左上下文不足**。藥＝`--enc-win 3`（encode 專用 3s
  滾動緩衝、只取渲染窗尾端幀）＝**加的是過去＝零延遲代價**（`96a6bf25`）

候選版實測（rep5w3＝sustain250/floor−36/enh-tail/ema0.3/enc-win3）：
- melL1 1.638→**1.332**（rehearse 1.525 都被超車）
- DNSMOS sig 2.03-2.12→**2.24**（canon 2.47＝差距砍半；已超過原唱 2.15）
- units-cos p25 0.335→0.407
其他戰果：rehearse 譜模式煙測過（melL1 1.525、RTF 0.42＝腦旁路省一大塊
＝EarV3/BrainV3 tick 在跟渲染搶 MPS 的證據）；block 0.12 離線 RTF 0.84 可行。
基建：session 背景任務屢遭外殺 → 長鏈一律 nohup 全脫離＋marker 檔。

TODO 晨間：enc-win 5 上限、block 0.12 全配置候選、live 空轉 p50（跑著）；
聽單 v2＝rep5w3/rep5cand vs diag5_angel vs canon。

§F 收官補記：enc-win 5 無增益（melL1 1.355）＝3s 甜蜜點；block 0.12
live 空轉判死（p50 125ms>預算 116、70s 缺塊 30＝音訊執行緒 ~35ms 稅）
→ live 下限 block 0.14–0.15（端到端 ~0.30s）。聽單_0806 已加 E_候選版v26。

## §G 事故記錄＋戰場轉移（08-07 中午）

**Harry 判 v26 候選「沒有比較好」**＝兩輪耳測否決 vs 工程指標進步 →
J-PIVOT：串流層打磨對「擬真」無感，瓶頸＝CombSub 模型天花板本身
（canon DNSMOS 也只 2.47）。戰場轉移：模型層升級 → 6.x Rectified-Flow
（repo 已在：260724_ddsp_svc_6x，exp/reflow-harry-run1 訓到 14000 步；
pretrain 缺件全部可從舊 repo/server 副本補）。

**事故（我的失誤）**：6x 的 `pretrain/nsf_hifigan` 整個資料夾 7/24 起就是
symlink → 舊 repo；我 `ln -sf` 穿透資料夾連結，把舊 repo 真檔
`nsf_hifigan/{model,config.json}` 蓋成自指死圈。
復原：從 `260615/voice-changer/server/pretrain/nsf_hifigan/` 副本拷回
（model 56825430B、config 44100/512 與損毀前一致；enhancer 實載驗證過）。
**血訓：對可能是 symlink 的目錄下 ln/寫檔前先 readlink；跨 repo 共用權重
一律「先查現況再動」。**

## §H reflow 天花板判決＝模型層升級路線成立（08-07 午後）

seg25（diag5 最有力 25s）同段位 DNSMOS：原唱 2.80｜串流 v21 混音 1.24｜
CombSub canon 混音 2.09｜**reflow identity 3.08｜reflow k−12 3.24**
（reflow 為單聲部、混音有折扣，但量級差距遠超折扣）。
「沒有比較好」的完整解釋鏈閉合：串流打磨是在低天花板模型上拋光。

已動工：
- ODE 步數掃描（1/2/4/10）＝即時可行性數字（跑著）
- **reflow-bass1 訓練點火**（684 clips 個別檔 symlink 進 6x data_bass1，
  config=configs/reflow-bass1.yaml，同 reflow-harry 配方）——在 Harry 機器
  上吃 MPS，停：`pkill -f train_reflow`
- 聽單：`scratchpad/聽單_reflow/`＝Harry 單一檢查點（D/E 過不過擬真 bar）

待辦：sop/girl 聲部訓練、spike_stream 的 Svc 換 reflow 推理
（load_model_vocoder／ODE 步數／initial_phase 相位帳重接——直呼
model.ddsp_model 可能保得住）、6x venv 相依驗證。

§H 補記：ODE 步數掃描（seg25、k−12）＝step 1→3.12、step 2→3.27、
step 4→3.30（飽和）、step 10→3.23；25s 段含載入僅 ~8s 牆鐘＝**step 2–4
的即時預算完全可行**。preprocess 多工在 macOS 炸（作者已留 --workers 1
路徑）；bass1 預處理重跑中，完成即點訓練。

§H Harry 判決（聽單_reflow）：**D（reflow identity）「有擬人」＝reflow
路線過耳**；E（他的聲音 k−12）「像強行壓喉嚨」＝出音域強壓，預期內——
正解＝各聲部專屬模型在自己音域唱（reflow-bass1 訓練中）。
**血訓：DNSMOS 給 E 全場最高 3.24 而 Harry 判死＝客觀指標對音域強壓感
全盲**；指標只守工程退化，質感終判永遠歸耳。

## §I 三聲部 reflow 化推進（08-07 傍晚）

- Harry 裁 bass 20000 步樣本「還可」＝**bass reflow 過耳**。bass1 訓練停於
  25697 步（loss 2.3→0.26），保留 model_24000/20000。
- girl（245 clips）→ sop3（710 clips）**排隊訓練啟動**
  （`scratchpad/train_queue.sh`，各到 model_20000 自停＋渲 k0/k12 樣本進
  聽單_reflow/G_*；佇列 log＝train_queue.log；預估各 ~4.5h，總 ~9h）。
- 三嘴到齊後下一戰：spike_stream 換 reflow 推理（load_model_vocoder／
  ODE step 2-4／initial_phase 相位帳重接或直呼 ddsp_model）。

§I 收工（08-08 04:07）：**三張 reflow 嘴到齊**（bass 24000／girl 20000／
sop3 20000 步）。樣本 DNSMOS（seg25）：girl k12 **3.24**、girl k0 3.02、
sop3 k12 2.63、sop3 k0 1.65（出域，預期；sop 用例本來就 +12）。
sop3 偏弱＝候補動作：從 ckpt 續訓加深（train_reflow 可 resume）。
佇列/watcher/caffeinate 全自動收乾淨。晨間聽單：聽單_reflow/G_*。
下一戰開打：spike_stream6（6x venv、reflow 嘴、無 enhancer、
相位帳改 SOLA-only＝gui_reflow 同款）。

## §J spike_stream6＝reflow 串流跑通（08-08 凌晨；commit 1fccf363）

v26 串流機制全繼承、只換嘴。四輪除錯（vocoder 相對路徑×2、bass ckpt
輪替、& 優先權）後 replay diag5 全鏈成立：
- **DNSMOS 2.39＝canon 2.47 的 97%**（舊 CombSub 串流 1.24）
- melL1 1.41、seam 0.34-0.37＝無相位帳下 SOLA＋phase_vocoder 撐住接縫
- t_start 0.7（gui 預設、CombSub 基底淺精修）melL1 微勝＝採用
- 延遲：offline p50 156ms（step 2）→ live 建議 block 0.19（~0.42s）起步；
  提速候選：per-voice 平行、fp16、CoreML
晨間聽單：聽單_reflow/H_reflow串流_全鏈.wav（同 25s 段）。
bass 實訓至 32000 步（model_24000 被輪替清掉＝spike6 用 model_32000）。

## §K reflow live 首試＋「氣音竊竊私語」破案（08-08 上午）

diag7（reflow live 首唱、block 0.24）：Harry「每段結束後有吸口水的聲音」。
dump 量證＝橋接/淡出段高低頻比 0.040 vs 唱歌 0.011（3.7×）——凍結母音在
低音量下 reflow 吐氣音（諧波掉 5.5×、噪聲只掉 1.5×）。
- **v27 `--bridge-decay`**：橋接期 gate 走 exp(-t/τ)（子音仍不斷音）。
  A/B：τ80 尾巴 RMS 0.176→0.127；短橋接（sus120/rel60）尾巴時長 4.1s→1.9s。
  合併上線後 diag8 全程尾巴僅 45 幀（0.5s）。
- diag8 Harry 仍聽到、且描述變成「氣音竊竊私語」→ 逐聲部 stem 歸因
  （--dump-stems）**破案：sop3 唱歌時高低頻比 0.016＝girl/bass 0.005 的
  3 倍**＝這張嘴從頭到尾在吐氣音，與句尾無關。
  根因＝**訓練不足**：sop3 710 clips 只跑 20000 步（girl 245 clips 同步數
  ＝每片段被看 3× 少）。
- 動作：**sop3 續訓中**（pid 46921，從 20000 步接續；train_reflow 自動
  resume 最新 ckpt）；`--sop-db` 新旗標（-99＝靜音）供續訓期間 live。
- 血訓：**訓練與 live 搶 MPS**＝live p50 從 207ms 爆到 530ms、缺塊 27。
  live 前務必 `pkill -STOP -f train_reflow`，唱完 `pkill -CONT` 續跑。
- 未驗：sop 靜音版 live（開了但 Harry 改重開 session）＝下個 session 第一件事。

## §L 氣音三連翻案＋v28/v29（08-08 深夜～08-09 凌晨；commit f44aee21、8cc1b79e）

§K 的「氣音＝sop3 訓練不足」**判死**。Harry 三軌耳測（sopmute／soponly／
bassonly 各聽）：**三軌都有**——bass1 是 32000 步、−12 半音、完全不同的
模型與音域，卻有同一個氣音 → 不可能是任何單一張嘴，必然在共用的上游。

連帶翻案兩條：
- **「換掉喉麥」**：Harry 指正 0802 與 diag8 是同一支 **USB 麥**（頻譜指紋
  確認）。「喉麥」是 worklog 一路沿用的錯誤標籤，我拿「接觸式高頻滾降」
  去論證同一支麥——結論碰巧對上他的記憶，理由是錯的。
- **「輸入位準低 11.7dB」**：同一唱 +13.2dB 推進訓練分布 → CPP **−4.5%**。
  這個「沒效」本身是證據：放大不改變 SNR。

### 方法論血訓（本節最重要）
**reflow 推論的 randn 沒設種子**（`reflow.py:80`）＝同輸入同旗標兩次跑
不一樣。實證：`--no-enhance` 是個**從未接線的空旗標**（`Svc` 收下
`enhance=` 後不使用、`self.enhancer` 恆為 None，但開流橫幅照印
"enhancer on"），拿它當 null 介入卻量到 **+5.9%** → **±6% 以下的離線
A/B 沒有解析度**。08-08 上午那批判讀（AGC +8.4%／step8 +4.6%／
減噪 +15.8%／頻譜平坦度／高低頻比）**全部作廢**。
教訓與 metric-blind-spots 同源：先確認尺有沒有刻度，再讀數。

### v28（f44aee21）
`--t-start`（原寫死 0.7；查上游 `gui_reflow.py` 確認是即時版慣例、非
bug）、`--seed`、移除空的 `--no-enhance`、`mic_check.py`（開唱前收音
體檢；診斷起因＝diag8 SNR 29dB vs 0802 42dB，整輪耳測建在壞底片上）。
Regime A：全預設 vs 加種子的原始碼 **max-abs-diff 0.0**。

### v29（8cc1b79e）
**`--bridge-hold`** 治「唱到一半被切」。根因：`bridge_feed` 進洞要
「無聲**且**音量掉到包絡 1/4 以下」，但**待在洞裡只看 uv**（v17 為修
長休止的 bass 低頻嗡而定）→ 子音瞬間下陷進洞、音量馬上回來但
parselmouth 仍判無聲 → 出不去，gate 一路衰減到零。量證：23 次橋接有
**13 次**洞內多數幀音量已回到門檻以上；36s 連續歌唱裡樂句內中斷 15 次。
修法＝音量還在時**凍結衰減時鐘**（不重開 gate，那會放內插假 f0 進來
＝v17 要防的）。0.5 實測 **15→10 次（−33%）**、零 RTF 成本。
併修 `F0_Extractor` 每 block 重建（parselmouth 便宜所以沒人發現）。
`--f0` 開成旗標；rmvpe **擋掉並大聲報錯**（pretrain 權重是舊版架構、
無 `unet.tf.*`，`strict=False` 會靜默留 50 層隨機權重＝f0 是垃圾）；
fcpe 接上 MPS（RTF 2.13→1.13）但對本問題無額外貢獻，預設不動。

### 上游 gui_reflow 對照：三軸全部結案
| 軸 | 上游 | 現行 | 結果 |
|---|---|---|---|
| t_start | 0.7 | 0.7 | 本來就一致 |
| infer_step | 20–30 | 2 | **無差異**（0.5% 內） |
| extra_time | 2.0s | 0.7s | **無差異**（多給反而略差） |

＝**串流配置不是瓶頸，不要再掃旋鈕。**

### Harry live 判決與新問題
t_start **0.85** 收尾穩（採用；0.95 的收尾 p90 變差）。整體「順很多」。
**新問題＝ㄔ／t 音（time）發不太出來**：量證 `--bridge-hold` 是共犯，
把子音落在 units 凍結區的比例從 **20.1% → 29.3%**（`:770` 的
`al = 0.0` 無條件凍結），且讓它們更大聲（gate>0.3 的子音幀 1010→1171）。
→ **下一步：橋接期改成「音量還在時不凍結 units」**（與 bridge-hold 對
gate 的處理對稱），寫好驗完 Regime A 由 Harry live 耳測。

### 延遲
block 0.24 是這台機器的地板：0.20→RTF 1.07、0.17→1.14（皆不可即時）；
extra 0.7→0.4 視窗砍 42% 卻只省 5% ＝**成本是每塊固定開銷、不是視窗
長度**。再低要動架構（三張嘴目前 for 迴圈逐一推論）或砍 `--enc-win 3`
（會讓子音更糟，不建議）。

### 新血訓：開唱前關掉 Chrome／Adobe
今天**最大的單一改善**，而且不在程式裡：缺塊 **76→2**、p50 **245→217ms**、
同素材 RTF **1.29→0.89**。負載平均一度 5.35、壓縮器裡 3M 頁。
**今天所有跨時段的延遲／掉塊比較都被這個汙染過**，包含「block 0.28
沒買到東西」那條結論——作廢。

### 其他
sop3 續訓已砍（跑到 22065 步；model_22000.pt 留著，live 仍用
model_20000）。STATE 舊版的 live 指令路徑 `../../` 是錯的，應為 `../../../`。
