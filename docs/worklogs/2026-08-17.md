# 2026-08-17 — 應答式 app 整體 review → 20 條全修

Harry：「debug 應答式app」→「就是整體review」→「全部修好給我」。

## Review 流程

兩輪多 agent code review（high 檔）：
- **第 1 輪（diff 範圍 origin/ui-app...HEAD）**：8 個 finder 角度 → 19 候選 → 逐條對抗驗證 → 10 條入榜（4 CONFIRMED / 2 PLAUSIBLE / 0 駁倒；多角度共識另計）。
- **第 2 輪（diff 之外全檔面）**：6 個角度 → 27 候選 → 10 條入榜（**全部 CONFIRMED、0 駁倒**）。
- 驗證手段含磁碟實證：tenor 領唱與 MEM_SPK 借音 npz `cmp` 逐位元相同；cache 中毒三種型態實際重現。

## 修復（六個 commit）

| commit | 內容 |
|---|---|
| `eeea5b3f` | cache key 投影（渲染欄位 only）、npz 原子寫入＋載入驗證、渲染進度行 |
| `042169cf` | worker try/except＋活性偵測、attack/release assert、dump 移關流前＋`--dump-max-min`、心跳斷=⚠GATE DEAD、ready 移開流後 |
| `3c8b4a93` | `--mem-real` 預設 0＋領唱排除＋建庫 gate、vlfloor 記實際發聲音、註解翻案（frame-b64 冤案/+9→14/惰性 shift/68 上緣來歷） |
| `d70d0032` | respond_shell：_retire registry＋兜底 kill、退場 exit 降級、看門狗活動計時、ngain 0 修復、calgate 科學記號＋身分守衛、--octave/--key-seed 種回 |
| `b151725e` | respond2：gate %.6g＋≤1e-6 拒收、--key-seed、tail 後 pre.resync()、rotation 行不縮排 |
| `a54ab963` | UI：bank_st 解隱藏＋dropped/GATE DEAD/WORKER DEAD sniff、mode 下拉 blur＋回復 |

＋ VIVA_RUNBOOK（自檢第 3 步＝整條凍結指令+--view 1；[mem-norm] 消失是正常；dump 時序更新；冷 cache/--rebuild 段）＋ STATE。

## 驗證

- **Regime A 證明**：凍結配置 `--file take17` 渲染，「全部改動、唯 vlfloor 還原舊語意」的副本 vs 改前基準 → **max-abs-diff 0.0**（rms 0.1110 / glide 148 逐位吻合）。
- **渲染決定性**：新 key 重渲的四個領唱庫 vs 舊快取，陣列內容 max-abs-diff 全 0.0。
- **vlfloor（唯一刻意行為變更）**：dump 法醫比對——take17 有 59 個 n≤43 hop；tenor<bass 交叉 **153→13**（殘餘為換音暫態）。A/B 檔留 `scratchpad/rvfix_before/after.wav` 待 Harry 耳裁。
- shell 全 regex 對真實輸出行實測（含新 backlog/dropped 群組、1.23e-05 gate、⚠ 行）；三個 py 檔 py_compile、UI script node --check 過。

## 落榜但 CONFIRMED 的近失（viva 後再議）

- channel-map UI 假設 default 裝置 8 聲道；2ch 裝置上 `--out-map` 開機即死且壞值跨切換沿用（respond_live.html:314 + respond2.py:702）
- 死掉的 duo 按同鍵是靜默 no-op（`mode==self._mode` 早退；workaround：切走再切回）
- 相機 worker 單層 try 包整圈＋暗房間亮度 assert＝一張壞幀/暗場退休整場門控（有橫幅）
- `--gate-model auto` help 承諾 model3 但刻意不載（碼內有註解，help 沒跟上）
- reverb 每 hop fftconvolve 重算 IR FFT（28 次/s）；`--trim` 鍵驗證在建庫之後才 assert
- respond2 檔案模式 gate=0 同樣會一句到底（只修了 live）

## 待 Harry / live 環境

1. ~~vlfloor A/B 耳驗~~ → **同日 Harry 耳裁「after 可以，過」＝結案**（差異範圍 87.6s/110s——voice-leading 有記憶，低音段一次不同配音往後傳；他聽完整體判過，不只低音段）
2. duo 模式 CPU live 實測（盯 drop 讀數）——從未量過
3. app 整鏈 smoke（Start→1-4 切換→Stop→dump 在）
