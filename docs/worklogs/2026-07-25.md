# Worklog 2026-07-25（凌晨場，接 07-24 晚）——載體升級雙線：①combsub 加料重訓＋②6.x reflow

## §A 裁決脈絡

F19 定罪「autotune 感住在合成載體」後的四岔路，Harry 拍板**①②同時做**：①combsub 加料重訓（測資料軸）＋②DDSP-SVC 6.x reflow（測架構軸）。③enhancer 鏈不立項（F18 決賽已在乾淨載體上測過＝4=1 無感）；④WORLD ap 實驗降為保底嘴的閒暇實驗。設計鐵則：**兩線的變因軸分離**——①動資料不動架構、②動架構不動資料（同 F9 clean set、同 hubertsoft encoder、同 parselmouth f0），兩邊都贏再補「reflow＋混合集」收割局。

另插曲：Harry 要求「不考慮 docs、限制歸零」第一性原理重推載體問題（first-principles skill），產出**選項⓪＝母音取樣嘴**——長音根本不合成，直接播他真實錄音的母音單元（音高微修 ≤50c＋SOLA 接縫；每聲部抽不同 take＝天然去相關）；論證基礎＝他自己的身分盲聽 original > 一切重合成（F19 step 3）＝分析再合成家族的漸近線永遠低於錄音本身，而現役長音觸發天使正好只要長音。墓園查過**無此死路**（kNN-VC 諸死因不適用）。暫記待議，未動工。

## §B ①combsub 加料重訓（`260724_ddsp_svc/`，repo 外）

- tenor.wav（`260618/man/`，25.0min，44.1k float32）過同一條 `tools/preprocess_voice.py`（同 0619 配方；`--sr 44100 --min-clip-s 2.1` 保 DDSP `duration: 2` 下限）→ **211 clips / 13.2 min**（`260618/test/man/tenor_clean_clips/`）。
- `data_mix/` = 0619 train 220 顆＋tenor 211 顆（prefix `tenor_` 防撞名）＝ **431 clips ≈ 27.6 min**；**val 原封不動同 5 顆** → test_loss 與舊 run 直接可比（這是本實驗的判準設計核心）。
- config `combsub-harry-mix.yaml`（只改三個路徑），MPS 訓 30k（~65min，7.7 步/s）。
- **結果：test_loss 12k 起落 1.04–1.07 帶、最低 1.042（12k）**，vs 舊 run 自 2k 起平 ~1.09 → **資料軸有真實但溫和的增益；F9「別混舊料」在 DDSP 架構下初步鬆動**（F9 的實證是 Beatrice 時代；此處只是 loss 證據，最終以盲聽為準）。
- 決賽刺激已出：`harmony/out/angel_v2_ddspmix30k_angel.wav`（同 world2_target 管線、`-pe parselmouth -e false` 裸 DDSP、與 F18 決賽 ddsp cell 同配方；main.py 預設 pe 是 rmvpe、未裝會炸，`-pe` 必帶）。

## §C ②DDSP-SVC 6.x reflow（`260724_ddsp_svc_6x/`，fresh clone，5.0 原封不動）

- 6.x master（yxlllc/DDSP-SVC，`743a081`）＝RectifiedFlow（6+6 層/1024ch/50-step euler＋nsf-hifigan vocoder），**純 DDSP 已被砍掉**——5.0 那批 `.double()` 相位累加雷連帶不存在，MPS 免補丁。
- venv 同 python3.10 ＋ torch 2.13.0；requirements 的 `gin` 假貨照舊跳過裝 `gin_config`；hubertsoft/nsf-hifigan pretrain 從 5.0 clone **絕對路徑** symlink（相對路徑 symlink 解析基準是連結所在目錄，第一次連錯過）。
- **新雷 ×3**：(a) 6.x 新加的平行前處理在 macOS spawn 下 pickle 爆（config 物件不可 pickle、`-j 1` 照樣走 ProcessPoolExecutor）→ `preprocess.py` 加 workers==1 in-process 補丁；(b) val 標籤改名 `test_ddsp_loss`/`test_reflow_loss`（監視器 grep 5.0 的 `test_loss` 會永遠靜默）；(c) **`interval_val` 存檔會刪舊 ckpt、只留最新**（`interval_force_save 10000` 才永久留）——run1 谷底顆因此被吃掉。
- config `reflow-harry.yaml`：encoder/f0/資料與 5.0 完全對齊（隔離架構軸）；`device: mps`、`amp_dtype: fp32`、batch 16、workers 0。
- **run1**：1.40 batch/s（combsub 的 5.5 倍重）。val 總 loss：2k=1.004、**4k=0.892（谷底）**、6k=1.001、8k=1.083、10k=1.016、12k=1.054、14k=1.089 → **過擬合遠早於 combsub（4k vs 30k 不到頂）＝大模型×14.9min 資料的預期形狀**；4k 顆已被 (c) 刪除。歸檔 `exp/reflow-harry-run1/`（keep/ 有 8k–14k）。
- **run2**（全程快照版）：2k=1.073、**4k=0.943 已快照在手**；5132 步時進程被外力終止（log 無 traceback、斷在步中）——run1 已證 4k 後單調惡化，**判定證據足、不跑第三次**，取 `exp/reflow-harry/keep/model_4000.pt` 出賽（render 需 ckpt 同目錄有 config.yaml，抄一份即可）。
- **RTF 實測：CPU ≈ 2.05**（57.86s 素材跑 118.8s；combsub 同機 0.09）→ **reflow CPU live 資格出局**；定位＝離線 render 嘴（降 step／MPS 加速未試，屬後續）。

## §D 三格盲聽包（待 Harry 解盲）

`harmony/blind_pack.py` 同配方（0.5 take＋0.8 peak-norm stem、48k、shuffle）：

- **`harmony/out/blind_carrier_upgrade_0725/stim_{1..3}.wav`**＝ ddsp30k（F18 冠軍，控制組）vs ①ddspmix30k vs ②reflow4k，同 take（`260722_harmony_brain/data/take.wav`）同腦線（world2_target `_notes.json` 線）同 trim 0。
- **聽完才開 `key.json`**。依 F19 規則 (b)：以 mix 情境聽（stim 本身已是 mix）。
- 判讀表：①贏控制組＝資料軸成立 → 下一步「錄新料」或「reflow＋混合集」；②贏＝架構軸成立 → 值得補一發 reflow-mix＋MPS RTF 實測；控制組守住＝雙軸皆未過耳，回頭議選項⓪（取樣嘴）或加錄新料。

## §E 教訓（血訓，已固化）

1. **開任何新 trainer 前先查它的 checkpoint 保留政策**——run1 谷底被刪＝白花 1.6h。監視器現在標配「每次 val 快照到 keep/」。
2. 監視器 grep 的字串要先對 log 實物驗過一次（5.0→6.x 標籤改名讓第一代監視器全程靜默）。
3. macOS spawn ＋ 不可 pickle 的 config ＝ 平行前處理必炸；upstream 新功能在 mac 上先假設沒測過。

## §F 解盲裁決（Harry「2>3>1 吧 但是差距非常小」）

對照 key.json：**ddsp30k（控制組）> reflow4k > ddspmix30k**——判讀表第三分支命中：**雙軸皆未過耳，控制組守住**。

- ①資料軸 FAIL → **G30**（混舊料墊底，儘管 test_loss 較低＝loss 弱代理再實證；F9 在 DDSP 下由耳朵重新確認）。
- ②架構軸 FAIL → **G31**（谷底顆仍小輸＋CPU RTF 2.05 出局）。
- 「差距非常小」＝三者同一水位 → 天花板是**嚴格意義的資料天花板**：出路只剩 (a) 錄同條件新料再訓、(b) 選項⓪母音取樣嘴。**現任嘴不變＝14.9min combsub 30k**。
- 全案收 **FINDINGS F20**。訓練側資產保留：`260724_ddsp_svc/exp/combsub-harry-mix/`（G30 證據）、`260724_ddsp_svc_6x/`（G31 證據＋6.x 血訓）。

## §G SOLA 版 DDSP live 重測（Harry 耳判）

「消一半，但是還有」——三者不相伯仲的同夜，SOLA splice 的 pending gate 也解了：

- **判讀：splice-phase warble 是真因之一**（SOLA 有效、滑感砍半）＝ F19 的機制診斷方向正確但不完全。
- **殘餘滑感仍屬 chunking 層、非載體**——離線 render（無 chunking）不滑是分界證據；載體貢獻的是 autotune 質感（F19/F20），不是滑。
- 候選旋鈕（未動工）：SOLA search window 加寬／crossfade 加長／hop 縮短（`world_live.py` DDSPMouth，全是參數層、server/ 零改動）；另一嫌疑＝hubert 單元邊界的 content 不連續（這個 SOLA 管不到）。
- SOLA 保留（無害且有效一半）。

## §H 硬體場（晚間）——Task 4 佈局圖重修（artifact `c86270e1`，無程式碼變更）
Harry 看不懂佈局圖類比小區（「交叉太多」）→ 追查發現不是讀者問題，是圖畫壞：R 路訊號線直接穿過 L 路 100k 電阻本體，跳線弧又多。修法（同一 artifact URL 原地更新）：
- **類比小區重畫**：兩條乾淨直排（左＝R 路→GP27、右＝L 路→GP26，走線順路所致，電路相同），每排「3V3→100k→節點→100k→GND」，節點左出 ADC、右過 10µF 接音源；跳線從 4-5 個減到 2 個（GP27 一個大弧跨 GP26＋3V3、R 音訊線跨 L 直排）。
- **新增「先看懂一路」原理卡**：單路 schematic＋四步解釋（0V 擺動 vs ADC 0–3.3V、100k×2 偏壓 1.65V、10µF 擋直流放擺動、節點即 ADC 讀點），電路對照 `firmware/pico_led/README.md` Task 2 定案確認無誤。
- **新增擬真焊法特寫**：洞洞板實物風格（A–K×1–8 洞位座標、100k 色環棕黑黃金、電解白條紋＝負極朝音源、虛線＝背面焊橋）；洞位是示意，要對上實板需照片＋洞數重畫。
- 順帶回答工具問題：Fritzing（擬真圖始祖、付費）／Wokwi（可跑 Pico+WS2812 但類比前端模擬不了）／Falstad CircuitJS（看電不看圖，驗證 1.65V 偏壓直觀）。盤點結論：焊板用特寫卡即可，零採購。
- **驗證**：電路 netlist 逐段對照 firmware README；佈局幾何逐線檢查無交叉錯誤；artifact 已重新發佈同 URL。下一步＝07-25 NEXT 不變（焊兩軌→LED 掛軌→類比小區 GP27 首驗→整板 live）。
