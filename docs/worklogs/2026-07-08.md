# 2026-07-08

## Session 1 — llm-council: mask frame material (verdict PENDING Harry's ratification)
Question (debiased): PLA print vs wire+fibre vs hybrid for the head-piece structure; goal
"visually striking and professional" (exhibition / viva / PhD-portfolio photos).
Full 5-seat council + blind peer review. Aggregate: Rigorist (1.4) > Red-teamer (1.6) >
First-principles (3.0) > Pragmatist (4.0) > Long-horizon (5.0).

**Chairman synthesis (NOT yet ratified — Harry said "先這樣", decision open):**
- Reframe (unanimous): there is no single "frame" — three separate pieces (rigid cup+boom
  at mouth / BCE-1s in sewn pockets ON the elastic band / soft throat-mic collar). Visual
  quality lives in the FINISH layer, not the structural substrate.
- Only rigid part = the mouth cup + short boom (print or buy; if printing, do it early —
  summer printer queue is the one external schedule risk).
- Head positioning structure: **v1-first gate** — 1-hour wear test of frame_A v1; fits →
  v1 as invisible chassis + fibre/textile visible layer (default); doesn't fit → millinery
  wire formed directly on the head (fallback; no CAD loop).
- Hardened fibre (PVA/shellac paper) is DEMOTED from structure to surface language —
  revises the 2026-06-28 "wire+fibre shell as structure" decision. All 5 reviewers ranked
  the fibre-as-structure answer last (sweat/humidity/8-h/IPA-wipe failure modes).
- Hard rules regardless of branch: v1 never cut/modified (permanent fallback); shellac must
  get a water-based PU topcoat (IPA wipes dissolve shellac); no raw PLA in photos.
- Vetoed: cutting the cup out of v1; full v2 reprint; fibre as primary structure.
Visual: artifact `a9969f55` diagram F (three-piece side view + v1-first flowchart).
**Next:** Harry ratifies (or amends) → write into HARDWARE_ROADMAP + mark the 06-28 §8
entry as revised; then run the 1-hour v1 wear test.

## Session 2 — device hot-plug fix · school-test pivot · Masque research
### A. solo_min device rescan button (`5a7a8f15`, Regime B)
Symptom chain across the evening: throat mic replug → app device list stale → picking a
dead handle → PortAudio `-9986` crash (or a restart ritual every time). Root cause:
PortAudio's device snapshot freezes at init; `sd.query_devices()` in the running process
never sees hot-plugged devices. Fix (bridge layer only, no DSP): ↻ button →
`rescan_devices()` → stop engine → `sd._terminate()/_initialize()` → rebuild (G7 toggle
state carried) → fresh list to UI. Verified headless (stream→re-init→stream clean) + live
log evidence: post-replug index (in=11) appeared after rescan; **overflow stopped once
in/out landed on the same device (in=11 out=11)**. By-ear pass pending. Also re-diagnosed
tonight's overflow spam = F7 two-clock drift (UGREEN in + other out), not a bug.

### B. School test + teacher consult → hardware pivot (docs commit `1d86517d`)
- **DAEX exonerated**: same exciter far cleaner from Mac 3.5mm jack than via UGREEN
  (suspects: UGREEN DAC / 07-07 ground loop). Spotify > our harmony through same chain =
  content + gain-staging variable, separate test. Controlled A/B pending before FINDINGS.
- **Teacher's advice (agreed)**: quality first; chest placement instead of mouth-front;
  don't complicate pickup (no piezo — removed from cart); keep throat mic.
- **Decisions logged in HARDWARE_ROADMAP 07-08 entry**: RØDE AI-Micro ordered (£62) as
  single-clock front-end replacing UGREEN + splitter (4-step arrival checklist); perform-
  mode carrier direction → **chest DML panel** (DAEX25 on light stiff panel; mouth cavity
  PAUSED, not graveyarded); headset metal parts ordered (hair hoops 1.2mm ×10, glasses
  hinges ×4 pairs, spring wire 0.5mm — springs/lashing only, not load-bearing).
- Total order ~£83. Wire-joint hardware scouted for later: RC wheel collars / pushrod
  connectors / K&S brass tube / 4D modelshop.

### C. Research & vault (outside repo, pointers only)
- **Masque (Xin Liu, MIT Media Lab 2017) deep-read** — bone-conduction false-respiration
  feedback altering sense of self (stress p=.048, attraction p=.01, physiology unchanged
  → purely cognitive). Direct prior art for our altered-feedback + bone-conduction line;
  thesis PDF + citation in vault; prior-art note 06 updated (+ Digital Speech Makeup
  ICMI 2021 flagged must-read).
- Vault: theory spine note created (Varela → Dewey/McCarthy&Wright → Tanaka&Donnarumma →
  Masque → Solo Choir; + Pasquinelli critical-AI strand); RAVE+EMG latent-intervention
  idea logged as PhD material (04 note).
- Interactive rig-builder artifact for frame layout planning (real ordered parts, weight
  budget vs 116g, assembly rules): claude.ai/code artifact `2d264167`.

**Next:** AI-Micro arrival checklist (bias → overflow → F6 latency → DAC A/B) · DML panel
by-ear test (scrap 3mm ply + DAEX25) · rescan button by-ear confirm · v1 wear test (S1).
