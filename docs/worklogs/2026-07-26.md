# 2026-07-26

## §A 選項⓪取樣嘴快篩：girl 料驗管線 → Harry 真嗓版 → 四輪耳測判死（G32）

**脈絡**：Harry 指示「和音天使不看限制走 1–8 方向」（07-25 晚間清單），第 1 條＝用 girl 母音料零成本搭選項⓪管線，錄音（Part B）前先驗最大未知數。跨夜執行，07-26 凌晨結案。

**建設（commits `3158a236` v1 → `b4451cb9` v2 → `e1fbc77b` v4）**：
- `harmony/sampler_mouth.py`：build（YIN 逐幀 → 穩定 sustain 單元收割，±0.5st 內 ≥0.35s）＋ render（吃 `{tag}_notes.json`／`.targets.json` 兩種腦線格式，零改動接現有下游；mix 同 world_mouth 0.5/0.8 配方）。
- 單元庫：girl clip_5＋clip_11（9.8min 母音）→ **126 單元，E3–G5，中位 4.4s**；Harry 0619＋tenor（46min 歌聲）→ **805 單元，43–70 全半音階，中位 0.76s**（歌裡收割）。
- **設計規格得證**：全音階庫＋C 大調腦線 → 微修全 ≤50c（girl 中位 8c max 44c；Harry 中位 21c max 50c）——「±50c 微修」假設在管線層成立。
- v2 手勢層：scoop、legato varispeed 彎音（≤4st 彎、大跳重起音）、句尾垂降、呼吸跟隨；YIN 抽查證實 65→64 連續滑音無斷點。
- v4 咬字工程（Harry 兩度判「無對詞一起」後的客觀拆解）：音節帶（2–10Hz）包絡相關 vs take——v1 r=0.02、v2 r=0.67；拆出兩個稀釋源①因果追隨器 +23ms 群延遲②單元自身振幅紋理與咬字訊號 1:1 等量 → 熨平載體＋零相位蓋印 → **r=0.878**（轉換載體參照 ddspgirl=0.951）。

**耳測（四輪，全 FAIL）**：v1「像鋼琴，沒有唱的過程」；v2「還是一樣，無對詞一起」；v4「鬆鬆的音色皮，音跟音接的都不好」；Harry 真嗓版「成果不好，很不穩定」。→ **G32 登記**：全旋律線取樣嘴死於結構（無過渡體、無咬字繼承、收割單元不穩）；選項⓪降級長音候補（②婚配線未判死）。

**機制釐清（Harry 問「不是跟訓練我的聲音一樣嗎」）**：DDSP＝訓練你的音色、現場輸入你的歌聲（詞/legato 免費繼承）；取樣嘴＝零訓練、輸入只有音符清單（詞無處繼承）。同一場錄音餵兩個機制完全不同的嘴——Part A/Part B 分開錄法的原因。

**存活資產**：`sampler_mouth.py` 管線＋單元庫索引（`out/units_{girl,harry}.json`，產物不入庫）＋音節帶包絡相關量測法（咬字耦合客觀代理）。

**待 Harry 裁（影響 07-30 前錄音清單）**：Part B（母音庫 15–20min）三選一——①照錄（賭②長音觸發婚配；乾淨刻意的 2–3s 直音單元會比歌聲收割穩一截）②縮成 5min 保險版（單一八度、a/o 兩母音）③砍掉、分鐘數全數改餵 Part A 訓練料（F20 主線）。Claude 傾向 ②或③：四輪耳測無一正向訊號，Part A 的同條件分鐘數才是 F20 定價的稀缺資源。

**其他**：昨日 Pico 燈條第一段 JST 焊接完成（刮膜＋flux 解「不黏」；蜂鳴六項全過、上電待辦）。任務清單 1–8 中：#1 結案（G32）、#2 母音跟隨隨之降級、#3 咬字/包絡蓋印已在 v4 落地一半（live 版另議）、#5/#6/#8 未動。

## §B G32 撤回：量測歸因 → 消融 v5（Harry 駁回速判後的重審）

**Harry 裁示**：「直接判死好嗎，先好好分析原因吧」——正確。§A 的判死混淆了機制限制與可修因子，逐項量化歸因：

| 嫌疑犯 | girl 版 | harry 版 | v5 消融後 |
|---|---|---|---|
| 循環撐長音 | 0.25x 中位／21% 需循環（無罪） | **1.16x／55%／p90 2.94x（主犯）** | girl 4%／harry **8%** |
| 相鄰單元音色跳動（MFCC cos） | **0.125（主犯＝母音亂跳，選取從未約束）** | 0.069 | girl **0.031**／harry 0.046 |
| 跨 session 拼貼 | — | **25/52 次換音跨房間（共犯）** | 17/52 |
| v4 全熨平 | 載體表情全死＝「音色皮」候選主因 | 同 | 降 0.35（部分熨平） |

**v5 實作（`sampler_mouth.py`，同 commit）**：`timbre_clusters()` MFCC k-means 音色分群（母音無標註的代理）→ 樂句內鎖同群；`pick_unit()` 加 need_len 軟約束（偏好免循環單元，池空逐層退讓）；`flatten_texture(strength)` 參數化。產物 `sampler_{girl,harry}_v5_mix.wav`。**狀態：停在 Harry 耳測重審**——若仍敗，G32 恢復生效（屆時消融證據完備，判死才乾淨）；若過，繼續攻剩餘因子（harry 版跨 session 唯有 Part B 新料可解）。

**方法論教訓（本次真正的血價）**：判死前先做**單因子消融**——耳測 FAIL 的歸因必須先排除選取策略／料的品質／後處理三層，才能指控機制本身。G28「客觀指標沒抓到質感」的對偶：**耳測也會被混淆因子騙**，兩邊都不是單獨可信的法官。

## §C 「偏不穩」排除鏈 → v7 run 制（同單元連續彎音）

Harry v5 重審：「有連貫起來了,但還是偏不穩」。逐項量測排除：音內慢漂移 5–7c（無罪）→ 安定時間 81ms vs 參照 58ms（無罪）→ vibrato 一致性、AM 顫動皆 ≤ 參照（無罪）→ **剩餘差距＝過渡區 std 33c vs 參照 18c ＋ 接縫數量本身**（每音換單元＝34 個交叉淡接;參照是單一連續源）。接棒曲線（入場音高匹配）先行落地但單獨無效（33→32c）→ **v7 run 制**：樂句內音串（音程 ≤4st、距 root ≤3st）鎖同一顆單元一路 varispeed 彎過去＝真 legato 零接縫,只在大跳/出域換單元。接縫 34→14、girl 過渡區 33→23c（穩定區 6c 優於參照）;harry 卡 27c＝短單元強迫斷 run,Part B 新料可根治。`--per-note` 保留 v5/v6 行為供 A/B。**停在 Harry 耳測：`sampler_girl_v7_mix.wav`／`sampler_harry_v7_mix.wav`**。

## §D 夜班（Harry 指示「執行所有 task、自我檢查、裁決留早上」）——六線並進

**D1. Key detection（任務6）＝本夜最大戰果。** `harmony/keydet.py`（音階隸屬度評分,非 K-S 主音;K-S 首測把 A 中心旋律判成 A 大調主音——腦要的是音階集合不是主音）。**重大發現：take.wav 實際是 A 大調**（A 大調集合覆蓋 84% vs C 的 65%）——此前整條腦鏈 `--key C` 假設全程錯誤,VoiceToTokens 的半音 snap 把他的 C#/F#/G# 全部折錯音級＝腦一直在非母語環境工作。`world_mouth.py` 加第 7 位置參數 `key_shift`（auto/int,預設 0 行為不變）：輸入 MIDI 進腦前 +shift 到 C 框架、輸出 -shift 回真實調。兩輪血訓：①pre-pass 用 PitchTracker 差 1 半音（G#）→ 改直接 YIN 2048/1024（與 keydet 驗證同路徑）;②MIDI 域移調是整數加法,誤用 Hz 乘法（tracker.latest 是 rounded MIDI）＋np 型別讓 embedding 炸。**客觀驗收：腦輸出在他真實調（A 大調）內比率 61–72%（world2/3 無歸一）→ 99%（world4keyed）**——天使線至今最大單筆品質跳升,回溯解釋部分歷史「怪」聽感。keydet 自測：移調追蹤 4/4 PASS（f0 域 ±shift root 全對）;girl clip_8 前後半不一致（覆蓋型歌曲 margin 小,conf 弱＝已知限制）。
**D2. 換氣單元（任務3 下半）。** build 加 breath 收割（無聲、>2×底噪、0.12–0.8s、鄰接有聲段）：girl 12 顆/harry 65 顆。render 在咬字蓋印**之後**插入（先插會被 take 靜音段 floor 壓死）,樂句前 25ms、gain=0.3×med_rms:girl 7/13、keyed 4/6 樂句成功插入（其餘 gap 不夠）。任務3 至此三件套齊：包絡蓋印（v4）＋樂句呼吸（v2）＋換氣單元（v8）。
**D3. 多天使三重唱 v1（任務4）。** lineB＝world_mouth 二跑（無 seed 天然不同線）→ girl DDSP 12k `-k 12`（血訓:main.py 必須在 260724_ddsp_svc 根目錄跑,hubert 相對路徑）。lineA/B 同音只 35%＝真兩條線;`out/trio_mix.wav`（take 0.5＋兩天使各 0.6）。**Caveat 已量化:兩天使互不聞,27% 時刻互撞不和諧音程**→ brain v3 方向（雙聲部條件生成）或碰撞消解後處理,早上裁。
**D4. 預感定價（任務5）。** peek_probe_v2 重複樂句實驗:take 樂句1 單遍 pitch-masked top1 24.1%/top3 62.1%（已高於 G29 Bach 基線 11-16%）;同樂句 ×2 聚合 32.3%/66.1% → **推回第二遍單獨 top1≈40%、top3≈70%（他的真嗓實測）**。「第二遍副歌」賭注首次有實測定價;timing 預測仍差（G29 那半不變）＝預合成音高、時機由真實 onset 觸發的設計成立。
**D5. 天使性格素材（任務8）。** respond.py 離線跑 respond_take（10 短句）→ `260722_harmony_brain/data/respond_night_{angel,mix}.wav`（待耳測,連同 07-23 掛起的 sustain_mix/sustain_resp_mix）。四性格版圖:同行（world_live/solo_min 線）/應答（respond）/長音降臨（sustain,ear gate 未過）/預感（D4 數字放行設計）。
**D6. 天使的身體（任務7,設計定稿）。** 天使 voice unit 的 LED＝既有 Tier-2 ADC 分接架構原封套用:分接點＝該 unit PAM **輸入端**（07-13 規則:永不碰 BTL 輸出腳）,GP27 類比讀取＋min-of-3＋NOISE_FLOOR 500（b1bdd569 配方）,韌體 MODE="live" 不需改——**每個 unit 的燈本來就唱自己的聲部,天使 unit 掛上去即得「它唱它亮」**。07-28 兩 unit 整合時零額外工作,僅接線位置決策（天使聲部路由到哪顆 unit＝`--out-map` 一行）。
**D7. 嘴×keyed 線鋪面。** `ddsp30k_keyed_{angel,mix}.wav`（現任嘴×在調線＝早上主打）;取樣嘴 v8keyed:girl 庫是 C 大調全音階,A 大調線 7/21 超 50c（最大 113c）＝**庫的半音階覆蓋是取樣嘴的硬需求**（harry 歌聲收割庫全半音階,同線只 1/21 超標）——Part B 清單「五母音×半音階」的設計被反向證實必要。

**產物清單（全部 headless 驗證:時長/峰值/NaN）**:sampler_girl_v8{,keyed}_mix、sampler_harry_v8keyed_mix、trio_mix、ddsp30k_keyed_mix、angel_v2_world4keyed_target_mix、respond_night_mix、probe 數字。**耳測與裁決全部留給 07-26 早上**（清單見 morning report）。

## §E 早上耳測全數回收 → 四項裁決 → 直驅嘴（autotune 的未檢驗環節）

**Harry 耳測（七項）**：①`ddsp30k_keyed`「和音表現確實滿好的**但音質還是 autotune**」②sampler v7「好了一些，但有幾個段落還是斷斷的尤其前面」③v8（＋換氣）「跟 v7 差不多」＝換氣無感 ④harry v8keyed「不自然」⑤trio「可以，但一樣 autotune 偏重」⑥respond「還不錯」＋問調性 ⑦長音定位**否決**：「只做長音不太符合定位——一個只會長音的天使（還不是跟上的那種），還是和音天使嗎？」

**裁決落地**：
- **選項⓪全線判死（G32 恢復生效，含長音候補）→ Part B 母音錄音取消**，15–20 分鐘全數改餵 Part A（F20 的稀缺資源＝同條件訓練分鐘數）。錄音清單待改。
- **多天使＝走雙聲部條件生成**（brain v3，週量級；碰撞消解後處理不做）。
- **預感＝開**（本節 D 已落地，見下）。
- push 完成（origin/ui-app ＝ `48e8b53a`）。

**respond 調性解答（任務6 延伸）**：respond.py 本來就吃任何調（`--key C/D/Eb/F#/Dm…`），**現在預設改 `auto`**＝自動偵測，不需人工校準；live 模式在第一句鎖定。實測 respond_take＝**D# 大調**（此前一律強制 C）→ 天使音落在他真實調內 **57% → 94%**（`respond_keyed_mix.wav`）。

**⭐ 直驅嘴 `direct_mouth.py`（autotune 追殺，本日最大技術發現）**：F19 消融判「合成載體本身」有罪，但**它定罪的載體從未被隔離**——現行鏈路是「腦 → WORLD 合成 → DDSP 轉換」，DDSP 一直在忠實轉換一個**已經帶 WORLD buzz** 的訊號。直驅嘴把 WORLD 整段拔掉：DDSP 吃**他的原始 take**（content units＝他真實的音素/咬字/氣息），f0 用腦的目標線注入（走 main.py 既有 f0 cache，DDSP repo 零改動）。量測：音高準度 **9c**（WORLD 路 8c，平手）、**咬字耦合 r=0.965 vs 0.946＝史上最高**（超過 ddspgirl 參照）。血訓：cache 檔名的 hop_size 是浮點（`512.0`），名字打錯 → main.py 靜靜 extract 他的原音高並存成正確名字**污染後續執行**。產物 `direct30k_mix.wav`（他的音色）、`directgirl_mix.wav`（她的音色唱他的字）。

**預感式落地（任務5）**：`BrainV2.step_anticipate()`——用腦對「他下一顆音」的預報提前決定天使的音（context 不留痕，真實 token 隨後正式寫入）；`world_mouth` 第 8 位置參數 `anticipate`。設計要點：**只預測音高，不預測有沒有在唱**（voiced 用他當下真實狀態）——首版沒遮 HOLD/REST 導致腦一路押 HOLD（G29 的老毛病），天使發聲率 92%→42% 直接閉嘴；修正後 91%。成效：**天使換音與他 onset 同時落地 26/43 → 40/43（60%→93%）**，在調率維持 99%，與反應式選擇一致率 47%。產物 `angel_v2_ant2_target_mix.wav`（WORLD 嘴）、`direct30k_ant_mix.wav`（直驅嘴＋預感）。

## §F 夜場（跨夜到 07-27 凌晨，獨立 session）wire-light unit1 — 從「插上去不亮」到完整正式狀態運作

承 07-24 §N 的 NEXT（焊兩軌→LED 掛軌→類比小區 GP27 首驗→整板 live）。Harry 焊完後回報「都焊接好了怎麼測試」，一路查到四個實體故障＋一個軟體設計問題。commits `e479fe2c` / `869506eb` / `f834388d` / `dc368938`。

**結果：unit1 在完整正式狀態下跑起來了** —— 拔 USB、吃外部 5V（不吃 USB＝07-12 接地規則）、`MODE="live"`、真實音源（`trio_mix.wav`／YouTube），燈跟著音量動、音停會暗。plan gate **T1 ✅ T2 ✅**（開口就反應／流動有生命感／靜音底光穩定，三判準全過）。

**四個實體故障（依發現順序）**

1. **WS2812 電平門檻**（主線）：資料誤碼＝隨機分散且會變化的彩色，送靜態純紅也看起來像在跑燈。量出門檻曲線 4.825/4.9/5.0 → 串 1N4007 在燈條 5V 進線。**推翻 README 舊結論「直連即通，3.3V 過門檻沒問題」**——那句是踩懸崖，07-23 能過只因 TENMA 實際輸出略低。詳 FINDINGS **F21**。
2. **下臂 100k 冷焊**：中點量到 3.26V（≈3V3）一眼定位；重焊後底噪降 65%。
3. **排母 GND 那一格虛焊**（最有欺騙性）：症狀＝拔 USB 後 Pico 不執行、燈凍住，但 VSYS 量到漂亮的 4.56V。四重偽裝：空載無電流就無壓降／**蜂鳴檔照樣叫**（門檻 50Ω，而幾十歐姆就足以讓 Pico 開不了機）／USB 插著時地走 USB 回 Mac 一切正常／Pico W 沒有電源指示燈（板載 LED 在 WiFi 晶片上）。**診斷心法＝兩條杜邦線（VSYS＋GND）直接從電源接到 Pico、繞過整塊板子，再逐條拆回二分**——比逐點量測快一個數量級。中途先繞路查過電流不足（低電流測試光仍不跑，排除）與 VBUS/VSYS 接錯（Harry 自己發現接在 VBUS，已改 VSYS）。
4. **3.5mm 沒插到底 → 單聲道消失**：症狀是「一條燈完全不動、另一條正常」，極易誤判成板子壞。

**一個軟體設計問題：燈條自我耦合造成自鎖**
燈黑底噪 ~1000、燈全亮 ~5300（同一時刻黑→亮→黑可重現）。單一門檻 → 燈亮→破門檻→判定有聲→燈續亮，音停不熄。**麵包板 Task 2 沒發作＝當時 1 條燈、1 路分接、燈條直吃低阻抗軌；今晚為修①加的 1N4007 在供電路徑上加了阻抗，脈衝才變成電壓波動（修 A 引發 B）。** 三解法：消隱取樣（底噪 3203→729 有效，但 write 122px 3.7ms×2 條＝實際黑 5ms/20Hz，**閃爍可見，Harry 否決**）→ 遲滯門檻 2000/6000（自鎖解決但**中等音量振盪、反應鈍**，Harry 耳判）→ **耦合補償 `span -= COUPLING × env` 採用**（耦合正比亮度而 env 就是亮度；燈暗不扣、全亮抵消 → 單一低門檻 2000，無振盪、與音量無關）。

**其他落地**：`N=(122,122)`（色帶尺法實測，每 100px 換色數完整色段＋末段比例，比 walk test 快）；反應調快 `SPEED_MIN` 25→60（跑滿 122px 4.9s→2.0s）、`DECAY` 0.55→1.30（收聲 8s→3.4s）；Mac 音量直接決定訊號強度（44→span 1500-3200、65→9000-10000，工作點 65），而燈暗底噪 ~1000 不隨音量變。血訓：**`mpremote` 一連上就打斷 `main.py`**（raw REPL 停掉韌體且不自動重跑）——「連著讀 ADC」和「看燈的行為」不能同時做，Spotify 那次「燈不亮」就是我掛在 REPL 上讀數導致。

**未解／NEXT**
- 補償版 `dc368938` **Harry 目視驗收未完成**（當晚停在「先這樣吧」）
- **中點 0.52V**（應 1.65V）未查完：修下臂後「拔 Mac 端量中點」這個決定性測試從沒做過。不擋路（訊噪比夠、`NOISE_FLOOR` 已補償），但 1.65V 才是正解
- **大電解在 1N4007 哪一側未確認** —— 這是耦合的硬體根治（要在燈條側；必要時 470µF，改完 `COUPLING` 要重量）
- 一條燈**接觸不良**（間歇，會自己回來）未定位：閃爍測試＋搖動法可查，JST 是頭號嫌疑
- **unit 2 零件不足** → 去 lab 拿：0.1µF 陶瓷 ×4（比 10µF 電解好找，32Hz 截止綽綽有餘）、1N4007 ×數顆（現已用完）、100k ×4
- Task 4 機構部分（Pico 入盒卡位、燈段沿線可逆固定）、Task 5 unit 2、音染 A/B（等 PAM 上線）
- unit 沒有電源開關 —— 建議 TP4056 OUT+ → MT3608 IN+ 之間補一個（充電時斷負載、展場關機、故障時可切斷）
