# Worklog 2026-07-27

## §A 耳測回收＋裁決（下午，主對話 session；無程式碼改動，純裁決與規劃）

07-26 留下的待耳測清單（STATE 行首「⏳ 待耳測」四件）由 Harry 全數回收：

1. **direct30k_mix / directgirl_mix — PASS（方向確認）**：「autotune 確實有減少，還無法說是完全沒有，但好多了。」→ 直驅鏈路（DDSP 吃原始 take＋腦 f0 注入，WORLD 拔除；worklog 07-26 §D 落地）成為天使嘴主線。殘餘 autotune 未清零，來源待查（候選：f0 注入線本身的量化／PORTA）。
2. **direct30k_ant_mix — FAIL**：「會滑音導致和音偏離，而且是每個音都在滑音。」預感式暫不進 live，待修。**工作假說**（本 session 提出，未驗證）：`world_mouth.py:33-34`／`direct_mouth.py:27` 的 PORTA=0.15 一極濾波是**連續套用**（非按需），疊加預感與反應式僅 47% 一致（worklog 07-26 §D5）→ 預測錯的音先滑向猜的音、真 token 到了再滑向對的音＝每音兩段滑。**驗證法**：把「預測≠實際」的音列表對照滑音位置。**修法候選**：①預測信心低不提前動 ②修正時跳變不滑 ③portamento 改按需觸發。
3. **respond_keyed_mix — 條件 PASS＋一個設計題**：在調時表現好；但使用者跑調時天使跟著飄。→ **Harry 裁決：天使＝調性錨**——定調後不跟飄；使用者跑調時聽到的摩擦是把人拉回來的功能（真實合唱團的行為），不是 bug。live「第一句鎖定」（keydet 滑動窗 8s）即錨的正解；「跟飄」今後判 bug。待辦：查 respond 天使 f0 是「調內絕對音高」還是「相對使用者 f0 的音程」（respond.py 在 harmony_brain repo，本 repo 文件未收細節）；鎖定準度（覆蓋型歌曲 conf 弱，keydet 已知限制）另列。
4. **認調不需預先設定**：render＝每 take 全曲直方圖重判；live＝第一句鎖定。使用者「每次開口不一樣」由此吸收，操作零設定，維持現狀。

**Harry 併裁**：**brain v3（雙聲部條件生成）不受時程限制，規格討論完即可開工**；v3 應建在直驅鏈路上設計（07-26 該裁決成立時直驅嘴尚未驗證）。規格待決四題已列（見 STATE 行首），待一場 Grilling：①聲部配置 ②條件形式（串聯 vs 聯合，27% 互撞的解法本質）③兩天使個性同/異 ④CPDL 語料 lead 假設（`cpdl_pairs.py` 現只抽 top-two，需擴）。

**個性版圖現況**（worklog 07-26 §D5 四性格）：同行（現行反應式）＝在役；應答＝採用（錨版）；預感＝開、待修滑音；長音降臨＝死（G32 連帶否決）。未決的不是「還有哪些個性」，而是多天使組合（v3）與演出時個性怎麼切換（介面題，連到 viva demo 操演設計）。

**拍攝策略釐清**（Harry 問「影片要拍什麼」）：Documentation video ≤3min 佔 5%、viva 80%（vault 論文 audit 筆記）。07-30–08-01 拍攝 v1 用**現行已運作系統**：solo_min（5 聲部 live）＋穿戴硬體＋wire-light（07-28 T5 gate）；離線 render 的和音成果當研究歷程音軌素材。直驅嘴／respond 的 live 化目標放 **viva demo（08-26）**，viva 後樂器進步再拍 v2（vault 既有規劃）。live 化可行性依據：realtime DDSP 嘴已在役（hop p95 25ms、RTF≈0.09 CPU，worklog 07-24）。

NEXT（無時限排序，詳 STATE）：07-28 Pico T5 → 錄音場次 Part A（07-30 前）→ 拍攝 v1 → 滑音修復＋新料重訓 ∥ v3 實作 → live 化整合 → viva。

## §B v3 規格 grilling（同日下午續，六題全釘）

方法論先議定：**grill 釘終點，實驗選路線**——Harry 質疑「決策會不會反而限制執行」，共識＝品味/終點決策（只有 Harry 能答、缺了必做錯方向）進 grill；技術路線決策（串聯 vs 聯合等）不預先拍板，走「雙軌實作＋盲聽裁決」老模式（F19 岔路①②同時做、G32 判死前先消融的傳統）。

六題裁決（詳 spec `specs/2026-07-27-brain-v3-two-voice.md`，spec 為準）：
1. 驗收 gate＝**耳測 A/B 一票制**（trio v1 vs v3 盲聽，數字只歸因）。
2. 互撞＝**短同音可、黏線不可**。
3. 聲部配置＝**上下包夾**（cpdl_pairs 從 top-two 改抽 lead 上下聲部）。
4. 音域外＝**八度摺疊**。
5. 風格＝**獨立對位線進訓練；平行和音＝零訓練切換模式**（Harry 原答 1+2，釐清平行不需語料後定案）→ 樂器內建平行/獨立一鍵 A/B＝RQ1 現場對照。
6. viva demo＝**一首歌走完弧線**（獨唱→包夾→應答段→平行/獨立 A/B→收），切換用實體鍵/腳踏。

**v3 正式解鎖動工**：開工順序＝語料工程（可與錄音/重訓並行）→ 串聯/聯合雙軌小規模 → 盲聽包 gate → live 化。實作場開場讀 spec 即可接手。

## §C v3 語料工程完成（同日晚，Harry 裁決 push 後即開工）

三個新腳本（`harmony/`，全 additive，v2 管線 `cpdl_pairs.py`/`corpus_split.py` 一字未動）：

- **`cpdl_triples.py`**：CPDL → (upper, lead, lower) 三線對齊。復用 v2 全部工具函式（排序/對齊/轉調/tokenize/filter）；每個**相鄰三聲部窗口**出一組、中間聲部＝lead（上下包夾裁決）——SATB 曲出 (S,A,T)+(A,T,B) 兩組。輸出 `tokens_v3/cpdl_v3.npz`（tok_<sid>w<j>: (T,3) [upper,lead,lower]）。
- **`chorale_triples.py`**：JSB 聖詠（music21 corpus）同款三線（v2 chorale tokens 僅 S/A 兩線）。共用 `extract_score`。
- **`corpus_split_v3.py`**：**按歌切** 5% val（seed 7）——同歌相鄰窗口共用兩條聲部，按窗口切會把 val 聲部洩進 train。免品質過濾：兩來源皆真實譜面（v2 oblique 過濾只針對機器衍生 silver 線）。

**數字**：CPDL 2,842 檔 → 1,219 首、2,505 組（2.31M ticks；skips: dup_page 1,279／lt3_parts 296／off_grid 30／parse_fail 18）；chorale 371 首 → 791 組（180k ticks、零跳過）。合計 **3,296 組三聲部、2.49M ticks ≈ 7.47M interleaved tokens＝v2 語料的 1.6×**。split＝1,511/79 首（3,126/170 組）。產物驗證（read-back）：shape (T,3)／vocab 0–50／phase 對齊全過；音高排序 cpdl 2,465/2,505、chorale 786/791（少數為八度摺疊後聲部交叉，同 v2 行為）。

**明文記載的語料邊界**：POP909 天生無第三條人聲線，不在三線語料內——它在 v3 的角色（是否以雙線形式留在混訓當風格來源）是模型階段的實驗變數，用結果裁。

NEXT＝模型階段：串聯 vs 聯合**雙軌**小規模訓練（spec 技術岔路區），產 trio v3 盲聽包 vs trio v1 → Harry gate。

## §D 雙軌訓練發射（同晚，過夜跑）

**`harmony/train_v3.py`**——雙軌共用一檔的設計發現：三聲部 interleave 成 **[lead, upper, lower] 逐 tick 因果序**後，lower 天生看得到同 tick 的 (lead, upper)＝鏈式分解 P(u,l|lead)=P(u|lead)·P(l|lead,u)，**聯合式的互撞感知是架構內建的**。串聯式＝同資料同架構、loss 只算 lower（`--serial`；target 位移一格 → mask `t%3==1`，crop 對齊 3 的倍數保 voice embedding 對齊）＝專職第二天使，推論時 upper 由 brain_v2 供給（訓練吃真 upper、推論吃生成 upper——串聯的分佈偏移弱點會誠實暴露在盲聽裡）。vs train_v2 差異：語料 tokens_v3、voice embedding 2→3、無 induction probe（v3 語料無 POP909）；12.27M 參數、CTX 4096 不變；src accent id 沿用 v2（chorale 0/cpdl 2）。

兩模式 smoke 30 步各過（loss 正常下降）。**全量 4,000 步×兩軌已發射**（MPS 單佔，串跑；~9.5k tok/s 估 2.9h/軌）：ckpt `harmony/checkpoints/v3_joint/best.pt`、`v3_serial/best.pt`；log `checkpoints/v3_joint.log`、`v3_serial.log`。

**NEXT（訓練完成後）**：①三聲部 render 管線（brain_v3 推論器：joint＝單模型逐 tick 生成兩天使；serial＝brain_v2 出 upper→v3_serial 出 lower）→ trio v3 兩版 vs trio v1 盲聽包（blind_pack 配方、同 take）→ Harry gate。②盲聽包量測附互撞率/黏線統計當歸因。
