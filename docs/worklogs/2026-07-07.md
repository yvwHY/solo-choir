# 2026-07-07 — exhibition take-home QR (audio over LAN)

## Take-home QR for the rendered mix (`91bac453`)
Architecture decision (user chose LAN over cloud): QR cannot carry audio (~3 KB limit), so the file
is served from the Mac itself — works offline on a shared hotspot, no accounts.
- `studio/studio_bridge.py`: `make_takeaway_qr()` — one extra `ThreadingHTTPServer` bound to the LAN
  interface, port 8899 (ephemeral fallback), serving **only `recordings/`** (UI's own server stays
  loopback; path traversal verified 403). Mix → `afconvert` m4a (453 KB wav → 43 KB; wav fallback)
  → minimal dark player/download page → segno QR.
- `studio/ui/index.html`: "⬇ Take-home" button in the listen step (enabled after render), QR shown
  in the existing cardBox overlay with the URL caption.
- Verified headless: page + audio fetch over the LAN address; traversal blocked.
- Exhibition notes: macOS asks once to allow incoming connections — accept; audience needs the same
  Wi-Fi (consider a second printed QR with the hotspot credentials).

## Take-home polish (same session)
- Crooked QR fixed (`ef0235b3`): segno 264px stretched to 300px with pixelated rendering made
  module rows uneven → supersample (scale 20 → 740px) + smooth downscale; card QR too (scale 12).
- Keepsake-card design pass (`0d59b17d`, ui-ux-pro-max): kept minimal single-column/one-CTA
  pattern, rejected generic palette/webfonts (offline LAN → SYSTEM fonts only); brand teal +
  mono letterspacing, date stamp, 48px AA CTA, iPhone download hint, safe-area/dvh/reduced-motion.
  Verified at phone width in Chrome.

## QR entry-point merge (`f3cb70c2`, user-verified "很順")
- User review caught a real design flaw: two sibling QR buttons (Make card / Take-home) at the same
  toolbar level, same output modality, different payloads → merged into ONE "◫ QR" button; overlay
  chips "♪ recording" (default after render; disabled before) | "◫ score card". Chips stop
  propagation; scrim click closes. Verified statically (guards, no console errors) + by the user.

## Next
- Real-phone test at home (scan from a phone on the same Wi-Fi).
- Optional later: cloud-upload variant if the venue has reliable internet.

# 2026-07-07 (session 2) — cross-model institution docs (`4fe5854a`)

Last planned Fable-5 session, spent on distilling accumulated judgment into repo-resident
institution files so ANY future model (Claude Opus/Sonnet, Codex) inherits it:

- **`docs/GRAVEYARD.md`** — 19 dead ends with root causes (stream-mixing family,
  neural-detune deadzone, "distinct speakers = ensemble" disproven, CREPE, heuristic ceiling,
  mask-CAD lofts…). Rules: check before building, register when dead, never delete entries.
- **`docs/FINDINGS.md`** — 10 validated recipe/fact groups with numbers (glide
  3–6¢/10ms + 80ms debounce, decorrelation 25¢+20ms, latency table w/ 46ms sweet spot, kNN-VC
  refs, wiring, training hygiene, viva framing).
- **`docs/STATE.md`** — state moved OUT of CLAUDE.md (it had rotted badly: still
  claimed "2 voices, render-SATB not executed"); CLAUDE.md now carries a pointer only.
- **`docs/VERIFICATION.md`** + `tools/render_diff.py` — three-regime doctrine
  (A behaviour-preserving → max-abs-diff 0.0 / B intentional live → numbers+ears / C offline
  render); the diff check is now a script, not folklore.
- **`docs/DEBUG_PLAYBOOK.md`** — 14 diagnosed symptom→root-cause rows.
- Thin trigger skills `graveyard` / `audio-verify` / `live-audio-debug` / `sync-state`;
  **`AGENTS.md`** at repo root for non-Claude tools.
- Claude session memory migrated into the repo: 30 project-fact memory files deleted after
  migration (backup `~/.claude/backups/memory-solo-choir-pre-migration-2026-07-07/`).

Verified: `render_diff.py` exercised (identical→0.0/exit 0, different→nonzero/exit 1); all 14
referenced paths + 20 commit hashes checked; fresh-context Opus blind review of the whole doc
set (2 low-severity fixes applied: drifting ahead-count wording, `--render` line pointer).

## Next
- Keep STATE.md fresh via `sync-state` after every commit checkpoint (session 1 of today
  already updated it — the loop works).

---

# Session 3 (evening) — studio → satb2; MIDI myth corrected

## Studio voice render switched to satb2 (`b566d1c9`)
- `studio/studio_bridge.py`: `STUDIO_MODEL` = `studio_api._SATB2_MODEL` (same multi-speaker
  model as solo_min live), keeping the dir-missing → `USER_MODEL` fallback.
- `server/studio_api.py` `render_score`: satb2 speaker split changed from pitch-median
  (≥60 → female — never fired on male-range takes: this take's medians were Bass 46 /
  Tenor 49 / Alto 58 / Sop 58, ALL male) to **part name**: Sop/Alto → spk0 female,
  Tenor/Bass → spk1 male, matching live. `arrange.py` CLI keeps its own pitch split.

Verified (Regime C, same take `studio_mel_20260707_013135.wav`, score transcribed once):
- 0627_2k vs satb2: all four stems differ (mix max-abs-diff 0.21) — model really swapped.
- pitch-split vs part-name split (both satb2): **Tenor/Bass 0.0 byte-identical**,
  Sop/Alto differ — the split change touched exactly the two voices it should.
- Harry A/B'd all three mixes by ear and approved the part-name satb2 version.

## Correction: MIDI was never in solo_min
Harry hit "no MIDI panel" in solo_min; Explore confirmed all three MIDI layers live ONLY in
the original app (`app/bridge.py:610-756`, `ui/solo_choir_ui_v10.html:197-199`, original
engine) — the 2026-07-05 solo_min rebuild didn't port it. STATE.md had wrongly credited
solo_min. Fixed: Task-4 by-ear acceptance runs on `python app/shell.py`; porting MIDI to
solo_min is a separate, undecided task.

## Next
- MIDI Task-4 by-ear acceptance on the ORIGINAL app; then decide on the solo_min port.
- Priority 1 unchanged: baseline comparison layer + listening test (viva keystone, F10).

## Session 3 addendum — exports/ + LAN hardening (`0a6d5494`)
- Export mix/MIDI now land in `exports/` (git-ignored); Finder reveals the copy there, never
  recordings/. Found & fixed a real leak: the take-home LAN server (SimpleHTTPRequestHandler
  over recordings/) served a full directory index at "/" — 789 working files browsable by any
  QR visitor. `list_directory` now 404s; verified headless: listing 404 / named file 200 /
  traversal 404. Studio restarted on the new code.
- Old recordings (1.1 GB): decision = keep as archive (thesis process evidence); no functional
  need to purge now that the audience can't reach the folder.

## Session 3 addendum 2 — full security sweep (3 parallel read-only scans)
- LAN surface: only 0.0.0.0:8899 (take-home; GET/HEAD, listing 404, no symlinks in
  recordings/); all three UI static servers loopback-only; zero outbound/telemetry anywhere.
- js_api surface: all subprocess list-form (no shell=True); recordings methods all fenced;
  ONE defect — solo_min set_model missing the resolve+containment fence bridge.py has.
  Fixed + fence-tested (legit pass / absolute + ../ rejected).
- Public-repo surface: no license-restricted files tracked or ever added in the 255 own
  commits; no secrets; only low-risk /Users/liaoyu-ting paths in 16 doc files (clean before
  publicising the repo, no rush).

## Session 3 addendum 3 — MIDI ported to solo_min (plan Tasks 1-3)
Plan `plans/2026-07-07-midi-port-solo-min.md` executed through Task 3:
- `a0a8ef69` engine: set_control {"midi"} → low-to-low assignment to ON voices (ordered by
  steps role), surplus muted-warm; Regime A proven (headless _cb render, max-abs-diff 0.0
  with no midi control) + 5 assignment asserts.
- `cec75363` bridge: MIDI I/O ported from app/bridge.py; emits engine.set_control (in-process,
  no stdin); SHARED recordings/midi_chords.json; chord survives set_model rebuild; headless
  contract test passed (Mode A/B, black-key silence, close→off).
- UI card (device/mode/held/editor); app relaunched clean.
Task 4 (by-ear with the Launchkey) = Harry, checklist in the plan.

## Session 3 addendum 4 — MIDI removed from solo_min (GRAVEYARD G20)
After the port + a playability pass (`998289af`: 15 ms chord coalesce, CC64 sustain, latch),
Harry's by-ear verdict: chord-hold 跟 solo choir 不搭 — the keyboard takes harmony authority
away from the voice, contradicting the body-as-interface spine. Decision: remove (option c).
All four commits git-reverted (files verified byte-equal to pre-port state vs `14f0dadf`);
G20 registered with the two compatible future shapes (audience two-person interaction /
set-key variant) and the known release-click (unfixed, noted). Also hit + documented the
CoreMIDI hot-plug gotcha: device list snapshots at first MIDI call — plug the keyboard
BEFORE launching (added to DEBUG_PLAYBOOK).
Focus returns to Priority 1: baseline comparison layer + listening test (F10).

## Session 3 addendum 5 — llm-council: DAW plugin / TD connectivity
Five-lens blind council (First-principles ranked 1st, aggregate 1.40). Verdicts:
- DAW plugin (VST/AU): unanimous reject → GRAVEYARD G21 (license/thesis/engineering triple
  kill; stems+MIDI export IS the DAW interface; any future plugin needs own-model engine,
  F9 training is the real prerequisite).
- TD connectivity: yes, minimal form — ONE-WAY OSC telemetry out (flag, ≤half day) in the
  exhibition-prep batch, TD wall patch timeboxed ≤2 days, v10-UI-on-wall fallback; optional
  raw-audio-to-TD via BlackHole as a zero-code extra layer. Never before Priority 1.
Harry's architectural framing confirmed: app stays a pure instrument, visuals externalize.

## Session 3 addendum 6 — llm-council #2: the 7-week allocation (Harry ratified)
Question (de-biased): how should the 7 weeks to viva be allocated; where does app polish sit?
Verdict — unanimous STOP (feature freeze), not reduce. Red-teamer ranked 1st (avg 1.00, all
five votes): named the work pattern (incl. today's MIDI round-trip) as high-certainty
engineering displacing high-friction research; only rules beat that pattern, not ratios.
Landed in STATE: freeze + exception protocol, W1 deliverables (baseline path, protocol,
ethics email 48h, thesis audit Day 1, order Track B parts, book IELTS seat), 08-03 full-draft
hard gate, Track B 2-day timebox triggered by parts arrival, kill list (QR score-roll cut,
learn+re-record post-viva). Chairman note: channel build energy into research-serving code
(Pragmatist's synthesis) rather than fighting it.

## Session 3 addendum 7 — Track B parts arrived: exciter drive test (timebox running)
Parts landed → timebox triggered. Enablers built same-day (test-serving, freeze-compatible):
device selector in solo_min (`4b800a2c`, throat in/out working after the hot-plug-restart
gotcha — playbook updated `5f17b1ab`) and master output gain 0..+18 dB (`3c287180`, Regime A
0.0 at unity, +6 dB measured ×2.000, clip-guarded).
**Measured (FINDINGS F8): +10 dB = working point (speech + singing fine); +15 dB = thermal
limit (DAEX9 heats + slight distortion — coil power, not digital clip). Cavity under test
1.5×5.8×11 cm. Conclusion: ~5 dB headroom; loudness beyond that must come from cavity v2
acoustics (+3–5 dB target), not gain.**

## Session 4 — Track B bench review: power Route B + island connectors (evening → past midnight)
Terminal died mid-session (solo_shell.py survived as orphan PID 72727; repo was clean/pushed,
nothing lost). Then a photo-based bench review of the full wiring against HARDWARE_ROADMAP/F8
found three structural issues: ① ground loop (hub → UGREEN + Pico carrier, amp tapping VBUS —
the predicted whine/hum topology), ② mic chain = 3 adapters in series, ③ transducer leads
direct-soldered, no strain relief.

**Decisions (HARDWARE_ROADMAP 2026-07-07 entry, commit `3834f070`):**
- Power = Route B pulled forward from "final wearable" to now: LiPo 623360 1500mAh ×2
  (PH 2.0, hot-swap; one cell ≈ 6–9 h vs 8 h exhibition day) → TP4056 Type-C protected
  (on hand) → MT3608 (set 5.0 V UNLOADED first; trimpot needs ~20 CCW turns) → PAM8403.
- Island-boundary connectors: JST-SM 2.5 mm kit (pre-crimped → no crimp tool). Keying:
  L/BCE-1 = 2-pin, R/DAEX = 3-pin → cross-plugging physically impossible.
- Pico + Goldsmiths carrier retire from bench the moment amp is on battery (only live role
  was the 5 V tap); bare Pico W returns post-viva for on-body control.
- Finger PTT: remove (parallel switch — cut Y-branch, heat-shrink stub cores SEPARATELY,
  verify pickup). No spare throat mic / DAEX (Harry's call; discipline: +10 dB working point).
- Belt-pack architecture ratified: head = transducers + throat mic only (two isolated
  islands); card/amp/power/Pico to waist pouch; wear targets: don ≤30 s, ~1–2 N contact,
  ≤+10 dB, 8 h day.

**Parts ordered** (SM58 deselected from basket): LiPo ×2, MT3608 ×2, JST-SM kit — W1
"order all Track B parts" DONE (STATE updated, `6159b134`). Arrivals: MT3608 07-08,
battery + SM kit 07-09. Multimeter + heat-shrink available at uni.

**Bench facts confirmed:** current BCE-1 pair wiring photo-matches F8 series spec (red = +,
inter-transducer jumper, black = −); lead ends already bare/tinned → connectorize WITHOUT
cutting; jumper + lengths untouched until mask form decided. PH pigtail bag = board-type
headers, NOT for wearable joints — reserved for battery hot-swap receptacle (pitch-check
against battery plug on arrival).

**Visual reference:** claude.ai/code artifact `a9969f55` — A bench-current (3 issues),
B belt-pack target, C power chain, D connector zoom, E plain-language 6-step wiring guide.

**Next:** assemble power chain per diagram C (verify PH polarity → 5.0 V unloaded → amp →
pull carrier board → record no-input noise floor), connectorize per E (DAEX first, then
BCE-1), PTT removal needs no parts. Open: mask body = frame_A PLA vs wire+fibre (§8) —
decides where the saved head-weight budget goes.
