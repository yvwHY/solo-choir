# Worklog — 2026-06-18

Branch: `ui-app`. Theme of the day: **set up the Render-SATB pipeline** — design →
plan → first engine task — toward offline full-SATB rendering of a recorded take.

## Commits today (4)
- `f4c6832d` chore(skills): install grill-me, ponytail, adapted brainstorm/write-plan
- `b3acfbf7` docs(spec): Render SATB on a take — offline multi-part design
- `7a3169a6` docs(plan): add render-SATB design + 4-task plan; point CLAUDE.md at it
- `5bc427b8` feat(engine): `--no-you` flag for harmony-only render (per-part SATB stems)

## What happened

### Tooling
Installed the working-process skills (`/grill-me`, `/brainstorm`, `/write-plan`,
`ponytail` / `ponytail-review`).

### Design + plan (Render SATB)
- Spec written via `/brainstorm` → `docs/specs/2026-06-18-render-satb-design.md`.
  Goal: an in-app **Render SATB** action that re-renders a take's raw "You" melody
  through a multi-part SATB pass **offline**, writing a mono mix + **per-part stems**
  to `recordings/`. Locked scope: per-part stems are the deliverable (DAW mix + the
  S/A swap seam for the future female model); 3-state async status
  (`Rendering…` → `SATB ✓` → `Render failed`); `SATB_VOICES` as the single source of
  truth, default You+Tenor+Bass on, S/A wired-but-off; render source = the L channel
  (melody) of the existing `take_*_stems.wav`.
- Plan written via `/write-plan` → `docs/plans/2026-06-18-render-satb.md`:
  4 tasks (engine flag → bridge orchestrator → UI button+status → docs), each gated
  on verify + commit. CLAUDE.md "Next steps" repointed to execute this plan.

### Task 1 — engine `--no-you` flag (this session, committed `5bc427b8`)
The only engine edit in the whole plan. Additive, 3 insertions / 1 deletion in
`server/beatrice_solo_choir_live.py`:
- new `--no-you` argparse flag (store_true);
- `_ctl` init `"you"` default changed `True` → `(not args.no_you)`. The callback
  already reads `_ctl.get("you", True)`, so no callback/DSP math was touched.

Verification (HARD RULE 2, by byte-diff):
- Reference captured on HEAD *before* editing: rendered
  `recordings/take_20260617_121616_stems.wav` → `/tmp/ref_before.wav`
  (3.4s, voices=1, RTF ~0.13).
- **Default `--render` after the edit: max-abs-diff 0.0** vs the prior build →
  byte-identical, non-regression proven.
- `--no-you` render produces harmony-only as intended: peak 0.368 → 0.178, the raw
  You passthrough removed (`/tmp/ref_noyou.wav`).
- `ponytail-review`: lean, nothing to cut.

## State at end of day
- Render-SATB **Task 1 done + committed**; working tree clean.
- Plan Tasks 2–4 not started: bridge `render_satb` orchestrator (`app/bridge.py`),
  UI button + 3-state status (`ui/solo_choir_ui_v10.html`), docs.

## Next
Execute **Task 2** — `app/bridge.py` `render_satb(stems_path)`: async worker that
solo-renders each active voice `--no-you` (per-part stems) + one full `--render`
(mix), streaming `window.onRenderStatus`. Verify in isolation against an existing
`recordings/take_*_stems.wav` before the UI wiring (Task 3).
