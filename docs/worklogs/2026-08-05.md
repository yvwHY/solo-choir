# 2026-08-05

## §A 合成器單一變因裸測（接 08-04 §O 判決 2；autotune 案）

**背景**：六版（A/B/B2/C/D/E）全數未過耳，已排除模型層／f0 曲線層／逐幀
複製層。但回頭看：A–E **全都走 `dm.target_f0` 骨架**（porta 33ms／leap_snap
／量化音符）再往上疊修正——「合成器本身」從來沒被單獨聽過。剩餘嫌疑
（CombSub 起音形狀／幀級平滑／`_vol_mask` 硬門檻／porta 骨架）裡，骨架和
合成器一直是綁在一起出庭的。

**裸測＝把骨架整條拔掉**（`harmony/bare_test.py`，新檔、零改動現有檔＝
live/respond2 行為不變）：他的 harvest f0 原封不動（只乘固定倍率）直接進
CombSub；AGC／voicing gate／1200Hz 哨兵／vol mask／÷agc_g／uv 靜音全部與
現行配方（phrase_render＝A 版）逐項相同＝**唯一變因是 f0 來源**。素材同
prosody_ab 兩句（同斷句邏輯＝同段，可直接對聽）。

產物（`out/prosody_ab/`）：
- `seg{0,1}_bare_harry_ident.wav` — 他自己的模型 ×1.0＝同聲者同音域，最純
- `seg{0,1}_bare_bass1_oct.wav` — Bass-1 ×0.5（八度下）
- `seg{0,1}_bare_girl_up.wav` — girl ×2.0（八度上）
- `seg{0,1}_bare_mix.wav` — 乾＋girl(+8ve)＋bass1(−8ve)，混音比同 A
  （平行八度＝不需量化也成立的合唱）
- `seg{0,1}_dry.wav` — 原聲參照（同段、同正規化）

儀器驗證（曲線真的進去了）：seg0 幀間 f0 活動 dry 13.2c｜harry_ident
12.3c｜bass1_oct 11.0c｜girl_up 10.5c（對照 A 版 4.4c）；seg1 dry 9.6c｜
ident 4.7c（seg1 62% uv、唱得直，本來就沒多少料）。

**判讀表（原始版）**：裸測仍 autotune → 合成器裡；像人 → 骨架/量化。

**§A 判決（Harry）：dry 是好的；harry_ident 仍有 autotune 感，且跟 A–E
一樣「不穩、像亂掉」＝裸測未過。** f0 曲線層正式全滅——連原封不動的
harvest f0 都救不了。

## §B 正典 identity 對照（main.py 整套馬具換掉；亂掉的罪再切一半）

檢討 §A 的縫隙：裸測只換了 f0 來源，**dm 的馬具還全套揹著**（harvest f0
＋voicing 硬 gate＋1200Hz 哨兵＋input_agc＋vol mask＋uv 靜音）。而「離線
正典品質對」（phrase_render 檔頭）指的是 main.py 子行程——再查
`vs_male3_trial.py`：它的 "identity" 也是 PhraseRenderer 骨架渲染＝
**純 main.py 正典 identity 從來沒在他的 live 素材上被聽過**。Bass-1／male3
過耳的音色判決全都是在 dm 馬具上做的。

新對照（`seg*_canon_*.wav`，main.py 直跑、模型與素材同 §A）：
- f0＝parselmouth（rmvpe 權重不在）、`uv_interp=True`＝換氣段 f0 內插、
  無哨兵無硬 gate、無 input_agc、main.py 自家 -60dB threshold；
  `_enh` 版＝再加 NSF-HiFiGAN enhancer。
- `seg{0,1}_canon_harry_ident[_enh].wav`（k0）＋
  `seg{0,1}_canon_bass1_oct[_enh].wav`（k−12）＝2 模型×2 enhancer×2 句。
- 與 §A 裸測的差＝**整套馬具**（f0 器＋uv 處理＋AGC＋mask）；模型/units/
  素材不變＝亂掉的罪被夾在「馬具」與「模型/units/素材域」二選一。

**判讀表（作廢，見 §C）**：canon 也亂＝模型/素材域；canon 乾淨＝dm 馬具。

## §C 素材翻案（Harry 抓到）＝A–E／裸測／正典全部作廢，乾淨素材重審

**Harry：「你用來 render 的 dry 裡面本來就是三個人的聲音，不會影響嗎？」**
——對，而且比想的嚴重。

**事實鏈**：①respond2 的 session dump＝「他被聽見的＋播出去的同軌」
（respond2.py:850；cb 內 `sess = indata×(0 if muted else 1) + outdata.sum`）
＝獨唱句與回應段（他的重播＋兩天使）**時間交錯、同一條 mono 軌**。播回應
時 mic 歸零（含 mute_tail）＝句內疊加構造上不可能——但整條軌是三種內容
交錯。②舊選段 `[0::2]` 想取「他的句子」，奇偶對位被前置過濾（<12s 跳過、
<30 voiced、>340Hz）打歪。③互相關鑑定（回應段必含上一句的**數位重播**
＝xcorr 峰值高）：全 dump 33 句，回應段 xcorr 0.56–0.95、獨唱句 ≤0.30，
**prosody_ab／vs_male3_trial／裸測選中的 seg0(103.7s)/seg1(123.7s)
xcorr＝0.563/0.653＝都是回應段**。真獨唱句＝95.9–101.9s／117.2–122.0s。

**汙染範圍（全部作廢）**：A–E 六版及其判決、裸測 v1、正典 v1、male3 試訓
耳測樣本（同 dump 同選段）、以及立案時的量測——「dry 13.2c vs A 4.4c」
的 13.2c 是 harvest 在三聲部間跳的假象；乾淨獨唱句實測 **dry 2.1c、
A_lower 2.7c＝量化差距蒸發**。診斷的數字地基與六版排除鏈全數重來。
**待複核（不自動作廢）**：Bass-1「音色像人」與 male3 判決（音色判斷對
素材汙染較不敏感，但樣本同源）；G33 模型層閉環（訓練側、另一素材，應
不受影響）；live 終判（live 裁決、非這批渲染，仍有效）。

**重審＝`bare_test.py` v2**（commit 本次）：選段改 xcorr 分類器
（`his_phrases`：xcorr≤0.35 且下一段 xcorr≥0.5＝被回應過的真獨唱句），
在乾淨兩句上重跑**全階梯**，產物 `out/clean_ab/`（22 檔）：
- `seg{0,1}_dry.wav` — 真獨唱參照
- `seg{0,1}_canon_harry_ident[_enh].wav`／`canon_bass1_oct[_enh]` — 正典
  上限（main.py：parselmouth、uv 內插、無哨兵無 AGC；±enhancer）
- `seg{0,1}_bare_harry_ident／bass1_oct／girl_up／bare_mix` — dm 馬具
  ＋他的 f0 直進（零骨架）
- `seg{0,1}_A_mix／A_bass1_lower` — 現行配方基線（骨架量化＝原始被告）

**判讀階梯（待 Harry 裁）**：dry → canon → bare → A，autotune／亂掉在
哪一階出現，罪就在那一階新增的東西上：canon 就亂＝模型/素材域；bare 才
亂＝dm 馬具；A 才亂＝骨架/量化（原始理論復活，B–E 的修法值得在乾淨素材
上重試）；全都不亂＝舊判決全是素材假象、案子可能直接結。
舊 `out/prosody_ab/` 保留當案底；prosody_ab.py／vs_male3_trial.py 頂部
已加作廢警告。

## §D 實體鍵接線收尾（K669B 焊接完成；review bug #1 修掉）（原標 §C，與素材翻案撞號改 §D）

Harry 焊好按鈕、Pico 接上 Mac（`/dev/cu.usbmodem21101`）。本節＝把
「respond2 接 tap」從待辦收成能用。

- **Pico 韌體不用重寫**：抓下板上 `main.py` 對比＝與 repo
  `firmware/pico_button/main_usb.py` byte 相同（08-04 已寫入過）。
  備份留 scratchpad。reset 後 serial 監聽 7s：HB 每 2s 準時、seq 0
  ＝**無假觸發＝焊點沒短路**。
- **review bug #1 修掉**（commit `93e3d4ac`）：`respond2.py:596`
  UDP `TapListener` → `SerialTapListener()`；err 分支不再摸
  `tap.port`（Serial 版無此屬性）；`--no-tap` help 同步改 serial。
  介面盤點：respond2 只用 `alive`/`take()`/`err`＝Serial 版全有。
- **驗證**：py_compile 過；vcclient-dev 實跑 `SerialTapListener()`
  → err None、自動抓到 usbmodem、alive True、無 pending tap。
- **按壓導通測試＝過**：Harry 按壓，5s 監聽窗即時收到 TAP 7/TAP 8、
  序號 0→8 累積、一按一序號（去彈跳正常）＝焊點導通。實體鍵硬體側
  全綠。**剩**：貼麥＋respond2 live 全鏈（聽的時候按＝立刻收句）。

## §E gap-tap 預設 2s→6s（Harry 定；接 §D）

白話走查時 Harry 抓到縫隙：安全網 gap 2s 會搶切他刻意的樂句間休止
（>2s 的休止＝被誤收句，只能事後按鍵中止、句子已腰斬）。裁定：**句子
由按鈕決定＝安全網只防忘按，放寬沒有代價** → `--gap-tap` 預設 2.0→6.0
（`respond2.py:888`）。順手修掉 pre-existing bug：`:929` `--expr` help
的裸 `%`（61.6%）讓 argparse `--help` 直接 ValueError——改 `%%`，
`--help` 復活（實跑驗證，gap-tap 條目正確顯示）。

## §F autotune 案破口：病在音色層＝CombSub 裸輸出，enhancer 是解（接 §C 重審）

**Harry 對 clean_ab 的裁決**：「順了」（亂掉＝素材假象，§C 成立）＋
「bass 還是很重的 autotune」＋**「enh 檔其實還好，我說的是非 enh 的
bass wav」**。

**儀器交叉**：canon/bare 的 bass f0 曲線＝他的曲線 −12（|dev| p50
1.4–2.4c、r 0.95–0.99、跳變率與 dry 同）＝**音高曲線忠實、零量化**，
enh 與非 enh 的 f0 幾乎相同——兩者唯一差別是 NSF-HiFiGAN enhancer 有沒有
把 CombSub 裸輸出重整。**結論：autotune 感不在 f0、不在骨架/量化，在
低音域 CombSub 裸輸出的死平諧波／蜂鳴質地（vocoder 感）**。這也解釋
先前一切 f0 修法（A–E、裸測）為何全部無效——修錯層了。

**修法落地（本 commit）**：
- `phrase_render.py`：`enhance=False` 守衛參數——True＝mask 後過
  NSF-HiFiGAN（對齊 main.py:259-267，在 ÷agc_g 前）；False＝逐 byte
  舊路徑。**檔案級迴歸證明**：改後重跑 bare_test，A 檔與舊檔
  max|diff| 0.0000、corr 1.0000（同序整跑連 MPS 都可重現）。
- `bare_test.py`：音符線落檔 `seg{i}_notes.json`。**血訓**：EarV3 隨機性
  對呼叫序敏感——重現腳本少建兩個 renderer 就岔線（同音幀剩 11–37%）；
  單一變因對照必須鎖線，不能靠「同 seed 同順序」默契。
- `a_enh.py`（新）：A 版配方＋enhancer、吃鎖定音符線。驗證：Aenh vs A
  同音幀 98.6%＝純 enhancer 單一變因。**RTF 0.18（兩聲部含 enhancer）**
  ＝respond2 預算內。

**待 Harry 裁**：`out/clean_ab/seg{0,1}_Aenh_mix.wav`／
`seg{0,1}_Aenh_bass1_lower.wav` 對 `seg*_A_*`（同線、同配方、只差
enhancer）。過了＝respond2/live_v3 開 `enhance=True` 收案；不過＝
enhancer 治不了配方層的部分，查骨架×enhancer 交互。

## §G Aenh 終判過＝autotune 案收案；配方接線＋Soprano-3 開訓（SATB 補 S）

**Harry 終判：Aenh 過。** autotune 案收案：病根＝低音域 CombSub 裸輸出
（§F），解＝NSF-HiFiGAN enhancer。累計判決鏈：素材翻案（§C）→ 乾淨階梯
（順了）→ enh/非 enh 對照（bass 非 enh 重）→ Aenh 單一變因（過）。

**接線（本 commit）**：respond2 正式配方＝
- `VOICES.lower` → **Bass-1**（`combsub-m4-bass1/model_11000.pt`、seed
  20260805＝Harry 裁過的那顆），舊 lower（他自己的模型）註解保留退路。
- `rend` 兩聲部 `enhance=True`（phrase_render §F 守衛參數）。
- smoke：兩渲染器＋enhancer 冷啟載入 OK、3s 素材兩聲部 2.04s（含
  harvest；真唱素材實測 RTF 0.18）。**live 尚未實跑**——下次 respond2
  live 場即驗（預算內，風險低）。
- Aenh 過＝Bass-1 音色複核也一併過（§C 待複核格收掉：乾淨素材＋enhancer
  下 Harry 直接裁過）。

**SATB 現況（Harry 問「有 ATB 了是嗎？那 S 呢？」）**：對——
T＝他本人（dry）、A＝girl 上聲部、B＝Bass-1 下聲部（今日入配方）。
S＝缺聲部模型 → **Soprano-3 開訓**（M4Singer 盤點：S1 52.6min/p50 70、
S2 37.3min/p50 70、**S3 97.9min/p50 72＝料最多、音域最高**，正好在
girl(~66) 之上）。同 Bass-1 管線：60min 子集（718 clips 跨 38 首、
round-robin、濾 <2.2s）→ `data_m4_sop3`（train 710/val 8）＋
`f0_stats.json`（Hz p10/50/90＝392/523/659）→ preprocess →
`run_train_sop3.sh` 訓至 14k（背景跑中）→ 取 val 谷底 checkpoint →
耳測樣本待 Harry 裁。S 的接線（respond2 第三天使線怎麼編）＝訓完、
音色過耳之後的另一步（和聲編排歸 Harry 裁）。

**§G 續＝Soprano-3 訓成、耳測樣本備妥**：14k 訓完（28 分鐘、MPS），
全域 val 谷底 **10k＝1.092**（14k 1.097 未再低）→ 取 `model_10000.pt`。
耳測樣本（`out/clean_ab/`，main.py＋enhancer＝production 同款）：
- `seg{0,1}_sop3_up.wav` — k+12＝S 本域展示（他的中位 54+12=66，
  S3 p10=67＝下緣、可用）
- `seg{0,1}_sop3_ident.wav` — k0 對照（⚠ 他的線遠低於 S3 域＝出域
  參考格，僅看劣化型態）
- `seg{0,1}_sop3_mix_preview.wav` — 乾＋S(+8ve)＋Bass-1(−8ve) 粗配
  （位準目測、只供方向感；正式混音等 S 過耳後照 A/B 流程）
**待 Harry 裁**：S3 音色像不像人；過了→第三天使線編排（S/A 分工）
另立步、歸 Harry。

## §H S 入應答式 live（四聲部＋實體鍵可測；Harry 點播）

Harry：「先做進應答式 live，我想測試四個聲道以及按鈕。」接線（守衛
`--sop`，**預設 1**；`--sop 0`＝雙天使終判配方逐 byte 不動——kw/渲染迴圈
改迭代 rend、混音 sop 項尾端條件加）：
- `SOP`＝combsub-m4-sop3/model_10000.pt（val 谷底）、seed 20260806、
  VIB["sop"]=(4.9, 0.75)（三天使顫音去同步）。
- **S 線＝他的 mic 音高 +12**（旋律在頂＝合唱慣例；lead 腦空間 −off 換回
  mic 空間＝§B 血訓。S3 p10=67、他中位 54+12=66＝域下緣，過低的句子可能
  露餡——先聽再說）。polish 不動 S（純八度平行＝構造上不撞）。tail 由
  lead 的 last 帶著走。
- 混音：`--sop-db` 預設 0（相對 --angel-gain；upper +1、lower 0）。
- `--out-map` 收 3–4 個聲道（第 4＝sop；只給 3 個＝sop 跟 upper 同道）。
- respond_shell 不用改（--sop 預設開）；實體鍵（§D SerialTapListener）
  照舊。
- 驗證：py_compile＋--help 過；file 模式端到端 4 聲部 OK（6s 句渲染
  p50 1.18s＝RTF ~0.20、3 嘴含 enhancer）；--sop 0 正常。
**待 live 實跑**：四聲部位準（gs 預設 0dB 未耳測）、S3 低句出域劣化、
按鈕全鏈（聽時按＝立即收句）。

## §I 燈條翻案＋韌體合併（unit 燈不亮的真因；Harry 抓到）

**Harry：「你是忘記我的配置了嗎？還是燒錄按鈕複寫掉了？」——對，蓋掉了**：
08-04 按鈕韌體寫入 `main.py` 時蓋掉火流 v5.1（pico_led/main.py）＝unit
燈條從那刻起全黑；疊加稍早燈條電源未接＝直灌 NeoPixel 也不亮。共電後
GP1 紅／GP2 藍直灌測試**兩條都亮**＝硬體全通。

**修法＝合併韌體** `firmware/pico_led_button/main.py`：火流 v5.1 逐行照抄
＋按鈕/心跳插進主迴圈（腳位不衝突：燈 GP1/2＋ADC26/27、鍵 GP15；
去彈跳改**非阻塞**——原版 sleep_ms(200) 會凍火流）。mpremote 燒回
（板上舊按鈕韌體備份在 session scratchpad），實測 HB 每 2s 準＝迴圈
節奏沒垮。待 Harry：按壓 TAP＋unit 出聲看火流＋app 全鏈。
教訓：**一板多韌體要有登記**——main.py 只有一份，燒錄前先看板上是誰。

**§I 驗收（Harry）：燈有亮、按鈕在 app 也有運作**＝合併韌體收案——
一顆 Pico 同跑火流＋實體鍵，應答式 live 全鏈（唱→回應→燈燒→按鍵收句）通。

**§G 判決（Harry）：`seg*_sop3_up.wav` 表現良好＝Soprano-3 音色過耳、
S 聲部定案**（live 接線用的正是 +12 這個配置）；`sop3_ident` 確實怪＝
出域劣化符合預期、不扣分。SATB 四聲部素材正式到齊：S=Soprano-3(10k)／
A=girl／T=他本人／B=Bass-1(11k)，全過 enhancer。剩 live 聽感格：四聲部
位準（--sop-db）、S 低句出域、和聲編排（S/A 分工）歸 Harry。

## §J Alto 替換候選：Alto-5 訓成＋girl A/B 樣本（Harry 問「alto 可替換嗎」）

七位 M4Singer Alto 盤點：**Alto-5 勝**（158.1min 全集最多、音域 57–69
正罩 A 聲部）。同管線：60min 子集（748 clips 跨 77 首）→ 14k（val 谷底
**14k＝0.985**，尾端仍在降＝料多沒吃飽，先取現有谷底）。A/B 樣本
（`out/clean_ab/`，鎖定同一條 A 和聲線＋同 Bass-1 lower＋enhancer，
唯一變因＝upper 模型）：
- `seg{0,1}_Agirl_upper/mix.wav` — 現行 girl
- `seg{0,1}_Aalt_upper/mix.wav` — Alto-5(14k)
**待 Harry 裁**：過了＝respond2 `VOICES["upper"]` 換裝一行；不過＝
girl 續任、Alto-5 留備胎。

**§J 判決（Harry）：「比較起來兩個都滿好的，我覺得都可以勝任 alto」＝平手。**
處置：girl 續任（終判配方不無故動），**Alto-5(14k)＝認證備胎**（音色已
過耳、換裝＝VOICES["upper"] 一行）。備選論點留檔：全 M4Singer 陣容
（S3/A5/B1）的「同團感」假說未測；Alto-5 val 尾端仍在降＝要用前可續訓
30k。SATB 聲部庫現況：S=Soprano-3(10k)✓、A=girl✓（備 Alto-5 14k✓）、
T=他本人、B=Bass-1(11k)✓——四聲部全數過耳、各有退路。

## §K 即時線：batch 長音觸發 live 陣亡 → 滑窗串流 spike（Harry 拍板）

sustain_live v0（G28 案②）live 三刀陣亡：不及時（體感 ~1s；架構地板
0.4–0.5s＝致命）、不穩（回授螺旋 73 次自進場）、不消失（回授令 f0 永遠
有聲）。已葬 G33b（回授/不穩可修、延遲地板不可修；重開條件＝「晚半秒
進場」改為美學設計時）。**血訓：即時體感的東西不做 demo、只上 live
（Harry 本日親口立規）；回授是同時唱奏系統的第一殺手，設計期就要給
own-voice 隔離方案。**

**下一注（Harry：「先記錄 然後跑 spike」）＝G28 案③ DDSP-SVC 滑窗
串流**：mic 連續滑窗轉換（gui.py 為 RT VC 而生的架構）、無觸發概念。
Spike＝最小迴圈量兩個數字：端到端延遲、串流質感（G28 死味測試）。
單聲部 identity 先行，過了才蓋和聲層；不過＝即時線收案、應答式即本體。

## §L 滑窗串流 spike v1–v8：核心活了、和聲斷續未破（今晚收兵）

**戰果**：headless 滑窗迴圈（抄 gui.py 核心、MPS 化）——**單聲部 identity
Harry 裁「跳針沒了」＋「延遲可以」（~0.3–0.45s）＝G28 死味測試過、
串流核心成立**。這是 G28 案③第一次被實證可行。

**和聲模式（S+12/B−12）斷續未解**，八版診斷鏈全記錄：
- v1 跳針 → v2 **units 凍結**（hubert 全窗自注意力＝滑窗重寫 units
  p50 0.38＝根因一；凍結後 solo 過耳）
- v3 和聲上線 → 跳針回來 → v4 每聲部 SOLA（混音 SOLA 對不齊兩週期）
- v5 f0 凍結＋拔 SOLA → v6 **initial_phase 絕對相位**（forward 原生支援、
  gui.py 沒用；量測：enhancer 是重新合成＝相位每窗重洗＝白接）
- v7 儀器鐵證：全凍結後**相鄰窗重疊 corr 對齊後 0.959**＝內容一致、
  縫只剩 ±10ms 位移＝SOLA 本職 → SOLA 回歸
- v8 門檻 -45→-60（斷續≠跳針的重定性）——**仍斷**
**主嫌（未證）**：uv 天性——串流逐幀跟隨＝他的子音/換氣打洞在天使線上；
solo identity 洞跟他自己對齊＝不顯，移調天使獨立暴露＝顯。與 08-04
「stems 斷斷續續＝uv 設計性靜音」同源。**解法方向＝sustain 橋接**
（E 版 units 凍結/母音撐過短洞技術移植進串流）＝下場的正經工程。
**下場開場**：先做斷點定位聽測（母音中間也斷＝技術缺陷繼續修；只斷在
子音/換氣＝天性、上橋接）。工程資產：全特徵凍結網格、每聲部 SOLA、
絕對相位、儀器組（重疊 corr／units 距離／f0 偏差）全在 spike_stream.py。

**§L 終判（Harry）：v9「乾淨了 沒有斷斷續續」＝即時和聲串流成立。**
真兇＝架構蟲（v1 起就在）：110ms MPS 推論跑在 CoreAudio callback 裡
＝死線抖動掉塊；v9 callback 只搬記憶體、worker 執行緒推論後收案。
血訓兩條：①**音訊 glitch 先查執行緒架構、再查內容層**——v2–v8 七版
接縫手術全在追內容，真兇一直是掉塊（接縫工程不白做：儀器證明凍結後
corr 0.96＝內容連續性是真的，但可聞症狀是 dropout）；②**遙測要誠實**
——status 旗標被忽略＝「超時 0」假安心，v9 起 缺塊/旗標 入帳。
現況：S(+8ve)+B(−8ve) 跟唱、enhancer 全開、block 0.2s（延遲 ~0.45s
已過耳）、infer 110ms/197ms。**下一步**（下場）：block 往回走探延遲
地板（0.15/0.10）→ 第三聲部＋diatonic 腦（編排歸 Harry）→ 回授長測
→ 收進 app。

## §M 串流進化 v10–v14：diatonic → 天使腦；上下文機制路線圖（Harry：可能都做）

**v10** diatonic 腦（--key 大調映射、S+7步/中間聲部他的模型−2步/B−7步）
→ Harry：長音不穩。**v11** 長音包（遲滯音符 0.65半音/35ms、f0 到幀即凍、
音程進快取＝相位帳精確）→ 仍不穩：「唱嗚但輸出不會一直是嗚」＝元音漂。
**v12** units EMA 釘住（streak≥70ms→α0.12；換音α1）＝元音鎖住。
**v13** 天使腦接入：EarV3 本來就是 tick 機器＝ear.tick(chunk) 直接吃
音訊——girl=上聲部線/bass1=下聲部線/S=+8ve、KeyTracker 自動追調、
--no-brain 退回。代價：ear 自己的 pitch 偵測 +55ms 拖爆 197ms 死線
（拆執行緒也沒用＝GIL/MPS 搶）。**v14 音符直饋**：tick() 的成本全在
tracker.push(chunk)、下游只吃一個 MIDI——凍結 f0 現成就有 → 割掉 pitch
重算、keylock 外部化（KeyTracker 維護 k_shift）→ block 回 0.20＝延遲
~0.45s。**Harry 判：「和聲線有出來」**＝腦收案；延遲收回原級。
下刀備妥：三聲部 enhancer 合批（−30~40ms）→ block 0.17。

**上下文機制路線圖（Harry：「先記錄 可能都做」）**——
歌唱層：①lookahead 100–200ms＝hubert 雙向上下文（母音/咬字準；延遲
旋鈕）②**顫音開花**＝NoteTracker streak 驅動 vib_onset（respond2 哲學、
最便宜）③**樂句橋接＝天使不跟子音斷**（E 版 sustain live 化；「影子→
合唱團」分水嶺）④動態慣性（音量跟隨不逐幀貼）。
和音層：⑤終止式意識（收句時 V→I 解決）⑥anticipate（EarV3 內建 ant；
⚠ G29 有 JSB 版墓碑＝重開先量命中率）⑦**排練譜模式＝論文級**：EarV3
`rehearse` score-following 已存在，接進串流＝Bring Him Home 級曲目的
正譜 SATB＋即時嘴＝「反應式腦吃虧」的曲子全解鎖（連到選歌那格）。
優先序建議：③與⑦論文級、②最便宜、⑥風險最高。

**§M 續＝即時模式入 app**（`0755a3fe`，Harry 點播）：respond app 加
mode 下拉（應答/即時）——即時＝spike_stream 子行程（v14 全配置：腦直饋
＋長音三件套＋enhancer、block 0.20），gain/mic/out 沿用 UI 欄位；串流
遙測入 UI（infer→LAST WAIT 格、缺塊/旗標→IO 格、缺塊>10 自動警告）。
standalone CLI 照舊可用。第五個 app 形態不用開：一殼兩模式。

**§M 終判（Harry）：「可以」＝即時模式 app 全鏈過。08-05 收工。**

## §N K669B 夾環殼試印迭代 v1→v3（另一 session，Fusion＋切片；不動 repo 程式）

v1（08-04 §O 出圖）實印三病，Harry 回報：①按鈕窗 6.4 偏小（勉強塞）
②按鈕按壓向後退（口袋 7mm vs 鈕體 ~3.5mm＝無背擋）③C 夾 48.4 偏鬆。
v2（預設公差修）＝內徑 48.0／窗 6.7／口袋 3.8（背擋）→ 再敗：口袋太淺
（按鈕含焊點放不進，Harry 裁「不要墊，我自己塞東西」）、48.0 仍鬆。
**破案＝卡尺實測筒徑 47.0——規格 48 差 1mm＝前兩輪鬆的真因**（README
早自標「48 是 FIFINE pop-filter 配件規格非實測」，試環也照紀律印了，
但兩輪都拿預設值配公差＝兩次白印）。**血訓：配合公差先上卡尺，不猜。**

**v3（定版候選）**：內徑 46.7（實測 −0.3 干涉）／壁 2.0／開口 135°／
口袋回 7.0（背擋取消、Harry 自填靠山）／窗維持 6.7／boss 隨環內移
（外凸量恆 10.0）。組裝回 v1 路徑（底窗側進翻正）。

工程紀錄：Fusion 尾鏈兩度打掉線性重建（v1 鏈 in-place patch 疊兩層後
cut 參照自相矛盾＝止損；重建反而每刀體積帳可精確對解析值：環 1912.4、
口袋 314.3＝6.7²×7、底窗 52.8＝96−43.2 重疊）；v2 14 探點、v3 11 探點
全 PASS。v1 的 3.3mm 線槽未保留（幾乎整條被底窗覆蓋、無實體功能）。

產物（repo 外 `260804_prints/`，README 已更 v3 置頂、v1/v2 標作廢）：
`gcode/k669_testring_v3_PLA_brim5.gcode`（4m35s/0.98g）＋
`gcode/k669_shell_v3_PLA_brim5.gcode`（14m47s/3.88g），brim 5 內建。
**NEXT＝HatchLabs 先印試環驗 46.7 咬合（太緊白化即回報，放 −0.1 重出）
→ 過再印殼 → 按鈕裝入＋自填背擋 → 貼麥＝實體鍵最後一塊實體。**

## §O 串流 v15＝樂句橋接（§M 路線圖③；E 版 sustain live 化）

08-04 §O 的 E 版三件套（vm 補 1／units 凍母音／音量插值）因果化進
spike_stream：離線是掃全句再補洞，live 看不到洞尾＝改逐幀狀態機——
短洞（≤`--sustain` ms）gate 撐 1、音量 hold ×0.985/幀（≈−11dB/s 呼吸
衰減，近似離線的兩端插值）、units EMA α=0 凍母音；真休止＝撐滿後
`--release`（預設 120ms）餘弦淡出收尾＝「收句被 gate 剁」一併解。
f0 洞內連續性免費（prep uv_interp 已插值、凍在 fc）。狀態全走凍結
快取（st 的 gt/vc/br，同 fc 哲學）；gate 與原 mask 取 max＝原 9 幀
膨脹繼續管開口。守衛：`--sustain` 預設 0＝Regime A by construction
（新碼全在 `if sus_f:` 後）。review #7 的 blip bug（pc=0 階梯）在
live 路徑構造上不存在（無 prosody_cents）＝E 未過耳的污染源沒跟來。
驗證：bridge_feed 原始碼抽出實跑 5/5——短洞全橋／收尾 ramp 嚴格
遞減／3、7、16 幀分塊餵＝＝整段餵逐 byte（跨窗因果一致）／開場靜音
不誤橋。
**NEXT＝Regime B Harry live 裁（`--sustain 250`）**，聽三件：①唱含
子音的句＝天使連續不斷 ②正常收句＝自然淡出不是剁 ③淡出尾巴拖不拖
（拖＝`--release` 下調）。infer ms／缺塊不該惡化（新增算量 ~90 幀
numpy 量級）。

## §P 橋接 v16–v18＝三輪 live 迭代＋review 三顆 bass 病灶收割（08-06 凌晨）

§O 的 v15 Harry 裁「無感」＋「bass 怪」→ fresh opus review（報告
scratchpad/review_v15.md）實錘：**喉麥底噪 −55~−57dB 高於 −60dB 門檻＝
gate 恆開、橋接判準永不觸發**（20 分鐘 3 假洞 vs voicing 判準 30s 23 洞
＝差 4 個數量級）。v16 改 parselmouth 真 voicing → Harry「亂掉」：滑窗
尾端 uv 不可靠＝216洞/2481幀假洞亂凍。v17 混合判準（uv 且音量<包絡/4）
→ 觸發量正常（44洞/151幀＝子音級）但「音高/母音/音量三個都亂」→
**根因＝橋接把以前靜音的垃圾放大**：洞口幀（窗尾 f0 亂猜＋髒 units）
正是被 hold 的幀。**v18 定版**：
- hold 回捲：f0/units 取洞口前 ~45ms 的 FIFO 安全幀（免費 lookahead）；
- 洞持續只看 uv（v17 坑：長休止包絡衰到底噪→相對判準失效→gate 重開）；
- 橋接 gate **取代**原 mask（review F1：原 mask 在此麥恆開＝取 max 白搭；
  真休止現在真的靜音＝bass「內插假 f0 唱 55–90Hz 低頻嗡」一併堵住）；
- review F6【高】：ear_note_tick 音程參照改「他真唱的音」——舊版用 C
  大調吸附後內部值＝40% tick bass 高一個半音（respond2.py:419 同款血訓）；
- review F7【中高】：bq 的 uv 幀送 0＝腦收得到 REST（舊版 0/484）；
- review F8【中】：換音 ~35ms porta 滑進新音程，不整塊硬跳；
- 順修：brain 模式預熱走 ratios 路徑、輸出 clip、init 例外自癒。
全掛 `--sustain` 守衛＝預設 0 仍舊路徑。狀態機分塊==整段逐 byte 測試過。
**待 Harry 裁 v18**；過耳後 F6/F7 兩顆 bug fix 應解除守衛轉常開。

## §Q 整體判決→dump 量測破案：病在腦線抖動（08-06 00:40）

v18 Harry 整體判決：「沒有和聲感／輸出粗糙／音高亂／收尾亂／有 autotune
／像糟糕的變聲器」＋延遲不滿。停止猜測，spike_stream 加 `--dump`（mic／
angel／逐幀特徵，每 10s 自動落檔＋SIGTERM 也走 dump 路——nohup 背景行程
擋 SIGINT 的坑）。179s take 量測（scratchpad/diag1_*，分析腳本
analyze_diag.py）：
- **主嫌實錘＝腦音程抖動**：唱歌時段換音 ~7 次/s、音程駐留 p50 12ms、
  girl 常駐音程 4/6/7/8/9 全散、bass 常駐 −1（貼臉打架）＝和聲線每 tick
  重擲骰子——六項不滿裡四項（和聲感/音高/收尾/變聲器感）同此根因。
- **洗清**：凍結 fc vs 新鮮 f0 偏差 0.0c、活動量 3.7c/幀健康（autotune
  不在 f0 層）；接縫縫上 p95 < 全局 p99（接縫無罪）；vc/vol 1.00×；
  gate 快速翻轉 1 次（收尾機制本身穩）。
- **v19**（`--sustain` 守衛內）：①音程遲滯——新音程連續兩 tick 一致才
  上線（駐留下限 ~370ms）②k_shift 一鎖不再動（respond2 紀律；串流版原
  每 2s 重估＝一飄全隊平移）。
- 未結案：粗糙（嫌疑收斂到模型移調輸出；下一步拿 diag1_mic.wav 做
  offline canon 對照渲染＝把「串流固有 vs 模型固有」切開）；延遲（路徑
  ＝enhancer 三聲部合批 −30~40ms → block 0.17；lookahead 構想與延遲不滿
  衝突＝降級）；bass −1 若 v19 遲滯後仍在＝腦 feed 層再挖。
