# Worklog 2026-07-24

## §A 三種嘴 × v2 腦（Harry:「可以試試 world 不是 Beatrice 嗎 或是 Beatrice 換一個 model」）

延續 07-23 §T 的 ear-gate 等待,擴充聽測矩陣。腦統一 v2/cpdl 控制變因,同一段真嗓 take:

1. **WORLD 嘴**:`harmony/world_mouth.py`＝harmony_brain `world_replay.py` 的 v2 腦移植（G26/G28 只判死「流式」WORLD;這是 07-22 過 Harry 質感水位的離線 batch 版——天使由他自己的聲音 WORLD 重合成,formants 不動,voice-ownership 完全體）。跑在重訓 venv（pyworld）。
2. **Beatrice 換 model**:`solo_host.py` 加 `--model-dir`（additive,default satb2 不變）;render `260620/result/` 的 **myvoice**（paraphernalia_data_00005000,他自己的 Beatrice 模型）與 **girl**（paraphernalia_data_00002500）。

三 render 全 PASS（309 ticks、active 85%）。產出 `harmony/out/angel_v2_{myvoice,girl,world_cpdl}_mix.wav`——聽測清單從三版（satb2×三口音）擴成**六版矩陣**（嘴×口音）。全部停在 Harry ear gate。

## §B 耳測裁決＋嘴的 2×2 對照設計（討論,未動工——明天做）

**Harry 耳測:WORLD 比較好**（六版矩陣中）。追問「是不是 Beatrice 轉換/測音高有問題」→ 診斷誠實化:現有 WORLD vs Beatrice 對比**混雜三變因**（f0 哲學/音色/後端）,「移調式轉換是主因」目前是假設非結論——確定的事實只有 28c 殘差是算術（§R）。**明天補 2×2 缺的兩格**:
- **Test A**:你的聲音 → WORLD 重合成到目標 f0 → 餵 Beatrice(shift 0)純音色轉換＝「Beatrice 音色×目標式 f0」。Beatrice 原生無絕對 f0 控制,但可控其輸入音訊的 f0——此即「不重寫二進位的 Beatrice 重寫」,兼第四種嘴候選。
- **Test B**:WORLD 重合成但 f0＝你的 f0 平移音程(保留 wobble)＝「你的顆粒×移調式 f0」。
- 盲聽四格 → 主因水落石出,決定天使嘴的終局路線(檔0 離線 WORLD/檔1 流式 target-f0 WORLD 200ms/檔2 DDSP)。
- 關鍵架構洞察(討論確立):**46ms 約束是被誤植的**——DAF 毒性只對 delayed self(G23),天使是「另一個人」,可合法花 150–250ms lookahead;且 v2 腦的預感(top-3 65–74%)能預先武裝下一個音,把嘴的延遲買回來——**腦線與慢嘴互補,不競爭**。嘴的選擇不影響腦側 induction。

## §C 2×2 對照補齊（commit `a28cab3d`——四格產出,停在 Harry 盲聽）

§B 設計落地,全在 `harmony/`（server/ 零改動）:

- **`world_mouth.py` 改造**:單一腦 pass（同一條線,消掉 top-k 無 seed 的線間差異）合成兩版——`angel_v2_world2_target_*`（現配方,乾淨目標 f0＋porta＋合成 vibrato）＋ `angel_v2_world2_shift_*`（**Test B**:你的 f0 依每 tick 音程（腦音−你當下音）平移,wobble/換聲全保留;legato 缺口沿用上個移調量、你沒聲它就沒聲＝移調式轉換的誠實模擬;octave-error 幀代入局部中位數,免得 tracker 閃爍冒充 shift 瑕疵）＋ `_notes.json`（notes/heard/shifts,供其他嘴重用同線）。跑出 309 ticks、active 89%、移調分布 −9…+5 半音（大宗 −3…−8）。
- **`beatrice_mouth.py`（Test A）**:WORLD 目標 f0 angel → Beatrice satb2 spk0、steps 0 純音色轉換（「不重寫二進位的 Beatrice 重寫」,兼第四種嘴候選）。**音高保真驗證:對齊 50ms 引擎延遲後,輸入/輸出每幀 f0 中位差 4 cents**——「Beatrice 音色 × 乾淨音高」這格成立,不是又一個移調路徑。
- **`blind_pack.py`**:四格統一混音配方（0.5 take＋0.8 peak-norm stem、48k、peak 0.9）、洗牌成 stim_1–4＋key.json;兩個 Beatrice stem 各修 50ms 引擎延遲——不修的話「Beatrice 欄拖拍」會是與待測變因無關的欄位混淆（昨天六版矩陣其實帶著這個混淆）。
- **已知弱角**:C4（Beatrice×移調）沿用昨日 `angel_v2_cpdl`,腦線是另一次抽樣;C1/C2/C3 同線。盲聽結果曖昧時,補一個 replay conductor 把 `_notes.json` 的線釘進 solo_host 重出 C4 即可。

**待 Harry:盲聽 `harmony/out/blind_2x2_0724/stim_{1..4}.wav`（聽完才開 key.json）→ 裁定「移調式轉換是不是主因」→ 天使嘴終局路線（檔0 離線 WORLD／檔1 流式 target-f0 WORLD 200ms／檔2 DDSP）。**

## §D 盲聽解盲＝裁決（Harry:「3≥4>2>1」→ FINDINGS F16）

|  | 目標式 f0 | 移調式 f0 |
|---|---|---|
| WORLD 顆粒 | stim_3（第 1） | stim_2（第 3） |
| Beatrice 音色 | stim_4（第 2） | stim_1（第 4） |

- **移調式轉換確認是主因**:兩個音色下都是 target > shift（行效應完整分離）。07-23 §U「整段像機器音」的元凶主要是移調式 f0 路徑,不是 Beatrice 音色本身。
- **音色次要**:同欄 WORLD ≥ Beatrice,但 target 欄兩格 Harry 判「約等於」——**Beatrice 平反一半**:餵乾淨音高就幾乎追平（Test A 鏈,音高保真 4c）。
- **規則入 F16**:天使的嘴一律「按目標放音高」（重合成/釘選）,永不平移他的即時輪廓——他的 wobble 被平移後連在他自己的 WORLD 顆粒裡都是錯的。28c 移調架構下限（07-23 §R）同時是美學天花板,別再為天使打磨移調路徑。
- **終局路線影響**:檔0（離線 WORLD target）＝已驗證;檔1（流式 target-f0,~200ms chunk）現在有兩個出口——(a) 直接出 WORLD 聲(stim_3 路線)、(b) 尾端再過 Beatrice(stim_4 路線,+~46ms,仍在 150–250ms lookahead 預算內);檔2 DDSP 未動。voice-ownership 美學（07-22）偏 (a)。**路線裁決待 Harry。**

## §E 嘴的討論＋Harry 裁決:兩線平行、不受既有限制約束

嘴的分類學（討論確立）:Beatrice＝**音色嘴**（f0 是乘客,只有相對 shift,無絕對音高輸入,引擎封閉不可重訓）;WORLD＝**音高嘴**（f0 直接輸入,音色逐幀借 Harry 當下頻譜＝母音即時跟嘴型——它贏的隱藏原因）。「專門唱歌的嘴」＝SVC 家族:(內容特徵, f0) 分離輸入,兩邊優點兼得;最小可行＝DDSP-SVC。**Harry:「平行吧」＋「不要被任何限制綁住」**（F9/F10 的 post-viva 訓練凍結不當約束）。

## §F 線 1 落地:world_stream.py 流式 target-f0 WORLD（commit `dce76b43`,停在耳測）

嚴格因果 chunk 模擬,對 G28 三死因逐一反制:逐幀 dio+stonemask f0（1.2s 窗,dio 7ms/s vs harvest 103ms/s）、sp/ap 只算 0.4s 尾窗、幀分析一次即凍結入庫。**零接縫實證:WORLD 合成確定性 → 整樂句重合成逐 sample 重現已發出前綴（runtime 檢查 max diff 0.00e+00）**,前提是扣住實測 ~28ms 的合成尾端邊界效應（HOLD 40ms）。live 可行性數字（take 上）:analysis＋bounded-synth **p95 74ms／150ms hop**（單核 ~50%）;結構延遲 hop150＋guard50＋hold40＝**240ms＋計算 ~70ms**,旋鈕可壓 ~250。live 變體＝bounded（只重合成最後 1s、30ms 等功率 splice）。**待 Harry 盲聽 `out/blind_stream_0724/`（batch vs 流式exact vs 流式bounded,三刺激）**——bounded ≈ exact＝live 路線成立。

## §H 兩線耳測雙過（Harry 快報）

- **線 1 盲聽解盲（`blind_stream_0724`,Harry「3≥2>1」）:stream_exact ≥ stream_bounded > batch**——兩個流式版**贏過 batch**（假說:dio+stonemask 包絡 vs harvest 的差異）,bounded ≈ exact ＝ **30ms splice live 變體成立,檔 1(a) 品質 gate 全過**（FINDINGS F17）。下一磚＝live host（mic→WORLD 嘴→喇叭,腦即時餵 note,嘴介面可換 DDSP）。
- **線 2 ddsp10k 快篩（具名非盲）:Harry「都有像我的音色」**——10k 早期 checkpoint 已保有他的音色身分,方向確認,續訓至 30k 再正式盲測。音高中位差 4 cents、RTF ~0.09（58s/5s,比 Beatrice 0.14/voice 快）。

## §I 線 1 完全體:world_live.py live host（commit `469ba7b9`,待真唱）

耳（YIN tracker＋v2 腦,187.5ms tick）→ 每 tick 絕對音 → bounded WORLD 嘴（F17 配方）→ 喇叭 `--lag 0.35`（tick 量化 187.5＋hop ≤150＋計算;天使是他者,G23 DAF 毒性不適用）。**same-tick 語義**＝盲測過關的那個人聲/天使關係原樣保留,只是整體晚 lag。嘴介面收窄成（mic ring, 每 tick 音符）→ DDSP-SVC 之後可直接換插。驗證:`--notes-json` replay vs wstream_bounded **corr 0.9997／rms 差 0.002**（差異＝收尾 splice 排程）;耳腦全鏈 file 跑通 p95 77ms/150ms hop。跑法:`260722_harmony_brain/venv/bin/python harmony/world_live.py`（`--file take out` 離線;`--list-devices`／`--in-name`／`--out-name`／`--lag`／`--gain` live）。**待 Harry 開麥。**

## §K 女聲天使（girl DDSP）＋ world_live 首次真唱除錯

- **girl DDSP 訓成**:26.1 min（`260618/train_wav/` 4 長檔）切 245 clips,同管線 31 分鐘訓練;val 谷底 **12k（1.195）**、之後過擬合微升 → 取 `model_12000.pt`。**f0 出域才是 Beatrice「音高要夠高才轉」的架構對應物**:她的 f0 分布 p10 197／中位 299／p90 539 Hz（`data_girl/f0_stats.json`）,Harry 中位 147Hz +12st＝294 ≈ 她的正中 → render `-k 12`。產出 `harmony/out/angel_v2_ddspgirl_mix.wav`（同 take 同腦線,可與 ddsp30k 對聽）。DDSP 本身無轉換門檻——模型只會出她的頻譜。
- **world_live 首次真唱 FAIL →「開口出來一點就沒了」→ 延遲帳修正（commit `74665bf7`）**:喉麥/耳/dio/腦全被離線重現豁免（錄 `/tmp/throat_probe.wav` 跑同鏈完美;YIN 46/64 tick、dio 93% voiced、腦在訓練壓力下 p50 19ms）——兇手＝ **lag 0.35 付不起 tick 187.5＋hop 150＋計算 80＋hold 40 的最壞疊加（~450ms）**,播放頭追過發射前緣後永久斷糧。修:lag 預設 0.5、live 每 2s 印 margin/starved 狀態列、underflow 計數。教訓:**兩個量化（tick＋hop）會疊加,latency 帳要用最壞情況算**;優化選項（未做）＝next-tick 語義可砍 187.5ms 但人聲/天使關係變、要重過耳測。**待 Harry 重測開麥。**

## §L autotune 根源戰（Harry:「都差不多,從根源上去調整」）＋ R1／DDSP live 平行落地（commit `fb07a3f6`）

- **參數化 humanize 全滅**（§前段 hlab 三變體,Harry:差不多）→ 確立診斷:autotune 感是**結構性**的——f0 手勢被腳本合成、與聲音其他動作流脫鉤,調參數＝化妝。live 首唱本身 PASS（116s、margin 健康、starved 1.3s 暖機;結尾 margin 負值＝休止顯示假象,非故障）。
- **R1 語料手勢移植（`gesture_lab.py`）**:0619 語料挖出 **769 個真實音符手勢**（cents 輪廓對段自身中位數＝移植他的紋理不移植他的走音;onset 錨定 warp、長音自然速率 tiling、依音程/時長選料）——**零腳本曲線**。盲聽 2×2 `out/blind_gesture_0724/`（腳本 vs 手勢 × WORLD vs DDSP,同線同 take）。**待 Harry。**
- **DDSP live 嘴（world_live `--mouth ddsp`,DDSP venv 跑）**:hubert 內容特徵取自現場人聲、f0＝腦目標曲線（休止前向填充、由 volume 靜音）、volume＝mic 能量×目標包絡（天使跟他的呼吸,他靜它靜——與 WorldMouth 借幀保持不同,記為語義差異）;發射沿用 F17 驗證的 bounded splice（共用 Mouth 基底重構）。file 模式全鏈:**每 hop p50 23／p95 91ms**（比 WORLD 嘴快）。**待 Harry live 開麥（換 DDSP venv 的 python）。**
- 下一個嫌疑犯排隊:若 R1 仍有殘味 → **節奏格線**（80bpm 十六分 tick 量化）是下一個解剖對象。

## §M 載體定罪（FINDINGS F19,commit 見 git log 尾）

- `blind_gesture_0724` 解盲:Harry「其實都還有;單論音質 2,4 > 1,3」＝ **DDSP 欄 > WORLD 欄（複製 F18）,但手勢軸沒贏** → R1 排除「腳本曲線」根源假設(它的科學價值)。
- **載體消融**（身分重合成盲聽 `blind_carrier_0724`,純獨唱）:**original > WORLD 身分 > DDSP 身分**——連音高/內容/音量全是他自己的版本都輸原始 → **autotune 質感主要＝合成載體本身**。Harry 精準命名 WORLD 特徵瑕疵:「同音高的蜂鳴器在背景」＝諧波激勵 buzz。**載體排名在 solo 與 mix 情境會反轉**（DDSP 平滑在天使 mix 裡贏、solo 曝露子音/氣音輸)——嘴要在它的 mix 情境裡評。
- **DDSP live「滑來滑去」隔離為 splice 相位瑕疵**:combsub 每個窗重啟相位 cumsum,定位交叉淡化每 150ms 顫一次(離線無 chunk 故乾淨) → `DDSPMouth._sola` SOLA 對齊落地(±256 樣本相關性搜尋);另 DDSP 專屬 bound 0.7s(p95 91→25ms)、margin 休止顯示改「rest」。**待 Harry 重測 DDSP live。**
- 載體升級選項(明天的裁決):①combsub 餵更多資料重訓(現只用 14.9min;46min 在手,F9「別混舊料」是 Beatrice 時代的實證,DDSP 下未驗)②DDSP-SVC 6.x reflow(更高保真,訓練較重)③enhancer 鏈再驗④WORLD buzz:aperiodicity 實驗(ap_scale 旋鈕已在 world_mouth)。

## §G 線 2 開工:DDSP-SVC 離線 MVP（`SoloChoirCode/260724_ddsp_svc/`,repo 外）

yxlllc/DDSP-SVC **5.0 tag**（6.x 砍掉純 DDSP;CombSubSuperFast＝CPU/MPS 可行的輕量款）。環境血訓:py3.13 建不了 fairseq/numpy1.26 → **venv 用 vcclient-dev 的 python3.10 二進位另建**（vcclient-dev 本身零改動）;requirements 的 `gin`/`wave` 是 PyPI 假貨（gin＝古董 MySQL 套件,真身 `gin-config`）;fairseq 惰性 import patch（contentvec 才需要,我們走 **hubertsoft**（256 維,378MB ckpt,免 fairseq）＋ f0 parselmouth）。資料＝F9 的 0619 clean clips 225 個（train 220/val 5,min 2.02s 剛好 ≥ config duration 2）;preprocess MPS 18 秒完成;**train.py MPS 背景訓練中**（batch 16,log `train_harry.log`）。訓完→用同 take＋同腦線 render→與 stim_3/4 盲聽對決。

**§G 續（訓練收官＋嘴的決賽包）**:MPS 又拆兩雷（DDSP 相位累加 4 處＋nsf-hifigan 正弦器 3 處 `.double()`→float32,58s 相位誤差 ~0.006 rad 可忽略）。10k 快篩 Harry「都有像我的音色」;30k 出爐（58 分鐘,train loss 1.35→0.80）但 **test_loss 從 2k 起就平在 ~1.09**＝14.9 分鐘資料的容量上限,再訓只剩過擬合 → **30k 停訓**（checkpoints 保留,train.py 可續）。RTF ~0.09、音高保真中位 4 cents（透過 target-f0 輸入,和 Test A 同套管——DDSP-SVC 的 f0 是輸入,live 版可直餵腦目標曲線,連 WORLD 中介都可省）。**嘴的決賽盲聽包 `out/blind_mouth_final_0724/`（4 刺激,同一條腦線）:world_stream(bounded) vs ddsp30k vs ddsp30k_enh vs beatrice_target(trim50)。待 Harry。**

## §J 嘴的決賽解盲（Harry「4=1≥2>3」）＝ **DDSP-SVC 奪冠（FINDINGS F18）**

**ddsp_enh＝ddsp > world_stream ≥ beatrice。**同一天內:早上提問「專門唱歌的嘴」→ 晚上它以他自己 14.9 分鐘資料、58 分鐘 MPS 訓練,贏同線四嘴決賽。三個結論:①SVC 架構的 (content, f0) 分離輸入＝天生 target-式(F16 原生滿足)＋母音跟嘴＋voice-ownership 全保;②enhancer 無感（4=1）→ live 走裸 DDSP,省一級;③world_stream 只小輸（≥）＝ live host 的保底嘴,已在 `world_live.py` 跑。**live 化路徑:DDSP-SVC 5.0 自帶 realtime chunked 實作（flask_api/SOLA crossfade）,剩餘 delta＝把「提取+移調 f0」換成腦的目標 f0 曲線注入;world_live 的嘴插槽就是為此留的。**女聲天使候選:girl 26 分鐘同管線再訓一張。NEXT gate:①Harry 開麥 world_live（驗 host 架構/lag/負載）②裁 DDSP live 化開工時點。

## §N（硬體 bench，獨立 session，晚）wire-light Task 3 半截＋Task 4 洞洞板提前開工

- 承 Task 2 跨夜 PASS（`b1bdd569`），因 JST 套件忘帶，順序調整：Task 3 只做「剪」，Task 4 電路部分提前（兩者無相依）。
- **剪段**：DIN 端雙重認定（條上 `IN`/`OU` 焊盤印字＋昨晚實測就是從能動的那頭餵資料）；剪裁標記間距 12.5mm（TOPAI COB 規格）＝長度顆粒度極細；4 段皆於標記正中剪下。**長度表未記**（Harry 現場量未回報）——補進 firmware README 時要收。尾線端接＋逐段 walk test 待 JST。
- **JST 盤點（零採購優先）**：Amazon 提示 07-07 已購 YIXISI JST-SM 240pcs 套件（就是家裡那盒）。需求＝3-pin 公母各 4、2-pin 公母各 2、預壓線 ~32 條 vs 盒內公母各 10、線 80 → **大概率零採購**；回家逐項點（規格逐項對，不是「看起來還有」），真缺才補同款，且要挑 07-25 到貨變體（07-27 到貨的會擠壓 Task 5＝07-28）。
- **Task 4 洞洞板**：施工步驟 1 完成（7-pin 排母＋兩顆 3-pin socket 尾線＋2-pin 電源入口已焊上板）。佈局定案＝**兩軌版（凍結麵包板）**：5V/GND 兩條裸銅軌橫貫全板、所有元件掛軌（燈條座坐兩軌之間、大電解貼座跨軌、330Ω 從 DA 直上 GP1/GP2、1N4007 從 5V 軌上 VSYS）、類比小區（10µF×2＋100k×4）集中一角。**全板共地照 bench 原樣**——竄亮圈已由 min-of-3＋大電解解決（Task 2 驗的就是共地麵包板），獨立類比地線不做。介面決策：板端**排母**（供電側凹接點，帶電短路不到黃銅——07-13 BTL 血訓同型防護）＋公-母 1×7 一體排線接 Pico 公針；電源入口＝有極性 2-pin（JST-SM，防插反）；JST-SM 殼浮板邊、預壓線焊進板（SM 是線對線接頭，殼不板裝）。佈局圖＋血訓＋分步驗收：artifact `claude.ai/code/artifact/c86270e1-671c-4c02-86cc-264d53925906`。
- **NEXT（07-25 續焊）**：焊兩軌（驗 VSYS ≈4.4V、軌 ≈5V）→ LED 掛軌（驗收燈＝留原廠 DIN 接頭那段，A/B 座各插一次跑 demo）→ 類比小區（**GP27 第一次上場**，對 Task 2 數字：底噪 ~250／音樂 ~900–1400）→ `MODE="live"` 整板驗（放歌即亮、靜音 2 分鐘無竄圈）→ JST 帶到後補 3 段尾線＋walk test。bench 期 Pico 吃 USB 沒問題（不吃 USB 是表演規則）。
