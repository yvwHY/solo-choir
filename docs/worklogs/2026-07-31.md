# 2026-07-31

## A. 表現層規則版（兇手 (b) 的藥；commit `79bb1adc`）

**校準先行**：從三格梯同段 `seg_take.wav`（26.1s、19 條長 sustain run）量出真人 f0 紋理的組成（scratch 腳本，數字已入 `direct_mouth.py` EXPR 註解）：

| 成分 | 量測值 |
|---|---|
| 每音偏置 | sd 22c（median \|bias\| 20c，range −37..+34）＝**最大貢獻源，舊規則完全沒有** |
| 音內慢漂移 | sd 13c（≤2.5Hz 帶） |
| 快抖 | 9–20Hz 帶佔殘差能量 19%（含 harvest 噪音，規則版取保守 6c） |
| 顫音 | 深度 20c 峰值（p25 16.5/p75 29.4）、速率 5.9Hz、速率抖動 ~1.7Hz（含噪取半 0.9）、起振延遲 ~0 |
| 進音彎 | \|22c\| 中位、59% 由下勾入、35ms 中位安定（p75 75ms） |

尺對上：我量 sustain cents_sd 32.57 ＝ worklog 07-30 §G 的 32.57（同 `_score` 口徑）。

**實作**：`direct_mouth.py` 加 `--f0-mode express`（additive，target/shift/texture 全不動）——target 骨架（vib_semi=0）＋ `expressive_cents()` 規則合成上述五成分，固定 seed（`--expr-seed` 20260731）＝render 可重現，`--expr-gain` 整體倍率。進音彎只加在骨架瞬間落點（樂句頭/跨 rest/大跳）——級進已有 porta，不疊彎。**與 F19 三敗案（H1/H2/H3）的差異**：當年病灶 (a) 金屬感蓋台、contour 聽不見；(a) 收官後 (b) 才可單獨聽見（②過盲聽＝f0 紋理確為槓桿），且這版有每音偏置（當年三版都沒有）。

**三格梯結果**（`loopback_lab.py packe`，r 版配方只換 f0-mode）：

| cell | breath_v% | cents_sd | v_CPP |
|---|---|---|---|
| ①原聲 | 0.0 | 32.57 | 2.375 |
| ②r 真實 f0 | 8.5 | 32.53 | 2.895 |
| ③r 規則 f0 | 20.5 | **12.95** | 3.064 |
| **③e express** | **13.9** | **26.81** | 2.919 |

cents_sd 12.95→26.81（真人 32.5 的 82%），breath_v% 20.5→13.9 順帶改善。gain 1.2 實測**負優化**（sd 只 +1.5、breath_v% 惡化到 22%）→ 預設定 1.0，剩餘缺口交耳測裁決。

**待 Harry**：盲聽 `out/loopback_0730/stim_3e.wav` vs `stim_3r`（舊規則）vs `stim_2r`（真實 f0 天花板）vs `stim_1`（原聲）。裁決問題＝③e 的 autotune 感是否消失/可接受？（一票制）

**→ 裁決（同日晚）：過關**。Harry：「autotune 感差不多沒了」。**兇手 (b) 結案**＝autotune 案兩病灶全數收官（(a) 07-30 §H–I、(b) 本節）。記入 **FINDINGS F21**（配方＋F19 rule (a) 修正：contour 在 (a) 蓋台移除後確為槓桿）。使用規則：離線/demo 直驅渲染自此一律 `--f0-mode express`（CLI 預設留 target 保歷史 cell 可重現）。殘餘「擠著唱」照舊掛實戴 in-context 驗收（F19 及格線）。

## B. 譜追蹤器 v3——貝氏 forward filter（多假設；commit `8a49869f`）

07-30 §D2 指名的下一段槓桿「多假設粒子」。**技術裁決：不用粒子近似，用精確版**——狀態空間＝譜位置（離散、僅 274–826 格），HMM forward 遞推逐 tick 全後驗更新是 O(n·W) 的 numpy 小事：精確、決定性（無隨機）、天然多峰。分節重複段的歧義以多峰後驗**持有**，等證據自然消歧——v2 單指標被迫「改主意」（5/12 對觸發式重進入 ≥3 次）的問題在機制層消失。速度先驗進 transition kernel（d∈0..4 集中在 r̂）；ε 均勻漏失＝常駐機率式重進入。

**12 對結果**（`rehearse_bench.py`，表存 `pairs/ab_v2_vs_v3.txt`）：

| | A v2 | A v3 | oracle |
|---|---|---|---|
| 全 12 對 voiced | 25.4（0.56 ceil） | **33.9（0.73 ceil）** | 47.3 |
| 高信度 6 對 | 28.4 | **41.6＝首次過 40% 門檻** | 51.8 |
| 高信度 onset | 23.6 | 32.6 | 46.2 |

- **調參不背功勞**：27 組格點（σ×ε×τ）面平坦（mean12 31.7–34.4）。定案 (1.0, 1e-3, 0.8)＝hi6 最高且 same-take 迴歸不破（99.5/100；**τ≥1.2 會讓多假設坐錯自相似等價段落、HOLD 延音對錯 → 95.6%**）。
- **p11 A/ceil 1.41＝「超過天花板」不是 bug**：oracle 是位置天花板（DTW 單調路徑），v3 靠重複段歧義跳到當下最像的段落——對「預測下一顆音」合法，記錄在表頭。
- **殘餘 p09**（ratio 1.27）：v3 21 vs v2 33——低重複度素材上多假設只剩擴散成本。v2 保留（`--track v2`），機制選擇可依素材，不硬求單一贏家。
- **儀器教訓（自己踩）**：07-30 三輪的 driver 沒留檔，exact flags 不可考——我的 v2 復刻 p01 一字不差但其他對 ±5pp（warmup 期 octave 處理差異即可造成）→ **跨表比對無裁決效力；`rehearse_bench.py` 已固化 harness，本輪起表內比較才算數**。
- **live 化備註**：forward filter 天生線上（逐 tick O(n)，n<1000 毫秒級）；r̂ 仍需外部速度估計器（07-30 結論不變），但 v3 對 r̂ 誤差比 v2 寬容（transition 離散 τ 吸收）。

## D. 實戴場工程準備二連（晚；commits `01590bdc` 前後）

- **D1 命題一儀器（`live_v3`，commit 見 log）**：live session 結束自動 mic dump（`out/live_mic_*.wav`）＋印 voiced rms 分布 vs 膝點 0.019——全落在音訊流結束後，live 路徑零改動。yin 前端同口徑（rms<0.005 算無聲），0.005–0.019 危險帶不被儀器吃掉。**seg_take 實測 39% voiced 幀在膝點下**（p50 0.0243/p10 0.0085）＝命題一實戴量測有現成基準可比。
- **D2 表現層移植 live（`645be192`）**：`Mouth._build_target` 的固定 5Hz 正弦（兩天使**同相位**＝07-28 機械齊振根源）→ `ExpressiveF0` streaming 版，參數同 F21，per-voice 異 seed＝天使去同步免費送。guarded：`--expr-gain 0`＝舊行為，**函式級 max-abs-diff 0.0（Regime A 證訖）**；on 路徑偏差 sd 30.9c（Regime B，量級同離線）、渲染 hop p50 23ms 不變。**A/B 材料已出**：`out/lv3_expr_base_mix.wav`（舊）vs `out/lv3_expr_on_mix.wav`（新），實戴場前可先桌聽。

## E. 深夜工程加班二連（Harry 問「軟體面還能做什麼」後解鎖）

- **E1 live_v3 per-voice 出力路由（`d55732c1` 後、commit 見 log）**：發現真缺口——07-29 LED 整合驗在 solo_min，live_v3 只有雙天使混合立體聲＝rig 的 unit 收同一坨、燈看不到各自天使。加 solo_min 同慣例 `--out-map 'u,l'`＋`--dry-ch`（省略＝乾聲不進 unit＝07-29 慣例）；預設 None＝舊立體聲路徑不變。F11 物理源分離正式上 v3。**不替換 solo_min**（它的 LED 路由是驗過的活資產）。
- **E2 排練模式緊檔 v1 上 live（`560c576f`）**：spec 授權「工程先用手上 take 開發」——不用等選歌。排練檔＝反應式 file run 的 `*_notes.json` 擴充（lead 譜線＋k_shift＋兩天使線）；`--rehearse` 載入＝腦旁路，v3 forward filter 逐 tick 對位、天使唱排練好的線（緊檔＝重現），脫稿＝排練錨天然。pair06 端到端（take1 錄→take2 模擬 live）：譜位 239/274 走完（take2 快 21%、r̂ 固定 1.0 仍拉住）、**腦 tick p50 55→7ms＝腦旁路延遲紅利第一塊實據**。單變因 A/B 工具（`--notes-json` 重放）同場加映（Harry 抓到舊 A/B 對和音組成不同的混變因）。file mode 讀檔改 soundfile（float32 pairs）。
- 順帶兩筆命題一基準：seg_take 39% voiced 幀在膝下 vs pair06 錄音 1%＝儀器兩端都有樣本。
- 殘餘（排練 live v2 的下一段）：外部速度估計器（r̂ 仍固定值）；預先服務 j+lookahead（吃掉 tick 等待）；活檔旋鈕（天使線現場生成）；真 demo 歌材料換裝。

## F. v3 lab——測試 app（`b4c7b6ed`；Harry「3 可以先給我嗎」＋「不照過往限制，直覺給我」）

單檔 web 實驗台 `harmony/lab.py`＋`lab_ui.html`：**不走 app/ 三層 pywebview 模式**——引擎照舊是 live_v3 子行程（音訊它自己管），lab 只是 stdlib HTTP server＋SSE 串 stdout＋start/stop 兩個 POST。零新依賴、每個端點可 curl。UI 一頁：live/file 模式、裝置選單、key/lag/gain/expr-gain/anticipate、out-map/dry-ch、**排練檔下拉**（自動掃 `out/` 含 lead 的 notes.json）＋r̂、2s 心跳狀態列（in/ticks/U/L/margin/starved）、收官線高亮（mic level/rehearse/mic dump）。Stop＝SIGINT＝收官報表跑完才退。無頭驗證全鏈過；**live 模式（真開 mic）留 Harry 首玩**。啟動：`venv/bin/python lab.py`（自動開瀏覽器）。

## G. lab 首玩回收＋合成 live 測試（傍晚）

- **Harry 首玩踩三雷**：①file 空路徑穿檢（`Path('')`＝cwd 存在）②keydet 真歌又鎖垃圾（A#/E、conf 0.05＝07-30 死案重演，UI 加警語「唱真歌一律填整數」）③live `--anticipate` 跑出 margin **-7110ms**/starved 20.8s/50s。修①②＋心跳加 **brain/hop 近期 p95 遙測**（`183e53ad`）。重啟後反應式健康：margin +314ms、starved 2.0s/138s、brain 64/hop 56。
- **合成 live 測試上線**（app 測試金字塔第 3 層）：BlackHole 2ch 當虛擬 mic（feeder 播 wav）＋輸出 gain 0 → 真 sd.Stream/三執行緒全鏈無人測試。交叉驗證過：mic dump 命題一數字與 file 模式同段一致（膝下 40%）＝live 輸入鏈忠實。
- **兩條真發現**：(a) **brain tick p95 242ms > 187.5 預算**（反應式、桌面負載下；對照健康輪 64ms、07-29 單 forward 38ms）＝腦尾延遲對機器負載敏感 3–6×；anticipate 再 ×2 → 斷供案近乎定罪（不是 lag，是腦爆預算擠死同線的嘴）。**藥候選＝ticks/hops 拆執行緒**（音訊路徑 threading，Regime B 全套驗證，未動）。(b) 無聲輸入時 margin 深負＋lower 在 REST 上出音 48＝靜音邊界儀器口徑＋腦 REST 行為，記帳待查。
- speech-to-speech（HF repo）Harry 問有用嗎 → 裁決不開線：語音對話級聯（STT 丟音高時值）、延遲量級不對；chunked 串流養分已有自家路線②。

## H. ticks/hops 拆執行緒（Regime B 全套；commit `ab524ee0`，Harry 拍板「照 Regime B 做」）

- **方法**：BlackHole 合成 live probe（`live_probe.py`，Popen＋SIGINT——**血訓：shell `&` 背景起的行程 SIGINT 被 POSIX 設成忽略**，pkill -INT 全空包，第一輪基線因此跑到 100s 才被 SIGTERM 收掉）；同段 seg_take、stream 開了即餵（消除靜音前導污染）；負載受控＝8 個 CPU 燒機。
- **判別結果**（預感＋負載）：舊 starved **24.3s 持續爬（死亡螺旋）** vs 新 **13.3s 凍結（自我回復）**；平靜預感 1.9→**0.0s**、平靜反應式 0.6s 無回歸。拆線把「腦爆預算→嘴陪葬→追不回」變「降級可回復」。
- **劇情反轉記錄**：即餵之下**舊碼平靜預感其實健康**（starved 1.9s、brain p95 127＝07-29 水位）→ 下午災難＝「靜音期預感亂唱」＋「負載尖峰」合謀，非單一結構問題。預感模式在重載機器上維持謹慎（絕對值仍 13.3s）。
- **儀器口徑掛帳確認**：margin 的 `min(frontier)` 含休止聲部凍結值＝深負假警報（§G (b)），判讀一律以 starved（per-voice、note-gated、07-29 修過口徑）為準。
- file 模式不走 worker()＝程式路徑零改動；hop p95 併發下 31→93ms（MPS 共享的正常拉伸，cadence 未破）。

## I. 呼吸被唱案——live 嘴四症耳訴的定罪與治癒（晚；commit `ccafe9b0`）

- **起點＝Harry 耳訴**：live 應答式「嘴沒達到理想狀態」，追問四選四（斷斷續續/autotune 感/音色假/滑音黏）＝全面性劣化。先排除命題一：他 17:30–17:47 四段真唱 mic dump 膝點下僅 0–10%（p50 rms 0.05–0.08）＝軟發聲牆無罪；17:53/18:2x 的 39–40% 全是合成測試重播 seg_take。斷供也解釋不了（17:47 場 starved 僅 2.0s/138s）。
- **判別實驗**：同 seg_take＋同音符線（seg_notes→`--notes-json`）＋同模型，離線 express（stim_3e，昨晚盲聽過關）vs live 串流嘴（file 模式＝同 hop 程式路徑）——**breath_v% 13.9 vs 47.5–54.1**，cents_sd 完全相同（26.8）＝表現層移植無罪、**離線 (a) 修復從沒上 live**。嘴在他一半的呼吸/子音上唱出帶音高的音＝「autotune 感/假/黏」主源。
- **第一版藥（vol 條件歸零）無效**：uv 段能量只降 8%——`volume_frames` 只進 unit2ctrl 當條件，輸出＝combtooth×預測濾波器，歸零是 out-of-distribution 條件、網路照樣出聲（離線版被迫走 f0 哨兵＋透傳的同一堵牆，07-30 註解早寫了「沒有 per-frame ap 入口」）。
- **正解＝出口遮罩**：`_update_vuv`（批次 FFT 自相關峰＋RMS 0.005＋因果遲滯 40ms 進 uv/回溯標記/單幀強週期回 voiced；seg_take 對離線 voicing_mask 一致率 95%、門檻面平坦）→ SOLA 之後乘在 wav_b 上。live 比離線簡單：不需透傳（真呼吸在空氣裡）、無垃圾 f0（f0 來自腦）。
- **數字**：breath_v% 47.5→**4.2**（比離線 13.9 低＝離線透傳保留真呼吸、live 靜音）；uv 段 rms 0.0178→0.0034；cents_sd/v_CPP 不動；hop p50 26→30ms。`--uv-gate 0`＝舊行為（新碼全在 guard 內；byte 比對不適用＝SOLA 離散選擇使管線 run-to-run 非決定性，同旗標兩跑 max diff 28024——07-31 表現層做函式級證明的同一理由）。
- **儀器筆記**：uv 覆蓋 18.5% vs 離線 15.4%＝輕微過 gate（+3pp），風險＝軟起音被剪；cents_sd 無損說明 voiced 主體未傷，**終判掛耳測**。
- **待 Harry 耳測**：`out/loopback_0730/` 的 `ab_A_offline`（離線）/`ab_B_stream`（gate 前）/`ab_C_stream_uvgate`（gate 後）三格＋live 實測（lab 開 `--uv-gate` 預設已開）。聽點：①呼吸段 autotune 感是否消失 ②軟起音有沒有被剪的 gate 感。

## J. 嘴腦解耦 free-run——「即時」的第一性原理重推（深夜；commit `74827444`）

- **起點＝Harry 兩問**：「我還是想要他是即時的」＋「不要有限制 好好想想」。第一性原理拆 lag 0.5–0.6s 組成：tick 187.5（腦的 tokenization 選擇）＋hop 150（排程選擇）＋**嘴等腦（`lim` 被 `n_ticks*TICK_SAMPS` 硬鎖）＝可刪的耦合**。因果硬限只有偵測 ~50ms；**起音同步免費**（DDSP volume＝他的 mic，天使本來就跟他呼吸）——晚到的只該是音高修正，不該是整體等待。墓園複核：更快的捷徑（whammy 移調 G26、流式 WORLD G28）全死於音質、F16 target-f0 鐵律兩度確認＝快路裡唯一有活證據的是 chunked DDSP（F18 點名，Phase 2）。
- **機制**：正式狀態（expr rng/porta/env/樂句記帳）只走過「tick 已決定」的確定區；確定前緣→渲染前緣的臨時區每 hop 從確定態快照出發、「維持現音」建構、用完即棄。無分歧＝重建逐位元相同 → 發射 append-only 保舊質感；新 tick 推翻假設＝回捲發射前緣到分歧 tick 重貼（splice crossfade 接縫）＝彎音落地。換音聽感從「等 0.5s」變「~200ms 掛留式殘留後彎入」（合唱 suspension 語彙）。
- **兩個首版血 bug（file 等價檢查抓到）**：①樂句在 render 尾關閉、快照在 render 前拍＝已關樂句復活（修＝確定區 rest 撐起的關閉提交回快照）②`phrase["n"]` 隨回捲凍結＝每 hop 重貼整窗、v_CPP 2.92→2.76（修＝發射前緣改全域單調 `self.emitted`，分歧回捲唯一例外）。修後 file 模式 cents_sd/v_CPP 落回舊路徑 run 間噪音帶；**殘餘真差異＝breath_v% 6.2–7.7 vs 舊 4.2–4.6（樂句邊界 +2~3pp），掛耳測**。
- **合成 live 判決表**（`live_probe.py` 本輪固化進 repo——§H 的 probe 當時沒 commit＝儀器教訓再犯）：lag 0.60 舊 2.1s→**0.0s**；**lag 0.45 舊 12.2s 崩潰→0.2s 健康＝07-29 判死翻案**（當年死因＝舊架構＋斷供儀器污染＋MPS 冷啟，三個都收掉了）；lag 0.35 新 3.3s＝下一層地板（hop 節奏 150＋guard 50）。
- **已知代價（誠實面）**：樂句「進場」仍等腦（臨時區 rest 不開樂句、彎音救不了 opening）；極端腦尖峰時修正落在播放頭後＝偶發接縫；`--free-run 0`＝舊行為。
- **Phase 2 排隊**：hop 節奏 150→75ms＋BOUNDF 窗壓縮（衝 lag 0.25–0.3）；chunked DDSP 串流嘴（F18 預言路線）；排練模式 lookahead 預渲染（已知材料延遲 0/領先）。
- **待 Harry lab live**：反應式 `--lag 0.45`（比對 0.6）。聽點：①反應變快了嗎 ②換音瞬間的掛留彎入自然嗎 ③呼吸邊界殘留/gate 感。

## K. lab 二玩回收——free-run v2 單流化＋keydet 拒鎖（深夜）

- **Harry 耳訴二連**：「有變快但不夠理想」＋「拉長音長到一定程度像重複播放同一個音」。lab 遙測回收：該場 `--key auto` 又鎖垃圾——**`[keydet] locked G# +4 st (conf 0.01)`＝整場和聲錯調**（超時硬鎖，07-30 死案第三次咬人）；latency 面其實健康（starved 4.8s/300s、無螺旋）＝「不夠理想」至少一大塊是錯調污染。
- **長音循環感＝v1 結構錯誤定罪**：臨時區每 hop 從確定態快照重起算 expr → 每 ~150ms 發射塊各自帶重新起算的顫音/漂移相位；長音時音符不變、聽感全靠紋理 → 塊狀循環。**v2＝`_build_hold` 單流**（`c5f58b30`）：狀態每幀只走一次＝紋理天生連續；未決定 tick 用 hold 記入 `eff`、tick 邊界快照；對帳推翻假設才回捲重建＋發射重貼。v1 的 conf-restore/prov_held/樂句關閉提交全刪＝碼更短。file 等價過（cents_sd 26.84/v_CPP 2.921 正中帶；breath +3pp 照舊）；probe 0.45 starved 0.7s/28s。
- **keydet 拒鎖**：超時＋conf<0.03 → 拒鎖、回退 shift 0、大聲警告改填整數。**離線雙 take 印證他這首歌＝B 大調（conf 0.035/0.048 互相印證）→ key 填 +1**。
- **待 Harry 三測**：①key 填 +1 重跑（和聲該對了）②長音循環感還在嗎（v2 結構保證連續，終判耳測）③反應速度基準不變 lag 0.45。

## L. 低延遲旋鈕掃描——0.35 可用、0.3 撞 MPS 牆（深夜；commit `2eb1ec72`）

- `--hop-ms`/`--bound-ms` 參數化＋probe 掃描：**lag 0.35（hop 100/bound 450）starved 1.5s/28s＝可用實驗檔**；lag 0.45 hop150＝甜蜜點（0.2–0.7s）；hop 75 反而更糟（成本>節奏）；0.30 仍 4.0s。
- **關鍵發現：縮窗沒縮 hop 成本（57–67→62–76ms 不動）＝瓶頸是 MPS 佇列搶佔（嘴排在腦後面等），不是計算量**。0.3s 以下的正解＝chunked 串流嘴（渲染一次制，F18 路線）＋MPS 佔用重排——下一場主菜。
- lab 加「進階參數」通用欄；server 已重啟載新碼。**Harry 低延遲實驗配方：lag 0.35＋`--hop-ms 100 --bound-ms 450`**。

## M. MPS 牆第一塊磚——units 共享（深夜續；commit `b697d5d4`）

- **上游 realtime 方案讀後**：DDSP-SVC gui.py 也是「每 block 重算整個 context」（extra 預設 2s）＋SOLA/crossfade 出口——「chunked＝渲染一次」是誤解，上游靠 GPU 硬扛。我們的 bounded 窗機制本質同款 → 槓桿不在 chunking，在砍 MPS 派工。
- **分段遙測定位**：hop p50 25ms＝enc 16（hubert，2/3）＋fwd 8。兩張嘴 content 同源（同一段 mic、同一個 hubert）→ **units 共享**：窗起點對齊絕對 512 格、每 hop 週期快取、第二張嘴切片即用。實測 lower enc 16→1ms、hop 24→11ms。
- **probe 梯定案**：0.45 乾淨（0.2–0.7s）／**0.35（hop100/bound450）1.3s 可用**／**0.30（hop75/bound400/window384）1.6s 實驗檔**。腦 heartbeat 仍 ~110ms＝隊列等待非算力 → 再往下＝腦離開 MPS 熱路徑：**排練模式腦旁路（7ms）＋低 lag＝demo 原生配置，下一段主菜**。
- **血訓**：`torch.mps.synchronize()` 在 hop 線與 tick 線的 encode 撞 Metal assertion 全行程炸掉——跨線計時同步一律讀值（.item()/.cpu()）。
- file 指標：cents_sd 26.67/v_CPP 2.930（窗對齊微移，帶邊緣、方向偏好），總驗掛 Harry 耳測。

## N. lab 三玩回收——波波聲定罪＋Stop 失靈（深夜終場；commit `5affcb2c`）

- **Harry 三訴**：新嘴「有比較清楚」✓；「live 始終不如離線檔順/清晰＋波波聲（換模型前就有）」；「Stop 按了不會停」。
- **Stop＝我造的孽**：lab server 用 nohup+& 重啟 → SIGINT SIG_IGN 遺傳給 live_v3 → Stop 空包（§H 血訓第三次咬人）。三層修：live_v3 強制還原 handler／lab 15s 升級 SIGTERM／`--dump-out` 診斷儀器固化。
- **波波聲定罪**：AM 調變譜——串流 5–9Hz（hop 節奏 6.7Hz）6.4–7.4 vs 離線 1.8＝接縫線性淡化在相位殘差下掉能量。藥＝上游 phase_vocoder numpy 移植進 splice_emit：**file 6.44→3.75、live 7.36→4.31**、常規指標無傷；成本 hop +11ms（0.35 檔 starved 1.3→2.1s＝PV 換順暢的定價）。
- **殘餘 gap 記帳**：live AM 仍 4.3 vs 離線 1.8、2–4Hz 慢泵 10.5 vs 2.3＝回捲重貼/樂句機制的下一層；live vs 離線的「順/清晰」差距未全收，續戰。
- 換新嘴（260730）Harry 首判「有比較清楚」＝實戴順裁的初步正向，正式一票留 in-context 場。

## O. StreamMouth 真串流嘴——「為什麼 solo_min 可以」的架構答案（凌晨；commit `36d73079`）

- **Harry 四訴直擊要害**：「波波沒少／都很碎／為什麼 solo_min 可以又即時又順？」——答案＝solo_min 是裝配線（每樣本合成一次、狀態連續、零接縫），DDSPMouth 是批次渲染器硬塞進 live（整窗重算＋SOLA＋貼尾＝每秒 6.7 接縫；PV 只能磨不能除）。
- **StreamMouth**：combsub `initial_phase` 入口＝激振器相位跨塊續接 → 零 SOLA 零重渲染；相位推進與模型同式重算；istft 左右邊窗 E=2 塊丟棄（右緣不補＝每 chunk 尾振幅凹陷，首驗抓到）；彎音免費（幀最後一刻建構＋porta 滑入）＝free-run 的 eff/快照/回捲在此架構全不需要；休止跳 model。
- **bench**：file breath 4.6（歷來最佳）/hop p95 38ms（splice 94）；live 0.45 starved **0.0s**、0.30 hop75 **1.8s（無需 bound/window 削減）**。AM 儀器兩路互有勝負（file stream 略勝、live splice+PV 略勝）＝儀器到極限；**2–4Hz 慢泵兩路皆在（~10 vs 離線 2.3）＝未解軸記帳**。
- **待 Harry 耳測終判**：`--mouth stream`（預設）vs `--mouth splice`（進階欄切換）——波波/碎/長音三聽點。

## P. 波波聲結案——uv 遮罩抖動定罪（跨夜；commits `49caf9a6`…`d0ec0438`）

- **排除法全鏈**：回授假說死（耳機/喇叭都有）→ 輸入鏈假說死（io 旗標遙測上線＋lab UI 第九格；Harry 實測 io 0/0）→ 剩兩嘴共用層 → **`--uv-gate 0` A/B：「波波沒了」＝定罪**（同時 starved 0.0、Stop 正常）。v1 遮罩「出場單幀」＋無位準佐證＝軟唱邊緣高速開關。
- **v2c 防抖三輪**（出場 3 幀/停留 200ms/分層門檻/軟底/25ms 坡）：切換 1.86→1.32/s，但呼吸抑制被犧牲（4.6→36.3）＝這組門檻無雙贏。
- **裁決（一票制）**：今晚遮罩關閉後 Harry 零呼吸投訴＝遮罩前提（早間呼吸被唱 54%）已被 F21 表現層＋StreamMouth＋260730 新嘴超越（F19 in-context 翻轉再一例）。**live 預設 --uv-gate 0、v2c 留選項**，實戴場回歸再議。
- 途中血訓：`--uv-gate 0` 曾被填進 key 欄（`--key --uv-gate 0`＝argparse 吃掉、session 沒起來）——lab 欄位誤填無防呆，記帳。
- **今晚收官狀態**：lag 0.30–0.45、io 0/0、starved ~0、波波沒了、新嘴較清楚——live 四訴（斷/autotune/假/黏）＋波波＋碎全數收斂，剩實戴 in-context 總驗收。

## C. 未解 / 待辦（日終前快照）

- ~~①表現層 stim_3e 盲聽~~ **DONE：過關，(b) 結案**（§A 裁決、F21）。②粒子 v3 已過量化門檻，下一步＝接進 live 排練模式（需外部速度估計器＋選歌後真材料）。
- Harry 側今日計畫未動：③選歌（唯一 blocking）④實戴 in-context 場（否證兩題＋新嘴驗收＋LED＋`--lag 0.6` A/B）。實戴過關 → 議拍攝解凍。
- p09 型素材（低重複、趕拍）v3 輸 v2 的機制選擇問題——留給真 demo 歌材料定案，不預先造自動切換。

## P. 硬體展示結構七迭代收官——一件式純梯形錐體（下午實測→跨夜 CAD，獨立 hardware session）

下午實戴整合測試 4 發現（DAEX 太大聲/音量不平衡、unit 1 疑重焊、unit 2 小 speaker 震動大、cone 架不利聲音）→ 晚間展示結構重想，Fusion 內七次迭代當夜收斂：

橫桿(機械串音) → 花束 hub(共構) → **bayonet 卡口底座**(止轉牆+0.2 卡窩,六狀態探點掃描全 PASS) → 重力深窩(Harry「能不能不用驗手感」) → 配重杯(Harry「cone 就是蓋子」) → 一體裙擺圓柱(Harry 給一體式喇叭參考圖) → 方錐(Harry 要平面貼桌) → **終版 v7＝一件式純梯形錐體 `vu2_pyramid`**(Harry 定案)。

- **終版**:底 65²→口 104²/h90、壁底 2.0(shell+taperAngle 12.23°)、樓面 ⌀25/35/50 黏貼定位環、側出線孔 ⌀12@z14。**Harry 關鍵洞察「固定=黏,桌子就是配重」**——沙倉/杯/卡口/silicone 介面全退役;立放 VHB、躺放稜面平貼(不滾;可刻意面耦合桌板=「貼桌很大聲」的可控版)。
- **物理紅線(貫穿全程)**:殼=exciter 振膜必須輕(06-24),「灌重進 cone」否決;陶瓷/水泥參考圖=錐盆單體+死箱體,物理不同,列為聲壓備援線(需採購)。
- **驗證**:每迭代體積帳+pointContainment 探點(bayonet 版並跑六狀態旋轉路徑掃描);Harry 抓尺寸 bug×3(cone 內徑 65 放不平/⌀8 過不了 JST/沙槽灌口 12mm 太窄);Claude 血訓×2(曲版樓面幾何孤島=裙擺取代下段壁後樓面須樓肩環焊回;刻定位環 annulus profile 誤選寬帶吃掉貼面)——皆修復重驗。
- **出件**:`prints/260731_prints/`＝`vu2_pyramid.stl`＋gcode 兩檔(0.2 層 4h38m/83.3g、0.28 draft 3h22m/82.8g,皆零支撐、brim 6)＋README 工單(貼法/出線/兩種耦合驗收)。舊 vu2_cone C 夾版 superseded。
- **軟體側兩釘(同下午 session)**:USB mic 輸入直選 `USB PnP Audio Device`(引擎 `sd.Stream(channels=(1,…))` 只讀輸入 ch1,mulit-set ch1=RØDE);喇叭在身 YOU 必 mute(乾直通→開放 mic 嘯叫,playbook 第 15 條重演)。
- **NEXT**:印 draft 首件(3h22m)→ 方 vs 圓 A/B＋躺放兩種耦合耳測 → 過閘門印四件。未 commit:`prints/260731_prints/`＋HARDWARE_ROADMAP/STATE 修改。
